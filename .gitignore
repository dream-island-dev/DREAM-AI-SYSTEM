node_modules

# סודות וסביבה מקומית — אסור שיעלו ל-GitHub
.env
.env.local
.env.*.local

# build
/build

.vercel

# Supabase CLI local temp
supabase/.temp/

# temp SQL runner
.run-sql.mjs
.verify.sql

# Make.com blueprints — מכילים סיסמה, לא לגיט
make-blueprint.json
make-blueprint-spa.json

# Personal Telegram remote-exec bot — hardcoded bot token + unrelated to this app, not for git
.claude/claude_bot.py

# Claude Code local settings — logs approved shell commands verbatim, which can
# include tokens/secrets typed during a session (e.g. curl with Bearer header).
# Never commit — local-machine only.
.claude/settings.local.json
