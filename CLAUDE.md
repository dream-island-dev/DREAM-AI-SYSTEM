---
description: 
alwaysApply: true
---

description: Dream Island AI System (XOS) Core Architecture & Rules
alwaysApply: true

CLAUDE.md — Dream Island AI System (XOS)

קובץ זה נקרא אוטומטית בכל שיחה. הוא מקור-האמת העדכני של המערכת. היסטוריית סשנים מלאה מועברת ל: claude_history.md (שמור שם לצורכי ארכיון, אל תציף את ה-Context כאן).

0. חזון הפרויקט ועקרונות יסוד — Project Vision & Core Principles

חמשת עקרונות הברזל (DNA)

1. ZERO DATA LOSS: אסור להשמיט שורה בשקט בייבוא. שורה שלא עובדה מוצגת כ"שגויה" עם הסבר; היא לעולם לא נעלמת מהתצוגה בלי עקבות (ezgoParser.js).
2. INTELLIGENT UI (Disable, Don't Hide): כפתורי פעולה תפעוליים לעולם לא נעלמים. אם פעולה לא חוקית כרגע — הכפתור נשאר גלוי, מנוטרל (Greyed out), עם title שמסביר למה ומה לעשות כדי לאפשר אותו (GuestsPage.js).
3. FAIL VISIBLE: לעולם לא להסוות ערך DB לא-צפוי מתחת ל-fallback "תקין-מראה". הצג אותו כ-⚠ גלוי ב-UI, אל תמפה אותו לערך ברירת-מחדל שמסתיר את הבעיה מהצוות.
4. UNIVERSAL ARCHITECTURE: שימוש בקומפוננטה אחודה לניהול טבלאות דאטה (EditableGrid.js) שמקבלת Schema ומספקת ייבוא + תצוגה + עריכה לכל מקור דאטה (סוויטות, שוברים, מלאי).
5. SINGLE SOURCE OF TRUTH: טבלת guests היא הפרופיל המוזהב (Golden Profile). כל לשונית ב-UI קוראת/כותבת אליה. טבלאות אחרות (suite_rooms, bookings) הן נתוני תמיכה בלבד.

UX Guardrails & שליחת הודעות
- Zero-Spam Policy: לעולם לא לשלוח broadcast/הודעה אוטומטית אם האורח מבוטל (status='cancelled'). דגל needs_callback הוא התראה לצוות בלבד ואינו חוסם את הבוט או ה-Cron.
- Graceful Fallback: אם דאטה דינמי (כמו spa_time) חסר, מנקים את ה-placeholder ולא שולחים {{VARS}} גולמי לאורח.
- Template Awareness: הודעות מעבר לחלון 24h של Meta חייבות תבנית מאושרת מראש (sendViaTemplate). אם החלון סגור, ה-Inbox מחזיר סטטוס window_closed מיידית. `sendViaTemplate` מיישר מספר פרמטרים לפי התבנית החיה ב-Meta (`_shared/metaTemplateVars.ts`) — `dream_room_ready1` מאושרת עם `{{1}}` בלבד (שם); שם סוויטה נשלח רק בנתיב session/Whapi. **`room_ready` (חדר מוכן):** כש-`GUEST_WHAPI_SUITES_ENABLED` — תמיד Whapi + `bot_scripts.room_ready_reminder` (לא Meta); לא תלוי ב-`guests.room` (יכול להיות ריק מול `roomId` מ-AICopilot/housekeeping).

1. הגדרת המערכת וסטאק טכני

Dream Island Resort Management System (XOS) — אפליקציית ניהול מלון יוקרה בעברית (RTL) הכוללת ניהול תפעולי ופלטפורמת AI. המערכת מושכת מידע ישירות מ- EZGO .

| שכבה | טכנולוגיה | הערות חשובות |
| ------ | ------ | ------ |
| Frontend | React 19 · CRA | SPA, Hebrew RTL, ללא ספריית ניתוב (רינדור דרך useState ב-App.js) |
| Backend | Supabase Edge Functions | קוד ב-Deno / TypeScript |
| Database | Supabase PostgreSQL 15 | אכיפת Row Level Security (RLS) בכל הטבלאות |
| AI Primary | Gemini 2.5 Flash / 2.0 Flash | משמש לשיחה חופשית, תמלול הודעות קוליות וסוכני Inbox |
| AI Fallback | Claude Sonnet 4.6 | גיבוי אוטומטי בכשל או לפי הגדרת preferred_model |
| WhatsApp | Meta Cloud API + Whapi.cloud | Meta לשיחות ישירות (1:1), Whapi לניהול קבוצות צוות וקליטת ריאקציות. אורחי סוויטות **ויום-כיף**: כל ה-outbound (כולל אוטומציה) דרך מכשיר הסוויטות כש-`GUEST_WHAPI_SUITES_ENABLED` — לא תבניות Meta (יום-כיף שנשאר על Meta נכשל בלולאת cron על `dream_checkin_reminder_v2`). Stage 1 שפספס מועד T-2 (ייבוא מאוחר) מופיע בתור חי כ-`missed_window` לשיגור ידני/מכה — לא נעלם כ-`date_passed`. **Late-import catch-up (2026-07-21):** אותו כלל לכל צינור הסוויטות/בילוי-יומי (`night_before`, `morning_suite`, `mid_stay`, mirrors) דרך `isLateImportCatchupEligible` — כל עוד השהייה פעילה (`departure >= היום`) והדגל לא נשלח; cron לא שולח אוטומטית. **Stage 3 morning (2026-07-14, migration 206):** `morning_suite`/`morning_welcome` חלון 06:00–10:00 / 08:00–10:00 (Israel) — אחרי התקרה `missed_window` בלבד (לא שליחה אוטומטית); Whapi `stage_3_morning` מסונכרן לתוכן Meta (12:00 כניסה / 15:00 סוויטות). קבוצת «בקשות אורחים» (`guestAlertWhapiNotify`): כרטיס בעברית + לינקים ל-Inbox/לוח בקשות — בלי תרגום HE→EN (זה לקבוצות תפעול שדה בלבד). ACC Live Queue/Override (`automation-queue`'s `effectiveWhapiGuest` + `AutomationControlCenter.js`'s `isWhapiRoutedQueueItem`): צ'יפ «מכשיר סוויטות» רק כש-`guest_suites_channel=whapi` (אוטומציה) — לא על כל סוויטה. Whapi-first guest outbound (2026-07-13): `broadcast`/`payment_and_workshops`/`stage_2_pay` וכל `force_channel=meta_template` ידני חסומים כברירת מחדל לאורח Whapi-eligible — Meta רק דרך `ALLOW_META_GUEST_TEMPLATES=true` (secret, כבוי כברירת מחדל) כ-escape hatch לתקלת מכשיר. כפתור «🔵 Meta Template» ב-ACC Override נשאר לחיץ (Disable-Don't-Hide) — לא מוסתר — עם title מסביר; אם ה-secret כבוי הוא יחזיר שגיאה ברורה בעברית, לא ישלח בשקט. **SOS dual-path (P0, 2026-07-13):** מכשיר הסוויטות יכול להיחסם ע"י וואטסאפ (spam/automated-messages, נצפה ~17ש') ללא התראה מראש — `WHAPI_GUEST_SOS_META=true` (secret) בתוך `isGuestWhapiSuitesEnabled()` עצמה מנתב את כל ה-outbound לאורחים (כולל room_ready שקורא לפונקציה ישירות) ל-Meta Dream Bot אוטומטית, ללא צורך לגעת בכל קורא בנפרד; `ALLOW_META_GUEST_TEMPLATES` נחשב true אוטומטית בזמן SOS. Badge אדום ב-ACC Pulse. **לא מכסה** את אקרוקת החדרנות (`WHAPI_HOUSEKEEPING_GROUP_ID`, inbound על אותו מכשיר) — נשארת תלויה בסיום החסימה + חיבור מחדש. כיבוי אחרי הבאן: `npx supabase secrets unset WHAPI_GUEST_SOS_META`. **Per-cohort channel control (P0, 2026-07-13, DEPLOYED):** `GUEST_WHAPI_SUITES_ENABLED` (env) הוחלף בבחירת ACC עצמאית לכל קבוצה — `bot_config` rows `guest_suites_channel` (`whapi`\|`meta`, ברירת מחדל `meta`) ו-`guest_daypass_channel` (`off`\|`whapi`\|`meta`, ברירת מחדל `off`) — migration 196. **פיצול אוטומציה/ידני (2026-07-28, עודכן 2026-08-03):** `guest_suites_channel` = ערוץ **אוטומציה** בלבד (ברירת מחדל **Meta**); כל שלבי הסוויטות (כולל `pre_arrival_2d`) עוקבים אחריו — חלון 24ש׳ פתוח → `bot_scripts` (סשן), חלון סגור → תבנית Meta; `canStaffSendViaWhapiSuites()` = שליחה ידנית מ-Inbox/Override תמיד דרך מכשיר הסוויטות (חוץ מ-SOS). `_shared/guestWhapiRouting.ts`'s `primeGuestChannelConfig()` נטען פעם אחת בראש כל handler (7 קוראים) לתוך cache מודול סינכרוני — כל ~20 אתרי הקריאה הקיימים נשארו ללא שינוי חתימה. `isStageEffectivelyActive` חוסם יום-כיף לגמרי כש-`daypass_channel="off"` גם אם `is_active=true` (OFF אמיתי, לא רק דילוג Whapi) — Override ידני לא מושפע. **Whapi Anti-Ban Velocity Guard (2026-07-23, migration 274, DEPLOYED — נבנה אחרי באן שני, ~24ש', מ-`WaiterPulseDispatchPanel`):** `_shared/whapiVelocityGuard.ts` — כל שליחת Whapi 1:1 שאינה לקבוצה (`@g.us`) מסווגת `guest` (hot/warm/cold לפי היסטוריית inbound, לא לפי חברות בטבלת `guests`) / `staff` (פטור ממכסות אך עדיין כפוף ל-`global_min_gap_sec` גלובלי אחד, 10ש') / `group` (פטור מלא). `sendWhapiTextGuarded`/`sendWhapiImageGuarded` מחוברים ב-6 קבצים בלבד (מתוך 25 עם `sendWhapiText` בקוד): `whatsapp-send`, `guest-club-broadcast`, `oritGuestOutbound`, `executiveAssistant` (כולל `request_missing_arrival_times` שהיה ללא throttle), ונתיבי-אורח בלבד ב-`whapi-webhook`/`whatsapp-webhook`. **תפוצה (3+ נמענים) חייבת תור:** `whapi_outbound_jobs` + jitter 8–15ש' ב-`scheduled_after`, מנוקז ע"י `whapi-queue-drain` (pg_cron נפרד כל דקה — לא `whatsapp-cron`) — `WaiterPulseDispatchPanel` עבר לנתיב הזה. **Inbox תפוצה קבוצתית/audience** חסומה לגמרי מ-Whapi (Disable-Don't-Hide) — Meta Template בלבד. **Cooldown אחרי באן:** `bot_config.whapi_velocity_state.cooldown_until` — חוצה מכסות + חוסם `cold` לגמרי; נזרע ACTIVE (48ש') ב-migration 274, לנקות ידנית רק אחרי אישור שהמכשיר תקין. `notification_log.status` הורחב ל-`rate_limited` (ר' §10 ב-playbook לפרטים על ה-CHECK constraint). |

1.1 Guest Experience Survey + Spa warm-up + Guest Club (DEPLOYED 2026-07-14)

**מועדון ב-WA (2026-07-26, migration 276):** ברירת מחדל **פורטל ראשון** — `portal_offer_enabled=true` (migration 278): סקר חיובי → הצטרפות למועדון בפורטל + סקשן קבוע «🌴 מועדון הטבות» ב-`GuestPortal.js`; גוגל ב-WA אחרי join/decline. WA גיבוי: `positive_feedback_reply` מיד → תור `guest_club_invite` (3דק') עם `{{portal_url}}#club` למי שלא הצטרף בפורטל. שלב ACC `guest_club_invite` + פאנל «מועדון + משוב». Fallback סקר סוויטות ביום עזיבה @19:00 בלי Co. **QA fixes (2026-07-27):** migration 276 נכשלה על `automation_stages.anchor_event` NOT NULL/CHECK (היה NULL ל-`event_immediate`) — תוקן עם `anchor_event='guest_club_invite'` + הרחבת ה-CHECK (כמו migration 194 עם `spa_time`). `guestClubWaInvite.test.ts` (6 טסטים: disabled/cancelled/already_sent/club_member_active/idempotent-unique-violation/happy-path). `guest-portal-data` מחזיר לאורח רק `portal_offer_enabled` בוליאני — לא את כל `club_wa_settings` (היה חושף `wa_invite_delay_minutes`/`departure_fallback_time` לפורטל בלי צורך, אף שלא נצרך ב-frontend). `whatsapp-webhook` כפתור «מושלם»: `enqueueGuestClubWaInvite` רץ רק אחרי ש-`sendReply` הצליח בפועל (לא בתוך `catch` גורף) — מונע תור להזמנת מועדון בלי שהאורח קיבל את התגובה שמובילה אליה.

MVP אורח: בילוי יומי + ספא → סקר → הצטרפות מועדון (`guest_club_members`). **סקר פורטל (DEPLOYED 2026-07-16, f19b44c):** סקאלה 1–3 עם אמוג'ים (👌 נהניתי במידה מסוימת / 🙂 חוויה טובה / 🤩 היה מדהים!) — `SURVEY_SCORE_OPTIONS` ב-`guestSurveyUi`; חיובי = **חוויה כללית 2 או 3** → כפתור גוגל בפורטל + הצעת מועדון (2 שלבים: הצעה → תאריכי לידה/נישואין להטבות, migration 268) + WA `positive_feedback_reply` **אחרי** join/decline למועדון (`guest-portal-club`); אם כבר חבר מועדון — WA מיד מ-`guest-portal-survey`; ציון 1 בלבד → תודה בלי מועדון/גוגל; כל ציון 1 בקטגוריה → `guest_feedback` שלילי לצוות; thank-you CTAs נשמרים ב-`survey_thank_you` אחרי רענון. **זרימה (2026-07-14):** 3 הודעות WA לקוהורט ספא בלבד (`night_before_daypass` + `spa_warmup_daypass` + `survey_invite_daypass` — **spa_time + 1h**, migration 279, ACC «דקות אחרי טיפול»); **`morning_welcome` (שלב 3 בילוי יומי, migration 280):** הודעה קבועה `morning_daypass` — **בלי** ניתוב שבת (`suite_welcome_morning*` רק ל-`morning_suite`); Meta cold-start `dream_checkin_reminder_v2` בלבד כשחלון 24h סגור; קישור סוויטות אחרי join; `consent_line` ב-`guest_club_ui`. **ערוץ יום-כיף/ספא (migration 205):** תמיד Dream Bot (Meta) — לעולם לא Whapi (מניעת חסימות); סנכרון לוח ספא מושתק אוטומטית (`spa_sync_opt_in`) עד אישור ב-ACC; `missed_window` אחרי 30דק מעבר מועד. **Whapi failover (migration 205):** `bot_config` `whapi_guest_sos_active` (SOS ידני ב-ACC) + `whapi_auto_failover` + probe `GET /health` בכל whatsapp-cron → כל outbound אורחים ל-Meta כשלא AUTH; כפתור «החזר ל-Whapi» ב-Pulse. **`spa_warmup_daypass` (DEPLOYED ce84783):** שליחה per-guest ב־`spa_time − 30דק׳` (ACC: «X דקות לפני שעת הטיפול»); `anchor_event=spa_time` (migration 204); cron מקשיח גם אם DB מקולקל; בלי `spa_time` → `missing_anchor_timestamp`. **ACC «חל על» (2026-07-20, migration 266):** `applies_to` = `suite`/`daypass` (כל) + `suite_spa`/`suite_no_spa`/`daypass_spa`/`daypass_no_spa`; מקור `_shared/automationCohort.ts`; `non_suite` legacy → `daypass`. צוות: Feedback → סקרים. **שידור:** `guest-club-broadcast` — רק status=active.

2. מפת ניתוב מרכזית (activePage ב-App.js)

- dashboard ➔ OperationalDashboard.js (דאשבורד תפעולי חי — `tasks` + `guest_alerts` מ-Supabase + Realtime; באנר דחוף: pending_approval / תלונות / Inbox / ETA חסר / Meta חסום; KPI: מגיעים·בריזורט·בקשות·משימות; משמרות + צ'קליסט מ-localStorage). shifts / checklist / employees — legacy tabs.
- vip_guests ➔ GuestDashboard.js (ניהול טקטי של הפרויקט - לינה / בילוי יומי). **💬 שליחת WA (2026-07-30):** כפתור 💬 → `GuestOutboundModal` — בחירת ערוץ Dream Bot / מכשיר סוויטות, טקסט חופשי או תבנית Meta, כפתור «פתיחת חלון שירות» כש-Meta סגור; אותו מודל ב-`ReceptionistView.js`.
- guests ➔ GuestsPage.js (מסך צ'ק-אין ייעודי לסוויטות בלבד, אורחי יום מסוננים החוצה). **טעינה מהירה (2026-07-30):** `checkinGuestsFetch.js` — שאילתה לפי timeline scope (לא `select *` על כל הארכיון), `guestsPageMemoryCache` לציור מיידי בחזרה מלשונית אחרת (דפוס Inbox), מוני פילטר bar מ-cache קל 5ד'. **ללא auto-checkout שקט (2026-07-30):**** טעינת/רענון הדף לא מעדכן `checked_out` — מקור אמת לצ'ק-אאוט סוויטות = קבוצת WA `Co N` בלבד (`shouldAutoCheckoutGuest` כבוי גם ב-`guestCheckinMatrix.js`). **ריענון שקט (2026-07-24):** צ'ק-אין/סטטוס מעדכנים שורה מקומית בלי להסתיר את הטבלה — `loading` רק ב-mount; realtime debounced + דיכוי echo אחרי mutation מקומית. כולל «לוח זמני הגעה — היום» — מיון לפי `arrival_time`, ⚠ למי שעדיין לא מסר שעה; **סגור כברירת מחדל (▶ toggle, 2026-07-19).** **פרופיל 360 (2026-07-19):** לחיצה על שם אורח → `GuestContextDrawer` (💬 צ'אט + ✏️ עריכה); `AddGuestModal` = מקור אמת מאוחד ל-CRUD + `GuestSmartProfileFields` (VIP/ETA/`guest_profile`/`arrival_time` בשמירה אחת). **עריכת טלפון (2026-07-21):** שדה טלפון ניתן לעריכה גם בפרופיל קיים — `normalizeGuestPhoneEdit` + `updateGuestPhoneCascade` מסנכרן `suite_rooms`/`bookings`/`guest_alerts`/`whatsapp_conversations`/`inbox_read_cursors`/`guest_club_members`. **מרובה-חדרים:** `suite_rooms` = מקור אמת per-room; `guests.room` = תווית משולבת (`אמטיסט 8 · וילה 3`); ייבוא Doc2 (CSV+מייל) = שורת דוח לכל חדר → פרופיל `guests` אחד + N `suite_rooms` (`ezgoDoc2SuiteRoomSync` במייל); `AddGuestModal` = «+ חדר נוסף» + `syncGuestSuiteRoomsFromSelection` (שורות `manual-*` בלבד — לא מוחק שורות EZGO); «חדר מוכן» WA per חדר (`roomId` ב-`whatsapp-send`); **סטטוס ידני `checked_out` (2026-07-18):** אפשרות ב-modal + `checked_out_at`/`room_ready_*` reset בשמירה. **בקשות/קריאות (2026-07-17):** Inbox `GuestContextDrawer` בורר «חדר פעיל» → `guest_profile.inbox.selected_suite_room`; `createGuestOpsTask`/`notify-manual-task` משתמשים בו; Ops Board חוסם אישור `pending_approval` בלי בחירת חדר כשיש 2+ סוויטות. **חסר עזיבה (2026-07-17):** סוויטה בלי `departure_date` → חסימת ייבוא (NIGHTS/iNights), תג ⚠ ב-UI, `guest_alerts.alert_type=missing_departure_date` (migration 222).
- wa_inbox ➔ WhatsAppInbox.js (חדר בקרה תפעולי, ניהול שיחות, צ'אט, Claim ופתרון משימות). **Inbox roster sync (2026-08-03):** `inboxGuestMap.js` — guest phone map paginated (1100+ rows); טאב «בריזורט»/«מגיעים היום» משלים אורחי סוויטה מ-`guests` כשאין להם הודעה בחלון 200 האחרונות; תיקון «פרופיל נמחק» שגוי (`guest_id` fallback + התאמת `+972`/`972`). **Egress health (2026-07-31):** `usePageVisibility` — Realtime+poll מושהים כשהטאב ברקע; טעינה ראשונה 200 שורות; poll fallback 30ש'. **קהל סוויטות/בילוי יומי (2026-07-21, strict 2026-08-02):** פילטר `audienceFilter` (ברירת מחדל 👑 סוויטות, localStorage) — סינון קשיח לפי `contactIsSuiteAudience`/`contactIsDaypassAudience`; `human_requested` מקהל אחר **לא** נכנס לרשימה — רק באנר «ממתינים בקהל השני» + צ'יפ «🔴 התראות»; מיון «פעילות» לפי inbound אחרון (יוצא בלבד לא מקפיץ). **Inbox מאוחד (2026-07-14):** thread אחד per-phone — הודעות Meta+Whapi ממוזגות כרונולוגית; בורר «ענה דרך» Dream Bot / מכשיר הסוויטות (נעול ל-Meta ב-SOS). **השתקה דו-ערוצית (2026-07-15):** בשיחה מאוחדת כפתורי 🤖/🏨 נפרדים — `setClaim`/`resolveClaimChannel` שומרים Meta ב-`guests.claimed_by` ו-Whapi ב-`guest_channel_claims`; שליחה ידנית auto-claim לערוץ הנבחר; באנר סטטוס per-channel; תגית ערוץ על בועות. **שחרור בוט אוטומטי (2026-07-19):** `_shared/guestStaffClaim.ts` — claim מושתק משתחרר אחרי דקות שקט (`bot_config.inbox_claim_idle_release_minutes`, ברירת מחדל 60) עם הארכת `claimed_at` על כל הודעה בשיחה; ניקוי ב-`whatsapp-cron`; חריג קבוע `guest_profile.inbox.bot_disabled` בפרופיל 360. **Dismiss אדום (2026-07-15, fix 2026-08-02):** כפתור ✓ על באדג' `human_requested` ב-`dismissHumanRequest` מנקה meta+whapi דרך `resolveContactInboxChannels` (לא את המפתח הווירטואלי `unified`); אדום ברשימה רק מ-inbound `human_requested` — לא מטקסט handoff יוצא (כדי ש-dismiss יישאר). **דגל ידני (2026-07-21):** כפתור 🚩 לצד כל שורת שיחה (גלוי כשאין עדיין `human_requested`) — `flagHumanRequest` מסמן `human_requested=true`/`human_request_type='manual'` על ההודעה הנכנסת האחרונה + `guests.needs_callback=true`; משתמש באותו מנגנון אדום/פעימה/פילטר «🔴 התראות» וכפתור ✓ הקיים לביטול. פילטר ערוץ מציג אורח אם יש לו הודעות בערוץ. Claim ומתג בוט (🤖/😴) נשארים per-channel: `guests.claimed_by`/`bot_active` ל-Meta, `guest_channel_claims`/`bot_active_whapi` ל-Whapi (migrations 170-171). **חובה:** סנכרון מפת אורחים ב-Inbox לעולם לא מעתיק `guests.claimed_by` לשיחת Whapi (רק `guest_channel_claims`); לפני stub ב-claim Whapi — חיפוש אורח קיים לפי טלפון. Unread = `inbox_read_cursors` per staff+phone+channel (migration 181); פתיחת שיחה מאוחדת מסמנת נקרא בשני הערוצים. תגי יוצא `[META]`/`[SESSION]`/`[WHAPI]` ב-`whatsapp_conversations.message` הם ל-Inbox בלבד — חובה לפלטר לפני שליחה לאורח או הזרקה ל-LLM (`_shared/outboundDispatchTag.ts`). **Whapi truncation guard (2026-07-15):** `whapi-webhook` LLM path משתמש ב-`isReplyObviouslyTruncated` (כמו Meta) — fallback לתבנית facility או handoff; `guestFacilityReview` תופס גם `מסטאג`/`לא התרשמנו`. **SOS broadcast:** ACC → `guest-emergency-broadcast` שולח `dream_service_fallback` (Meta) רק ל-`arrival_date=היום`; כפתורים «יש לי בקשה» / «הכל בסדר, תודה» → ack ב-webhook. **Inbox ⚡ (2026-07-23):** «🌴 פתיחת חלון שירות» בשיחה — שליחה ישירה של אותה תבנית לאורח בודד דרך Meta (גם חלון סגור / SOS). **Roster segments (2026-07-15):** `🟢 בריזורט` = סוויטות `checked_in` בלבד (`isSuiteInResortToday`); `🌅 מגיעים היום` = סוויטות בטרום-צ'ק-אין (`isSuiteArrivingToday` / `isPreArrivalTodayGuest`); צ'ק-אין «היום» ב-sessionStorage מפעיל `arriving_today` לא `in_resort`.
- orit_cs_agent ➔ OritCustomerServicePanel.js (סוכן שירות לקוחות לאורית — **גישה (2026-07-19):** `super_admin` תמיד; אחרים רק `profiles.orit_cs_agent_access=true` מניהול משתמשים; `canAccessOritCsAgent` + RLS migration 244 + Edge Functions; לא admin רגיל אוטומטי). **Inbox mode (2026-07-19):** היסטוריית שיחה גלילה + compose יחיד למטה (ack שלב 1 / full_reply שלב 2); כרטיסי **הצעות AI** (עד 2 וריאנטים) מעל הטקסט — «✨ הצעות» מריץ `manager-mail-analyze` עם `forceLlm`; **לינק מלינק סיגל** → פתיחת thread + ניתוח אוטומטי רק אם אין טיוטות (תלונה=LLM); שליחה מהממשק → סיגל מאשרת ב-WA; **תזמון (2026-07-19, migration 251):** toggle ⏰ + `orit_agent_scheduled_sends` — מייל/וואטסאפ; Sigal ברכת זמן + 21:00–05:00 מציעה «תזמני למחר»; cron `whatsapp-cron` → `dispatchDueOritScheduledSends`; כפתורי «שלחי מ-XOS» / «שלח בסוויטות» צמודים לטקסט; פרטי פנייה מתקפלים; בועות אורח/אורית + 📧/📱. **עריכת טיוטה ב-WA (2026-07-19):** «תסדרי…» → `refineOritDraftByInstruction` → אישור «כן שלחי». **Pulse WA (2026-07-19):** התראות סיגל phase-aware — שלב 1 דחוף «קיבלנו את פנייתך» בלבד → אחרי ack שלב 2 מכתב מלא; ניסוח שיחה + לינק ממשק בפלאפון (**deep link iOS fix 2026-07-23:** `oritSigalWhapiSend.ts` — תווית בעברית בהודעה 1, URL בלבד (LTR) בהודעה 2; `sendOritSigalWhapiText`/`LongText` בכל נתיבי Sigal→Orit). **תור (2026-07-19):** טאב «פתוחות»/«טופלו»; `closeOritThread` משותף UI+סיגל; מייל מאורח אחרי סגירה → פתיחה מחדש + WA. **שרשור מייל (2026-07-19):** Graph `/reply` על הודעה מקורית + `oritThreadMatch.ts` fallback (אימייל+נושא); תשובת אורח → אותו thread ב-`orit_agent_messages` (לא כרטיס חדש); `runOritThreadFollowUp` — טיוטת המשך בלבד בלי סיווג מחדש. **Graph API** לתיבת M365, AI לתלונות בלבד, **Sigal assistant (2026-07-19):** `oritSigalBriefing.ts` + `oritAgentSigalChat.ts` — ניתוח מראש + חבילת briefing חמה; מייל תמיד קודם; **גשר WA** (`oritGuestOutbound.ts`) רק בלי מייל+יש טלפון — «שלחי בוואטסאפ» דרך מכשיר סוויטות + לינק שיחה באינבוקס; תשובת אורח ב-WA → `oritGuestWaBridge.ts` (בלי בוט אורח); **תוכנית בוקר 07:00** `composeSigalMorningActionPlan` + pre-analyze ב-`manager-morning-digest`; **סיכום ערב 18:00** `composeSigalEveningActionPlan` ב-`manager-evening-digest` (migration 247 `digest_kind`); **משוב אורחים יומי (2026-07-26):** `guestFeedbackDigest.ts` — בלוק בוקר (24ש' + שליליים פתוחים + קישור `feedback_dashboard`) ותזכורת ערב קלה; פקודת WA «משוב אורחים»; לא תלוי ב-`alert_enabled`; **`runSigalUrgentComplaintLoop`** — תזכורות phase-aware לתלונות דחופות עד «סיימתי» (כל ~15דק `whatsapp-cron` + mail-sync); «סיימתי»/«טיפלתי» = `closeOritThread`; שקיפות מלאה לפני «כן שלחי»; למידת סגנון; HITL ack→מכתב מלא; migrations 245–246.
- feedback_dashboard ➔ GuestFeedbackTabs.js (משוב אורחים — **משוב חופשי** ב-`guest_feedback` עם סינון מתקן + דירוג 1–10, migration 212; **סקרים** ב-`guest_surveys`; **סבב מלצרים (2026-07-19, v3 2026-07-21):** טאב «סבב מלצרים» — סקר ציבורי `/pulse/:token` (ללא התחברות) «שאלון מלצרים - מסעדת ערמונים» אנונימי — **11 שאלות** (ותק, נוכחות מנהלים, כבוד, שיפורים, שיתוף פעולה, הסכמת טיפים, מדיניות טיפים א+ב עם `help_text`, הכשרה, חיזוק ידע, קושי מול מטבח/בר/מארחות, הערות חופשיות; migration 270); עריכה ב-`bot_config.waiter_service_pulse_ui`; תשובות ב-`waiter_pulse_responses` + ניהול סטטוס; **שליחת קישור למלצרים (2026-07-23, migration 272):** `waiter_pulse_contacts` + `WaiterPulseDispatchPanel` — רשימת מלצרים נפרדת מ-`guests`, ייבוא הדבקה, הודעה עם `{{שם}}`/`{{קישור}}`, שליחה סדרתית דרך Whapi (`inbox_reply`, 2.5s) — אפס אוטומציה). ביקורות מתקנים נאספות ע"י הבוט: Tier-0 `_shared/guestFacilityReview.ts` (Meta+Whapi) + כלי LLM `log_facility_review` (Meta בלבד). מסעדה מודגשת בפילטר. **תג סקר (2026-07-18):** חוויה כללית 2–3 = ירוק גם אם קטגוריה נמוכה; אזהרה נפרדת + הדגשת קטגוריה ≤1. **דוח Excel (2026-07-27):** כפתור «📥 דוח משוב לשיפור שירות» — 4 גיליונות (סיכום, שלילי עדיפות, משוב חופשי, סקרים); deep link `focus=negative`; פולס בוקר אליעד יומי עם קישור (`composeEliadDailyFeedbackLinkBlock`).
- restaurant_dinner_board ➔ RestaurantDinnerBoard.js (**2026-07-19, migrations 248–250**): **לוח מסעדה** — 3 טאבים: תיאום שעות | הזמנה | הזמנות פעילות. **גישת מסעדה (2026-07-20, migration 254):** `profiles.role=restaurant` או `restaurant_access` לעובד staff → קיוסק (`RestaurantKioskShell` ב-`src/components/restaurant/`) — לוח מסעדה + **Inbox שיחות** בלבד (ללא דאשבורד/סוויטות). **Armonim branding + משמרת (2026-07-20, migration 256):** מסך פתיחה — **שם חובה** + תפקיד (**מארחת** / מנהל משמרת / מלצר, migration 257) → `restaurant_shift_sessions`; שמות אחרונים ב-localStorage; כותרת ממותגת (`armonimKiosk.css`); הזמנות עם `waiter_name_snap`/`shift_session_id`; חתימת WA מ-`bot_config.restaurant_kiosk_ui`. **קוד מנהל משמרת (2026-07-20, migration 262):** בחירת תפקיד «מנהל משמרת» בשער המשמרת דורשת PIN אם מוגדר (`kioskUi.shift_manager_pin`, נבדק ב-`startShift`); הגדרה/שינוי הקוד רק ב-`UserManagement.js` (`ShiftManagerPinCard`, גלוי ל-super_admin בלבד) — RLS מגביל כתיבה ל-`bot_config` `config_key='restaurant_kiosk_ui'` ל-super_admin (RESTRICTIVE policy, מעל ה-admin/super_admin הכללי לכל שאר המפתחות). **תפריט באתר (2026-07-20, migration 258):** `https://armmonim.co.il/תפריט/` — לינק בקיוסק + **QR לאורח** בטאב הזמנה (`RestaurantMenuQrModal`). **תפריט רגיל = seed ב-DB, לא fetch חי (2026-07-20, migrations 259–261):** `menu_kind` standard|special; `src/data/armonimMenuSeed.js` + migration 260 — קלט חד-פעמי מהאתר (26 מנות, 3 קטגוריות) כ-`published`; עריכה מכאן ואילך רק ב-CMS (מנהל משמרת בקיוסק — `role='restaurant'` הורשה ב-`is_restaurant_menu_admin()`, migration 261 — או super_admin/manager ב-XOS). `fetchDraftRestaurantMenuVersion` מקלונן אוטומטית מ-published כשאין טיוטה (לא מתחיל מריק). **תפריט ספיישל AI:** `restaurant-menu-import` (מצב upload בלבד — אין יותר סנכרון אתר) + `RestaurantMenuImportPanel` — צילום/PDF/DOCX → תצוגה מקדימה → «החל על טיוטה» → פרסום. **`RestaurantOrderPanel`:** קטגוריות + חיפוש מנה + סל דביק; מלצר בוחר אורח סוויטה **או** שולחן/אורח כללי; מעבר רגיל↔ספיישל כששניהם פעילים; שליחה מוצלחת → מעבר אוטומטי לטאב «הזמנות פעילות». עריכת נוסחי WA בקיוסק (`canEditRestaurantDinnerTemplates`). **«הגדר שעה» (2026-07-20):** במסך צר (<768px) — master-detail: לחיצה פותחת מסך עריכה מלא + «חזרה לרשימה»; בדסקטופ — גלילה לפאנל ימני; toggle צהריים/ערב מדגיש את הסקשן הרלוונטי. **מחלקה (2026-07-20):** `src/data/hotelDepartments.js` — «מסעדה» באונבורדינג → role+restaurant_access אוטומטי. מנהלים עם דגל מסעדה רואים את הלוח בתוך XOS המלא.
- ops_board ➔ OperationsBoard.js (לוח תפעול ואחזקה, כולל טאב משימות ממתינות לאישור)
- data_sync ➔ DataSyncPage.js (מסך סנכרון וייבוא קבצי אקסל ודוחות EZGO ל-Admin/Receptionist). **API push EZGO (2026-08-03, migration 282):** `ezgo-webhook` — POST JSON staging sink → `ezgo_api_ingest`; secret `EZGO_WEBHOOK_API_KEY` (`x-api-key` or Bearer); URL `https://bunohsdggxyyzruubvcd.supabase.co/functions/v1/ezgo-webhook`; **לא** נוגע ב-`guests` עד מיפוי schema; dedupe על `external_id` / `x-idempotency-key`. **מייל EZGO (2026-07-20, migration 267):** לשונית «📧 מייל EZGO» — `ezgo-mail-sync` קורא Gmail IMAP → `ezgo_mail_ingest`/`ezgo_mail_import_lines`; אישור ידני לפני enrich; **סנכרון incremental (2026-07-22):** `newer_than:7d` (env `EZGO_MAIL_SEARCH_DAYS`) · envelope+Message-ID dedup מול DB לפני הורדת source/postal-mime · supplement envelope-only · **רקע cron opt-in (2026-07-31):** `whatsapp-cron` קורא ל-`ezgo-mail-sync` רק כש-`EZGO_MAIL_BACKGROUND_SYNC=true` + שעות עבודה (07–20 ישראל) + מרווח 2ש' (`shouldInvokeEzgoMailFromCron`) — סריקה ידנית מ-UI תמיד; **מחוץ ל-`CRON_ENABLED`** · UI realtime+poll 90s **רק כשהטאב פעיל** (`usePageVisibility`) · **toast (2026-07-23):** `skippedKnown>0` → «הכל מסונכרן» (ירוק); «ודא תיבת Gmail» רק כש-`searchUids=0` · כפתור «🔍 סריקה מלאה» (`full_sync`) · **סריקה לפי יום (2026-07-25):** «סרוק מייל» שולח `search_date_ymd` (ברירת מחדל היום) — date picker + היום/אתמול; native IMAP `since`/`before` ליום אחד, בלי supplement/escalation (פותר timeout) · **retention:** `purge_stale_ezgo_mail_ingest` מוחק ingest שטופל במלואו אחרי 3 ימים (`EZGO_MAIL_RETENTION_DAYS`, migration 271) — לא נוגע ב-`pending_review`. 3 מסלולים: סוויטות (מס׳ הזמנה→spa_time) · בילוי יומי ללא ספא→הצעת ספא · יצירת פרופיל בילוי יומי (עם/בלי ספא); `ezgoMailLineWorkflow.ts`. **ריבוי טיפולי ספא (2026-07-21):** Doc1 parser שומר `spa_slots[]` (כל השעות, לא רק המוקדמת) + `guest_profile.spa.doc1_slots` באישור; בוט מקבל `טיפולי ספא` מלא ב-`buildGuestContextForAi`; chip אזהרה ב-UI מייל. **שולחים מזוהים (2026-07-21):** `noreply@ezgo.co.il` (ישיר מאיזיגו) + `hagar.mesilati@dream-island.co.il` + `reception@dream-island.co.il` (CSV מצורף מקבלה) + `tzalamnadlan@gmail.com` (העברות) — `DEFAULT_EZGO_MAIL_SENDERS`; כל `@dream-island.co.il` מותר כשיש שולח dream-island ברשימה (כמו `@ezgo.co.il`). **CSV מצורף לכניסות (2026-07-23):** `ezgoDoc2SuiteCsvParser.ts` — Suite CSV / דוח מפורט עברי / ייצוא LineIndex·ItemID·SUBItemName; postal-mime אוסף `.csv` → `doc2_arrivals` (אותו match/apply כמו HTML Doc2); חיפוש IMAP `has:attachment filename:csv` לשולחי dream-island. **זיהוי חכם (2026-07-21):** `parseOrderIdentityFromCell` — טלפון בשורה שנייה בטבלה + סריקת 05x; `matchDoc1Record` — fallback DB לפי מס׳ הזמנה + שם+תאריך. **תיקון שם null ל-OTA (2026-07-21, DEPLOYED):** `extractNameFromOpsTail` היה מאבד את שם האורח ל-null בהזמנות "Hotel WebSite -"/"Booking Collect -" (השם והטלפון בשורות נפרדות בתא) — ה-dash-truncation הישן חתך את השם עצמו בלולאה השנייה; מתוקן — נבדק על מייל noreply@ezgo.co.il אמיתי דרך postal-mime (81/81 שורות, שם נכון). **Gmail Updates (2026-07-21, DEPLOYED):** IMAP `[Gmail]/All Mail` + `category:updates` ל-noreply בטאב «עדכונים»; reparse `processIngestReplace` (לא מוחק ingest); **`processIngest` const fix (2026-07-21):** `enrichRecordsPhoneFromDb` — תיקון `Assignment to constant variable` ששבר סנכרון אוטומטי. **Doc2 דוח כניסות במייל (2026-07-21):** `ezgoDoc2Parser` מזהה HTML «מס. הזמנה / סוג יחידה» → `report_type=doc2_arrivals`; מיפוי מס׳ הזמנה+טלפון+שם; UI: צור סוויטה / השלמת חסר / שיבוץ חדר (רק שדות ריקים). **Doc2 HTML remark occupant (2026-08-03):** אותו שם «לקוח» פעמיים+ → שם+טלפון מעמודת הערות (`ezgoDoc2RemarkIdentity.ts`, כמו CSV/`ezgoParser.js`); שורה בודדת ללא שינוי. **אורח חוזר (2026-08-03, DEPLOYED):** `guestProfilePick.ts` — Doc2 לא מתאים שהייה `checked_out` ישנה לתאריך הגעה חדש → `suite_arrival_create`; Inbox/`GuestContextDrawer` בוחרים שהייה פעילה/עתידית לפי טלפון (לא `limit(1)` אקראי). **Doc2 חדר לא חובה ליצירה (2026-07-23):** `classifyDoc2MailWorkflow` (TS+JS mirror) — שורת כניסה בלי מספר חדר (יישוב מאוחר, `room_raw` בלי מספר) עוד לא נחסמת כ-`no_match`; טלפון+שם/מס׳ הזמנה בלי guest קיים → `suite_arrival_create` (`room: null`, `createDoc2SuiteArrival` כבר תומך); guest קיים בלי חדר בשורה → `suite_arrival_enrich` (רק תאריכים/הזמנה/פנסיון — לא נוגע בחדר). **חיפוש IMAP לפי סוג דוח (2026-07-23):** `buildEzgoReportSearchQueries` — שאילתות `subject:כניסות`/`ויציאות`/`Operations`/`has:attachment filename:xlsx` ל-ezgo.co.il; SINCE-date supplement רץ תמיד per sender (לא נחסם ע"י cap break מוקדם); `EZGO_MAIL_PER_SENDER_MIN` 12→21, `EZGO_MAIL_PER_SENDER_MANUAL_CAP=36` לסריקה ידנית/מלאה; env אופציונלי `EZGO_MAIL_GMAIL_LABEL` מנסה mailbox של label לפני All Mail; `foundByReportType`/`reportQueriesUsed` בתוך meta לשקיפות ב-toast. **סדר עדיפויות שאילתות + label בפועל (2026-07-23, FETCH hardening):** `buildSenderSearchQueryPlan` (מיוצא, מכוסה בטסטים) מריץ את שאילתות ה-subject הייעודיות **לפני** הסריקה הגנרית `from:ezgo.co.il` — בעבר תיבה עמוסה יכלה למלא את המכסה-לשולח מהשאילתה הגנרית לפני ששאילתות ה-subject ירוצו בכלל (ה-loop שובר ב-cap), ולמעשה מנטרל את כל התוספת ברוב הסריקות. `EZGO_MAIL_GMAIL_LABEL` היה משפיע רק על שאילתות IMAP רגילות (`imap_from`/SINCE) — שאילתות `gmailRaw` עם `in:anywhere` מתעלמות ממיקום ה-mailbox הפתוח; ה-scan היקר בפועל (envelope-only supplement/fallback, עד 120–180 הודעות) התעלם מה-label לגמרי וסרק תמיד `INBOX`/`[Gmail]/All Mail` — `resolveSupplementMailboxCandidates` (מיוצא) מתקן את זה ומעדיף label אם מוגדר. **חיפוש IMAP native במקום gmailRaw (2026-07-24, שורש הבעיה):** `gmailRaw`/`X-GM-RAW` דורש capability `X-GM-EXT-1` — אם ה-IMAP session לא מדווח עליו, `client.search` עם gmailRaw זורק `MissingServerExtension`, וזה נבלע בשקט ע"י `catch{}` ריק → כל תוכנית החיפוש (לכל השולחים) יכולה להתאפס בבת אחת, בלי סימן. `buildSenderSearchQueryPlan` בונה עכשיו את שאילתות הליבה (`report_subject_native`/`ezgo_domain_native`/`dream_island_domain_native`) כ-IMAP native טהור (`from`/`subject`/`since`/`or`) — לא תלוי בהרחבת Gmail בכלל; `gmailRaw` (attachment/category) נשאר שכבת best-effort משנית בלבד. כל שגיאת `client.search` נכנסת ל-`meta.searchErrors` (חדש) במקום להיבלע — הטוסט ב-`EzgoMailSyncPanel` מציג אותה ומאדים גם על הצלחה חלקית. **איחוד שאילתות — כל השולחים בשאילתה אחת (2026-07-27, שורש הבעיה השני):** גם אחרי תיקון ה-native queries, `searchAllowlistedUids` עדיין רץ בלולאת per-sender — 5-8 שאילתות SEARCH נפרדות *לכל שולח* (~20+ סה"כ ל-4 השולחים המוגדרים כברירת מחדל), ו-`whatsapp-cron` קורא ל-`ezgo-mail-sync` בכל טיק (כל 15 דק', **מחוץ** ל-`CRON_ENABLED`) — כלומר ~96 פעם ביום, כולל כשאין שום מייל חדש. זה מה שהפך את חריגת 55ש' (`IMAP_BUDGET_MS`) מאירוע נדיר לתקלה תלוית-עומס. `buildCombinedAllowlistSearchPlan`/`buildCombinedAttachmentGmailQuery` (מיוצאים, מכוסים בטסטים) מאחדים את **כל** השולחים ל-2-3 שאילתות IMAP `or` בלבד — סדר גודל קבוע (O(1)) שלא גדל עם מספר השולחים ברשימה, בניגוד ללולאה הישנה. `searchAllowlistedUids` מנסה קודם את האיחוד; נופל בחזרה ללולאת per-sender הישנה (`searchUidsForSender`, נשארה שלמה) רק אם האיחוד לא מצא UID אחד בכלל — רשת ביטחון נדירה, לא מסלול קבוע. Deploy: `functions deploy ezgo-mail-sync`. **העלאת .eml ידנית:** כפתור «📎 העלה .eml» ב-`EzgoMailSyncPanel` → `ezgo-mail-sync` עם `eml_base64` (אותו נתיב עיבוד כמו IMAP). **Ledger UI:** באנר צהוב כשאין `ezgo_mail_ingest` עם `report_type=doc2_arrivals` ו-`report_date_ymd`=היום/מחר (חישוב דינמי בצד לקוח, בלי migration). **Phone hardening (2026-07-21):** `htmlCellText` DIV/p→newline; שורת המשך טלפון ב-HTML; `mergeDoc1PhoneFromSecondary` מ-Excel כש-HTML חלקי; `enrichRecordsPhoneFromDb` לפני match; «🔁 פרסר מחדש» ב-`EzgoMailSyncPanel` (`reparse_ingest_id`) — **לא מוחק לפני מציאת המייל**; snapshot `body_html`/`body_text` (migration 269); `fetchEzgoMessageById`; סריקה per-sender (12+ לכל אחד מ-3 השולחים הישירים). **כפתורי פעולה בשורת השם (2026-07-20):** אשר / צור פרופיל / 💆 בחר תבנית ושלח — `SpaUpsellConfirmModal` inline עם בחירת תבנית Meta; batch «בחר תבנית ושלח» per סיווג; `spaUpsellDispatch.js`. **SpaUpsellHub (2026-08-02, template picker 2026-08-03):** טאב «תזמון ממתין» (`fetchSpaUpsellPendingSchedules` מ-`scheduled_tasks`) — אורחים מתוזמנים לא מוצגים ב«לשליחה עכשיו»; עריכת שם inline; `SpaUpsellConfirmModal` בחירת Meta `spa_upsell_daypass1` / `spa_upsell_daypass` + תצוגה מקדימה חיה; `wa_template_name` ב-scheduled_tasks (migration 284). **spa_upsell_accept (2026-08-02):** אישור אורח → DM אישי Whapi למייק בלבד (`notifySpaUpsellAcceptOwnerDm`, ברירת מחדל `ARCHITECT_PHONE_DIGITS`; override `SPA_UPSELL_NOTIFY_PHONE`) + בלוק «להעברה לצוות הספא»; קבוצת בקשות + לוח בקשות נשארים. **Session A (2026-07-28):** `daypassGuestCreate.js` — shared `createDaypassGuestProfile` for mail/import/spa hub paste («צור פרופילי בילוי יומי» in `SpaUpsellHub` when paste has no DB row).. **מצורף Excel (2026-07-20):** מייל בלי HTML תקין אבל עם `.xlsx`/`.xls` (דוח Operations) → `parseDoc1FromExcelBuffer` + `report_type=doc1_excel` — אותם 3 מסלולים. **מייל Operations (Option A):** Doc 1 מקבל `.eml` מ-Gmail (הורד הודעה) או הדבקת HTML מ«הצג מקור» — `parseHtmlDailyReport` + מצב «ספא סוויטות בלבד»; לא העתקת טקסט גלוי. **Doc 1 PATH B (2026-07-20):** העשרה בלבד — `findGuestForDoc1Enrichment` + `buildDoc1EnrichmentPatch` — לא דורס `arrival_date`/`departure_date` של סוויטה רב-לילית; תאריך הדוח משמש רק ל-`spa_date` ולמיפוי «אורח ביום X בתוך השהייה»; עדכון לפי `guest.id`. כולל ייבוא דוח פעילויות ספא (עברי או CSV אנגלי מ-EZGO) דרך אותו `ActivitiesImportZone` כמו לוח הספא. **הצעת ספא לבילוי יומי (2026-07-20, migration 263):** מצב «ספא סוויטות בלבד» ב-Doc1 מזהה גם אורחי בילוי יומי בטלפון קיים בלי טיפול ספא היום (`msg_spa_upsell_sent`) וגם שורות ללא התאמת טלפון (מציע יצירת פרופיל בילוי יומי חדש, `room="Premium Day 1"`); כפתור «שלח הצעת ספא» פותח מודל אישור (`SpaUpsellConfirmModal`) — תצוגה מקדימה מ-`bot_scripts.spa_upsell_daypass`, רשימת נמענים, צ'קבוקס אישור, «שלח עכשיו» או «תזמן» (`staff_schedule_tasks_batch` → `whatsapp-cron` `dispatchDueSpaUpsellScheduledTasks`); **UI נפרד:** לשונית «💆 הצעת ספא — בילוי יומי» ב-`DataSyncPage` (`SpaUpsellHub` — שליחה + «ממתינים לתאום» + הדבקה; Whapi bulk `whapi-bulk-dispatch`; תשובות «אשמח לתאם»→`spa_upsell_accept`) — שואב אורחים חיים מה-DB לפי תאריך הגעה (לא תלוי בסשן ייבוא); ייבוא Doc1 רק מעדכן נתונים + לינק «עבור לשליחה»; **ערוץ כפול (2026-07-20, migration 264/265):** במודל האישור — 📱 מכשיר סוויטות (`whapi_session` + `bot_scripts`) או 🔵 Dream Bot (`meta_template` + `spa_upsell_daypass`, גוף סטטי 0 פרמטרים); תזמון שומר `force_channel` ב-`scheduled_tasks`; ברירת מחדל Whapi. **`dream_spa_warmup` (2026-07-20):** אוטומציית `spa_warmup_daypass` בלבד — {{1}} שם בלבד (בלי שעת ספא); מתג ACC `is_active`.
- spa_leads ➔ SpaLeadsPage.js (**2026-08-02**): לידים לתיאום ספא — כל `spa_upsell_accept` פתוח, פילטר תאריך, realtime, 💬/📅/✓, תג «ידני», `tel:`; Inbox «💆 הוסף ללידים ספא» + `spa-upsell-manual-lead`; בוט: `resolveSpaUpsellAcceptanceEligible` (flag + notification_log + scheduled_tasks). **Accept flow (2026-08-02):** Meta כפתור «אשמח לתאם» → `runSpaUpsellAcceptanceIntercept`; תשובת אורח «נציג מטעמנו יחזור…»; DM/לוח בקשות — מחיר מ-`bot_scripts.spa_upsell_daypass` (`spaUpsellPricing.ts`), לא hardcode 300₪; bare «אשמח» לא נספר כאישור. **קמפיין קבוצה (2026-08-03, migration 283):** קישור wa.me עם token `XOS-EVE-1008` → `_shared/spaGroupCampaign.ts` יוצר `day_guest` לתאריך הקמפיין + `spa_upsell_accept` + DM למייק — בלי פרופיל מראש; registry ב-`bot_config.spa_group_campaigns`.
- spa_board ➔ SpaBoard.js (לוח ספא חכם — אג׳נדה/חדרים, ייבוא פעילויות EZGO עברי או CSV אנגלי → `spa_appointments` + write-through ל-`guests.spa_date`/`spa_time`/`guest_profile.spa`. **בקשת ספא מהפורטל (2026-07-21):** `GuestPortal.js` מודל אישור → `guest-portal-spa-request` → `guest_alerts` (`spa_request`) + Inbox red-dot על ערוץ Whapi לסוויטות (`inboxRedAlert.preferredInboxChannel`) + WA ממכשיר סוויטות «היי, למתי תרצו לתאם טיפול בספא? נדאג שנציג מטעמנו יחזור אליכם בהקדם» (`portal_spa_whapi` bypass + `preserve_attention` ב-`whatsapp-send` — לא מנקה `requires_attention` אחרי אוטו-ריפלי). בילוי יומי → Meta. לא תלוי ב-ACC `guest_suites_channel`. CSV: תיקון `בע"מ` לפני parse; שיוך אורח לפי טלפון + שם עברי בסוגריים. חדר זוגי = עד 2 תורים חופפים. `iLineStatus=0` מדולג. באנר unmatched עם «נקה הכל». **Therapist sticky-room + female preference (migration 193):** `spa_therapists.gender` (staff-set) + `spa_shift_roster` (חדר-בית מומלץ למטפל/ת ליום — UNIQUE per therapist/day). «רק מטפלת» נשמר ב-`guest_profile.spa.therapist_pref` (read-merge-write, לא דורס מפתחות spa אחרים). אזהרות ⚠ רכות בכרטיסי תור למטפל/ת במספר חדרים היום או אי-התאמת העדפה — Disable-Don't-Hide, לעולם לא חוסם שמירה. **Hard sticky gate + Move Guest + safe יישור יום (DEPLOYED 2026-07-14, commits bc7c36b/b090f52 — live QA על יום עמוס עדיין ממתין למייק):** `src/utils/spaStickyRoom.js` — home room = roster row אם קיים, אחרת ההופעה המוקדמת ביותר של המטפל/ת באותו יום (`inferHomeRoomByTherapist`/`resolveHomeRoomMap`). `AssignModal` חוסם (FAIL VISIBLE) שיבוץ מטפל/ת מחוץ לחדר הבית שלו/ה אלא אם סומן «חריג — שבץ בכל זאת» — כפתור השמירה תמיד גלוי ולחיץ. `MoveGuestModal` (חדש) = נתיב התיקון הראשי: UPDATE יחיד ל-`room_id` (בלי RPC — לא כמו migration 177's swap, שורה בודדת לא חוצה את מגבלת ה-exclusion באמצע), המטפל/ת נשאר/ת אלא אם צוין אחרת. כפתור «🧭 יישור יום»: מזרע roster חסר + `planAlignDay` — רק העברות בטוחות (קיבולת חדר + cascade); deadlock הדדי → swapPairs דרך חדר חניה (3 UPDATEs); השאר FAIL VISIBLE עם שעה/מטפל + «➡️ העבר אורח» / «סגור רשימה». `SwapTherapistModal` (🔄) הודגם ל-«(חריג)» — לא נתיב ברירת המחדל.)
- agent ➔ InventoryHub.js (ניהול מלאי, ייבוא דוחות, יצירת קישורי-קסם לעובדים ותור אישורים)
- voucher_reconciliation ➔ VoucherReconciliationHub.js. **Smart read (2026-07-19, migration 233):** `_shared/voucherImport.ts` — Hebrew header presets, title-row scan, Excel float/scientific normalize; `truncate_4` also matches provider last-4-only + full alnum; `package_types_match` fuzzy (ו/+); exceptions board filters by `reconciliation_run_id` from last import. **Nofshonit ת.ז. (2026-07-19, migration 236–239):** join `מזהה` (איזיגו) = `מזהה לקוח` (ספק) resolved to ת.ז. (`nofshonitNationalId.ts` — direct or CouponNo→ת.ז. lookup); CouponNo-first match; FIFO per וריאנט; `over_redemption` when provider lines exceed EZGO bookings; **migration 239** `package_match_group` aliases (classic&more night↔classic&dinner, צהרים↔כל השבוע); `voucherQuantityAudit.ts` per CouponNo/ת.ז.+חבילה count audit; UI «✓ אותה חבילה» + פערי כמות; volume summary (איזיגו vs ספק); multiple אסמכתא per person → reuse (235); CSV `בע"מ` gershayim fix.
- executive_playbook ➔ ExecutivePlaybook.js (כללים שנלמדו + יומן פעולות ל-Executive Voice Assistant)
- cms_security ➔ עטיפת <ProtectedRoute> הדורשת אימות TOTP / 2FA מבוסס חומרה.

3. מבנה מסד הנתונים (טבלאות ליבה)

פורמטי טלפון קריטיים (חובה לסנכרון):
- guests.phone = E.164 עם `+` (ישראלי `+972…` או בינלאומי `+1…`/`+44…` וכו׳). Doc 2 מנרמל דרך `normalizeWhatsAppPhone` — מספר זר נשמר ונשלח ב-WhatsApp; מספר לאומי בלי קידומת מדינה נדחה (FAIL VISIBLE).
- bookings.phone / Meta Webhook = ספרות בלבד בלי `+` (972… / 1… / 44…).
- כלל פיתוח: בחיפוש הצלבות מול bookings יש לבצע תמיד phone.slice(1) כדי להוריד את ה-+.

טבלאות ושדות מרכזיים:
- guests (Golden Profile):
  * status: 'pending' | 'expected' | 'room_ready' | 'checked_in' | 'cancelled'
  * room: שם סוויטה קנוני מתוך SUITE_REGISTRY או "Premium Day 1/2". **שער אוטומציה (2026-07-19):** `_shared/suiteNames.ts` — בלי שיוך חדר תקין (`missing_room_assignment`) אין שליחה אוטומטית; בלי טלפון עם ספרות (`missing_phone`, `hasDialableGuestPhone` ב-`metaPhone.ts`) אותו חסימה — cron לא קורא ל-Meta ולא שולח 🚨 אדמין חוזר; סוויטה = חדר קנוני בלבד (לא `room_type=suite` לבד); יום-כיף = Premium Day או `day_guest`+חדר; Premium Day עם `room_type=suite` מנותב יום-כיף.
  * arrival_time: שעת הגעה משוערת שמדווחת ע"י האורח. נשמר ב-guests + שורת `guest_alerts` עם `alert_type='arrival_eta'` (תווית «🕐 שעת הגעה» בלוח בקשות). בלי needs_callback / משימות תפעול / נקודה אדומה. **טווח הגעה (2026-07-21):** `בין 13:00 ל 13:30` / `צפויים להגיע` → Record-Only שומר את השעה **המוקדמת** (`guestEta` + `ARRIVAL_TIME_UPDATE_RE`). **פעמון לאדיר:** `_shared/arrivalEtaAdirNotify.ts` — Whapi DM אישי לאדיר (`SLA_GUEST_ALERT_PHONE` / `972546294885`) אחרי persist מוצלח, עם תיוג ערוץ (Dream Bot / מכשיר סוויטות), dedupe אם אותה שעה, «עודכן» בשינוי. פרופיל חכם מציג «🕐 שעת הגעה HH:MM».
  * guest_profile: JSONB מובנה (VIP, רגישויות, אירוע).
  * claimed_by: שיוך שיחה ב-Inbox לנציג אנושי (מכבה אוטומציות).
  * **Smart Paste (migration 207, DEPLOYED):** `pg_trgm` GIN על `name` + RPC `match_guest_fuzzy`; Edge Function `parse-raw-paste` (Gemini 2.5 Flash, `suite_guest`|`day_guest`, HB/FB→`meal_plan`); UI `SmartPastePanel` ב-DataSyncPage — enrich / התראת חוסר / יצירת day guest. **פורטל:** `meal_plan` בלבד מספיק — מוצג «חצי פנסיון» / «פנסיון מלא» בלי שעות ארוחה.
- tasks: ניהול משימות שטח ואחזקה. סטטוסים: 'pending_approval', 'open', 'in_progress', 'done', 'rejected'.
- room_status: ניהול מערך הניקיון וסטטוס החדרים (נפרד לחלוטין מ-guests.status). קבוצת Whapi «צ'ק אין צ'ק אאוט» (`WHAPI_HOUSEKEEPING_GROUP_ID`): מחזור turnover — `Co N`→`checked_out`+לניקיון → `N✅`→`ממתין לאישור` (פעמון) → AICopilot→`room_ready` WA+`guests.room_ready`+`פנוי` → `N צ'ק אין`→`checked_in`+תפוס. **Co gate reset (2026-08-02):** כל `N co` — גם בלי אורח יוצא ב-DB — מסנכרן `room_status` ל-`לניקיון` + איפוס `room_clean_status`/`jacuzzi_status` (`syncRoomToCleaning` / `roomApprovalGate.ts`). **AICopilot stale gate (2026-08-02):** שער `ממתין לאישור` ישן (>24ש') או שקדם ל-Co אחרון נסגר אוטומטית בטעינה; כרטיס מציג מקור WA מ-`housekeeping_wa_events` (`source_line` + שולח). **Observer שקט (2026-08-01):** קליטה + עדכון XOS בלבד — **אין** תשובות בקבוצה כברירת מחדל; `HOUSEKEEPING_WA_GROUP_REPLY=true` מחזיר ack/clarify. **רשת ביטחון צ'ק-אאוט (2026-07-30):** `whatsapp-cron` `autoCheckoutMissedSuiteDepartures` ב-16:00 ישראל — אם עדיין `checked_in` ביום עזיבה בלי `Co N` → `performSuiteCheckOut` + תור סקר; fallback סקר בלבד @19:00 נשאר (`departure_fallback_enabled`). **החלפת אורח בצ'ק-אין (2026-07-30):** `housekeepingCheckInSignal` — `צק אין` לחדר עם אורח `checked_in` שעוזב היום + אורח חדש `arrival_date=היום` → `applyHousekeepingCheckOutSignal` (אותו נתיב Co: audit, סקר, מועדון) בשקט + צ'ק-אין לנכנס; ack בקבוצה כולל `יצא קודם: [שם]` ב-turnover. **Co turnover hint (2026-07-31):** `housekeepingCheckOutSignal` — אחרי `N co` מוצא אורח עם `departure_date≤היום` בחדר (כולל `suite_rooms` + תוויות מרובות `·`) ומוסיף ל-ack `מגיע היום: [שם]` כשיש `arrival_date=היום`. **`housekeepingGuestLookup` (2026-07-17):** שיוך דרך `suite_rooms` (מרובה-חדרים) + lifecycle scoring; צ'ק-אין/אאוט מסונכרנים ב-`_shared/suiteCheckinSync.ts` (`performSuiteCheckIn` / `performSuiteCheckOut`). **Premium Day ≠ suite # (2026-07-30):** `Premium Day 1` לא מתאים ל-`ג׳ספר 1` לפי ספרת סוף — `isDayPassRoomLabel` + סינון day-pass ב-lookup. **תאריכי שהייה = שער ראשי (2026-07-30):** CI/CO לפי `arrival_date`/`departure_date` לפני סטטוס; בלי `departure_date` אין Co; כפילות → `ambiguous_guest` בקבוצה (לא ניחוש שקט). `N✅`→ממתין לאישור; `N צ'ק אין` / `CI N`→`guests.checked_in`+תפוס; `Co N`/`N co` (כולל inline «שילמו בקבלה 7 co»)→`guests.checked_out`+`checked_out_at`+לניקיון (`housekeepingCheckOutSignal.ts`). `already_checked_in` → ack ℹ️ בקבוצה (לא שקט). **רשימות חדרים (2026-07-16):** `housekeepingWaParse.ts` תומך ב-`4,5` / `4 5` בשורה ואז `צ׳ק אין` / `✅` / `co` בשורה הבאה (או `4,5 צ׳ק אין` באותה שורה) — pending rooms בין שורות; **וריאנט `ציק` (2026-07-18):** מנורמל ל-`צק` לפני parse. **הועברה + ✅N (2026-07-26):** הודעות מועברות לא נזרקות — `normalizeHousekeepingBody` מסיר «הועברה» ומפרסר; `✅ 23` / `✅ 6,11,14`; near-miss clarify רק על הודעה קצרה עם סיגנל חדרנות (לא צ'אט). ack בקבוצה על updated/already_pending/occupied. **סקר משוב סוויטות (2026-07-16, migration 220):** אחרי Co מוצלח — תור `post_checkout_survey_queue` (ברירת מחדל 15דק, `bot_config.post_checkout_survey_delay_minutes` — עריכה ב-ACC Stage 5, 0–120דק) → `whatsapp-cron` → `whatsapp-send` `checkout_fb` עם `{{portal_url}}#survey` (Whapi/session); cron `checkout_fb` לסוויטות כבוי — רק טריגר חדרנות. **ACC Live Queue (2026-07-21):** `automation-queue` ממזג `post_checkout_survey_queue.send_after` (Co+delay) — לא מציג שעת legacy מ-`departure_date`. **Catch-up יומי:** אורחי סוויטה `checked_out` + `departure_date=היום` בלי `msg_checkout_fb_sent` נכנסים לתור אוטומטית (`catchup_departed_today`). **פורטל:** `guestSurveyEligibility.ts` — סוויטה אחרי checkout רואה `#survey` ויכולה לשלוח (`guest-portal-data` + `guest-portal-survey`). `already_checked_out` עדיין מעדכן `room_status` לניקיון ומנסה לתזמן סקר אם לא נשלח. **RoomBoard.js (2026-07-14):** שיוך אורח חכם (`roomBoardGuestResolve.js`); פילטר «הגעות היום»; **סנכרון אוטומטי** — `resolveEffectiveRoomStatus` מציג לפי פרופיל אורח; `syncBoard` מתקן `room_status` בשקט בטעינה + realtime (guests/room_status/suite_rooms) בלי כפתור ידני (`roomBoardSync.js`).

4. ארכיטקטורת ה-Webhooks וסוכני ה-AI

whatsapp-webhook (צינור העיבוד הנכנס)
**Burst + outbound dedup (2026-07-17):** `_shared/guestInboundBurst.ts` — `coalesceGuestInboundBurstIfLeader` (1.8s wait, 5s window, per `inbox_channel`) משותף ל-Meta ול-Whapi; Whapi `sendGuestDmReply` חוסם outbound זהה לאותו טלפון תוך 120s.
**POST auth (2026-07-16):** `X-Hub-Signature-256` + `META_APP_SECRET` — חתימה חובה על כל הודעה נכנסת (לא רק GET verify_token).
1. לחיצות כפתור (Interactive / Quick Replies): ניתוב קשיח (Hardcoded) ישירות בתוך הקוד. אסור לשלוח לחיצת כפתור ל-Gemini/LLM.
2. הודעות קוליות (Voice/Audio): הורדה מ-Whapi, תמלול אוטומטי (Gemini), והזרקת הטקסט לתחילת צינור הזיהוי כאילו הוקלד.
3. In-Room Context Override: אורח בסטטוס pending/expected שכותב מילות מפתח של חדר מקודם אוטומטית ל-checked_in וה-Persona עוברת לטון "בתוך הריזורט".
4. Tier-0 Interceptions (אפס טוקנים):
   * קריאות שירות (משק/תפעול) — **Human-First (2026-07-22):** טקסט חופשי ב-WA (Meta/Whapi) מזהה allowlist ➔ `human_requested` + `needs_callback` + משפט handoff (`flagGuestOperationalInboxHandoff`) — **בלי** `createGuestOpsTask` אוטומטי (מניעת רעש בקבוצת קריאות). קריאה לשטח נוצרת רק ידנית ע״י נציג, או מפורטל אורח בלחיצה על כפתור allowlist (`guest-portal-ops-request`). נתיב מזוודות/יציאה (`handleDepartureAssistIntercept`) בוטל.
   * Stay-Change Shield: בקשות ל-Late checkout או שינוי תאריך נחסמות מיידית לפני ה-LLM, מדליקות דגל date_change לצוות.
   * **ביקורות מתקנים (migration 212):** `_shared/guestFacilityReview.ts` — Tier-0 לפני reflection כללי; שומר ל-`guest_feedback` עם `facility_category` (מסעדה/ספא/פטיו/…) + `rating` אופציונלי; Meta גם כלי `log_facility_review` בשיחת FAQ. **Incomplete praise guard (2026-07-18):** רשימה שנקטעת (`, הבריכה`) → לא facility template.
   * **Truncation guard (2026-07-18):** `guestBotLlm` `llmTruncated` על MAX_TOKENS + `isReplyObviouslyTruncated`/`endsWithMidWordHebrewCut`; Whapi+Meta `resolveTruncatedReplyFallback` (check-in + dining).
   * **Dining / meal-decline Tier-0 (2026-07-18):** `isDiningQuestion`/`buildDiningReply`, `isMealDeclineOrApology`/`buildMealDeclineAck` — Meta+Whapi לפני LLM.
   * **Ice / amenity instant ops:** רק מנתיב פורטל (או אישור ידני) — `createGuestOpsTaskWithInstantAmenityDispatch` → `notify-manual-task` ל-amenities; וואטסאפ חופשי לא פותח task (Human-First 2026-07-22).
   * **Tier-0 allowlist expansion (2026-07-20):** `automationSchedule.ts` — כוסות/צלחות, ניקיון בבקשה, bare shorthand (`מגבות`, `2 כוסות`) ל-checked_in בלבד; פורטל CTA `הזמנת שירות לחדר` → `isPortalTrustedOpsLabel`; Whapi+Meta דרך `shouldInterceptOperationalInHouseRequest` (לא הרחבת LLM). **משקאות + מניעת בלבול כניסה (2026-07-21):** allowlist כולל בקבוק/זירו/קולה; `isInRoomDeliveryRequest` מונע מ-`isCheckInPolicyQuestion` לתפוס «אפשר…לחדר»; בקשה פיזית שלא מסווגת → `shouldHandoffUnrecognizedInRoomRequest` + `human_requested` (לא FAQ שעות כניסה).
5. משפט הפניה לצוות (`_shared/guestBotHandoff.ts`): "אני בודק את זה מול הצוות שלנו ונחזור אליך בהקדם 🙏" — זהה בשני הבוטים. כשהבוט שולח אותו → `human_requested` (נקודה אדומה ב-Inbox). **בקשת חזרה טלפונית / נציג** (`detectGuestHumanRequest` + `GUEST_CALLBACK_ACK_SENTENCE`) — Tier-0 משותף ל-Meta ול-Whapi: בלי LLM, מאשר שהצוות יחזור (אסור להפוך ל"תוכלו ליצור קשר"). Meta: עונה גם ללא פרופיל אורח; Whapi DM: auto-reply רק לאורח עם פרופיל פעיל (`shouldAutoReplyGuestWhapiDm`). ניווט: תפעול ב-WA → Tier-0/`log_guest_request` מסמנים Inbox בלבד (לא קריאת שטח); לוח בקשות / מנהלות → handoff.
6. מוח הבוט משותף (`bot_settings` + `_shared/guestBotSettings.ts` / `guestBotLlm.ts` / `guestBotLlmTools.ts`) — `assembleGuestBrainPrompt` בודד ל-Meta+Whapi; `resolveGuestBrainKnowledgeInjection` — RAG chunks בלבד כשיש hit; full KB רק כשאין הודעה; chitchat בלי hit = פרסונה בלבד (לא dump מלא); שאלה עובדתית בלי כיסוי → `lowConfidenceHandoff`; context אורח ב-`buildGuestContextForAi.ts` (כולל **שעה בישראל + ברכת זמן** מ-`guestTimeGreeting.ts`; `meal_plan`+שעות ארוחה דרך `formatGuestMealsForAi` + `guest_profile` דרך `formatGuestProfileForAi` — **לשימוש LLM בלבד, לא לשפוך לאורח**); **ברכת זמן Tier-0 (2026-07-19):** `greeting_reply` (היי/שלום) — `applyTimeGreetingToGuestReply` ב-Meta+Whapi (בוקר/צהריים/ערב/לילה לפי `israelLocalHour`); **Dining Tier-0 (2026-07-19):** בוקר = שעות אחידות מ-`knowledge_base` לכל האורחים (`formatResortBreakfastLine`); צהריים/ערב = `lunch_time`/`dinner_time` מפרופיל בלבד; `breakfast_time` ב-DB לא משמש לתשובות בוט; שאלה כללית → `buildDiningReply` מ-KB; פנסיון/תזונה → `isGuestOwnMealQuestion` + `buildGuestOwnMealReply`; היסטוריית LLM מאוחדת per-phone (`guestConversationHistory.ts`, 6 הודעות meta+whapi); Meta FAQ דרך `generateGuestChatReplyWithTools` (tools); Whapi דרך `generateGuestChatReply`; RAG keyword + `lowConfidenceHandoff` ב-`guestRag.ts`; בדיקת סתירות KB↔bot_config ב-`guestKnowledgeValidation.ts` (השוואת סטי שעות — subset/superset, לא מחרוזת מלאה; + אזהרה ב-BotSettings); `guestRag.ts` — prefix עברי + מזמינים↔הזמנת + chunk לפי שורה; audit שבועי `runWeeklyGuestHallucinationAudit` ב-`whatsapp-cron` (יום ראשון אוטומטי; ידני: `?audit=1` ב-URL **או** body `{audit:true}` — `functions.invoke()` לא מעביר query string; audit ידני הוא early-return לפני ה-kill switch של `CRON_ENABLED`, אפס הודעות, לא מפעיל את צינור השיגור); כרטיס «🩺 בריאות המוח» ב-BotSettings — סטטוס אודיט (`bot_config` `guest_hallucination_audit_last_run`/`_summary`) + סתירות KB + כפתור הרצה; balloon+admin Tier-0 ב-`guestBalloonAdminIntercept.ts` (שני ערוצים). הדלקה/כיבוי per-channel ב-`bot_config` (`bot_active` / `bot_active_whapi`). **Firewall יוצא:** `_shared/guestBotSanitize.ts` — חובה לפני שליחה לאורח בשני הערוצים.
7. אוטומציית שבת (migration 172): הגעה בשבת → שלבים 2.5 (יום שישי 15:00) ו-3 (בוקר שבת, צ׳ק-אין 18:00) נשלחים אוטומטית דרך מכשיר הסוויטות (Whapi) עם סקריפטים נפרדים (`night_before_reminder_shabbat`, `stage_3_morning_shabbat`) + תמונה `suiteshabat.jpeg` — עריכה ב-AutomationControlCenter → וריאנט שבת. `dispatch_channel=whapi` עדיין תקף לכל השלבים; שבת מנותבת גם בלי סימון ידני כש-`GUEST_WHAPI_SUITES_ENABLED=true`.
7.1 **Stage 1 אישור הגעה — מקור אמת (2026-07-17, Meta-default 2026-08-03):** ברירת מחדל Dream Bot — תבנית `dream_arrival_confirmation` (חלון סגור) או `bot_scripts.pre_arrival_2d` (חלון פתוח / cohort=whapi). ACC → שלב 1 «מקור האמת» + `Stage1ArrivalPanel`. רשת ביטחון: `ensureArrivalConfirmationCta` + מתג `bot_config.stage1_auto_append_cta`. **זיהוי תשובת אורח (2026-07-22):** כפתור Meta / ביטוי תבנית → Tier-0; טקסט חופשי → `resolveArrivalConfirmationIntent` (Gemini).
7.2 **Stage 2.5 Meta templates (2026-07-31):** `night_before_suites` / `night_before_suites_shabbat` — `resolveMetaTemplateImageHeaderUrl` שואל Meta API: גרסה מאושרת **body-only** = בלי header (שלח עכשיו); אחרי אישור IMAGE ב-`night_before_suites_shabbat` → `suiteshabat.jpeg` אוטומטית. Whapi path = `session_message_image_url_shabbat` כמו קודם.
8. Staff Voice Assistants (`_shared/executiveIdentity.ts` + `_shared/executiveAssistant.ts`, נקרא מ-whapi-webhook לפני handleGuestDirectMessage דרך `isStaffAssistantInbound`): **אליעד + מייק** — `KNOWN_EXECUTIVES` + `personaOverlay` + fallback env/profiles. **אדיר (`972546294885`)** — tier `front_desk` «עוזרת דלפק סוויטות»; כללים `module='front_desk'`; כלים `get_arrival_desk_brief` / `resolve_guest_alert` / `learn_front_desk_rule` (בלי CEO override / digest / ארכיטקט / team analytics). **בריף בוקר 07:00** — `front-desk-morning-cron` + `frontDeskMorningBrief.ts` (migration 214): הגעות, בקשות פתוחות. **תיקון 2026-07-16:** `israelYmd()` בלי ארגומנט קרס ב-07:00 — default `now=new Date()`; שליחה ידנית `?force=1` / מדריך `?onboarding_only=1`. **הפעלה ראשונה:** `frontDeskOnboarding.ts` — מדריך יכולות חד-פעמי לפני הבריף (`bot_config.front_desk_onboarding_sent`); הבריף היומי אחרי כן בלי בלוק «כוח בידיים». **תווית תשומת לב (2026-07-26):** `requires_attention` בבריף הגעות = `⚠דורש תשומת לב` (לא VIP); הודעת תיקון חד-פעמית `bot_config.front_desk_attention_label_notice_sent` / `?attention_notice_only=1`. **עריכת תבניות (migration 216):** `staff_message_templates` + `ExecutivePlaybook` → «הודעות ודוחות» — עריכה/תצוגה מקדימה/היסטוריה לאדיר ואליעד (`staff-notify-preview`). **דוח אליעד** (`resort-digest-cron` 07:00): **יומי = פולס קצר** `composeExecutiveMorningPulse` (אתמול בשורה אחת + היום + מקס 2 התראות תצפיתיות — בלי `👉 מומלץ`/משימות); **יום ראשון:** `composeSundayFeedbackNudge` מוסיף קישור ל-`feedback_dashboard` (תמיד, גם בלי סקרים אתמול); שבועי/חודשי = `composeResortDigestMessage` מלא (בלי שורת משימות). `get_ops_digest_now` daily = אותו פולס. אנליטיקת צוות בדחיפה הוסרה — רק בכלי `get_team_ops_analytics` בשיחה. **UI סוכנים (2026-07-17, super_admin בלבד מ-2026-07-19):** `ExecutivePlaybook` לשונית «סוכנים» — `StaffAgentsPanel` + `staffAgentRoster.js` (נועה=אליעד/מאיה=מייק/ליאת=אדיר/סיגל/Dream Bot) — כרטיסי דמות, פולס אחרון, תצוגה מקדימה חיה; מסך `executive_playbook` מוגן ב-`ROUTE_ACCESS` ל-`super_admin` בלבד (לא `admin`), גם בסיידבר. **שמות דמות ייחודיים (migration 230, 2026-07-19):** `ExecutiveProfile.assistantName` (`KNOWN_EXECUTIVES`/`KNOWN_FRONT_DESK`) — נועה/מאיה/ליאת מציגות את עצמן בפתיח (`{{assistant_name}}` ב-`DEFAULT_PERSONA_TEMPLATE` + `buildExecutivePersona`) ובפולס הבוקר של אליעד (`opening_line`); migration מסנכרנת שורות DB קיימות (`executive_bot_settings.persona_prompt` + `staff_message_templates.eliad_digest_shell`) לניסוח החדש. **אנליטיקת צוות (migration 219):** `staff_group_messages` (כל הודעת קבוצה כולל chitchat) + `get_team_ops_analytics` — נוכחות %, מעורבות תפעולית (פתיחות/סגירות קריאות, אותות חדרנות), זמן סגירת קריאה, checkout→מוכן (`teamOpsAnalytics.ts`); ingest ב-`whapi-webhook` דרך `staffGroupIngest.ts`. שליחת תשובה: `deliverExecutiveDmReply` + retry unclaimed webhook.
   כלים: 16 תפעוליים לאליעד/מייק (כולל `get_team_ops_analytics`); **+4 ארכיטקט למייק** (`get_system_health`, `get_executive_action_log`, `list_executive_rules_audit`, **`send_executive_brief`** — עדכון נקי לאליעד אחרי טיוטה+אישור, בלי «דגש ממייק», שוברים מחוץ לטווח); **+3 דלפק לאדיר** (כולל `escalate_to_eliad`). `list_guests_by_date`, `list_guest_alerts`, `set_guest_status` (room_ready בלבד), `learn_executive_rule` / `learn_front_desk_rule`. שער server-side: `tool_forbidden_for_role`. כללים `xos_ai_rules` לפי `module` + `owner_phone` (188).

מנוע אישור משימות שטח (Human-in-the-Loop)
משימות `pending_approval` נוצרות מפורטל (כפתורי allowlist) או ידנית ע״י צוות — **לא** מטקסט חופשי של בוט וואטסאפ (Human-First 2026-07-22). אחרי אישור ב-OperationsBoard.js, `notify-manual-task` מתרגם לאנגלית ומשגר לקבוצת Whapi. SLA נמדד מרגע האישור.
Failsafe (Hybrid unanswered escalation, `_shared/handoffEscalation.ts` + `sla-escalation-cron`): אם קבלת לא אישרה תוך 7 דק׳ — ה-cron קורא לאותו `notify-manual-task` (auto-approve + שיגור לתפעול) ומעדכן את מייק/אליעד/אדיר ב-Whapi DM. **DM אישי ללידור (`SLA_OPS_ALERT_PHONE`) כבוי כברירת מחדל** — רק עם `SLA_OPS_PERSONAL_ALERT_ENABLED=true`. בקשות רכות ב-Inbox כולל `operational_request` (אחרי Human-First) — soft SLA 20 דק׳ → פינג קבלה (`SLA_GUEST_ALERT_PHONE`) עם `handoff_escalated_at` (migration 186). דורש `SLA_ESCALATION_ENABLED=true`.

כרטיסי משימה בקבוצת Whapi (whapi-webhook + notify-manual-task): תבנית אחידה `_shared/taskCard.ts` — `📌 New Task Opened: Suite X` / `📋 Task` / `⏰ Status: Pending`; תג מקור: `guest_request` (פורטל/ידני) → `[BOT]`, `inbox_routed` → `[GUEST WA]`, `manual` → `[MANUAL TASK]`. בלוח: `guest_request` = «🤖 בוט · בקשת אורח».

5. קונוונציות קוד וקווים אדומים

- **Staff auth / owners (DEPLOYED 2026-07-17, migration 228):** `src/utils/admin.js` — `tzalamnadlan@gmail.com` (owner) + `mikeka13@gmail.com` (co-owner) = `super_admin`; Google Sign-In whitelist = `GOOGLE_AUTH_WHITELIST` (same file); DB trigger `handle_new_auth_user` mirrors roles on `auth.users` INSERT.
- שפה ועיצוב: ממשק עברית מלאה (RTL). שימוש ב-CSS Variables בלבד (var(--gold)). מצבי Hover מנוהלים ב-JS (useState).
- איסור שימוש ב-.single(): בשאילתות Supabase יש להשתמש תמיד ב-.maybeSingle() כדי למנוע קריסות קוד.
- קריאות ל-Edge Functions: תמיד supabase.functions.invoke() (לעולם לא fetch גולמי).
- אבטחת מפתחות: אין להחזיק טוקנים של Meta, מפתחות AI או סודות VAPID בתוך פרונטאנד או ב-Git.
- Whapi outbound (`_shared/whapiSend.ts`): timeout 45s → `timeout_no_response` (סטטוס `timeout`, לא `failed`). Inbox מציג «לא ודאי אם הגיע» — בלי fallback אוטומטי ל-Meta (סיכון כפילות).

6. פרוטוקול העלאה לפרודקשן (Autonomous Deploy)

בסוף כל פעולת פיתוח, יש לבצע את סדר הפעולות הבא:
1. שינוי ב-src/ ➔ מריצים npm run build ודוחפים ל-Git (Vercel מאזין ל-main).
2. שינוי ב-supabase/functions/ ➔ מריצים npx supabase functions deploy --no-verify-jwt (כולל פונקציות שצורכות את _shared/ אם השתנה).
3. שינוי ב-supabase/migrations/ ➔ מריצים npx supabase db push.

7. עקרון הפעולה הסוכנית (The Agentic Loop) & חקירת המערכת

- מחקר מדורג ואקטיבי (Active Context Building): כשאתה מתבקש לנתח באג או להוסיף פיצ'ר, אל תניח שאתה יודע הכל מראש. הפעל את "יכולות הביצוע" שלך כדי לקרוא קבצים רלוונטיים (קובץ גורר קובץ) ובנה את ההקשר במדויק לפני כתיבת שורת קוד אחת.
- אוטונומיה בפתרון תקלות (Autonomous Debugging): אם הרצת פעולה (כמו קריאת קובץ, פקודת טרמינל או שאילתה למסד הנתונים) ונתקלת בשגיאה, אל תחזור מיד למייק כדי להציג לו אותה. הפעל את "הלולאה הסוכנית" שלך: חקור את הודעת השגיאה, בדוק קבצים נוספים, ונסה לפתור את התקלה בעצמך. פנה לעזרה רק אם הגעת למבוי סתום אמיתי שדורש התערבות אנושית.

8. סוכני פיתוח — Claude Code Desktop (2026-07-24)

Mike's dev agents (לא סוכני מוצר נועה/ליאת/סיגל): `.claude/agents/maya-partner.md` (מאיה — שותף יומי), `qa-gate.md` (בודק QA read-only), `security-sentinel.md` (שומר אבטחה). Skills: `/xos-qa`, `/xos-security`. זרימה מלאה: `docs/xos_agent_playbook.md` §13. Cursor + Claude Code חולקים `CLAUDE.md` — session אחד לכלי על אותו repo dir.
