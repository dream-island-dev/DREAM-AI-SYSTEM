2026-07-19 | Orit CS — guest contact extract from form body + WhatsApp inbox link | migration 231 `guest_contact_*` on `orit_agent_threads`; `_shared/oritGuestContactExtract.ts` parses שם/טלפון/דוא"ל from website form emails; «שלחי» + auto-ack reply to extracted email; sync backfills existing threads; UI shows guest details + «💬 וואטסאפ באינבוקס» (Whapi). Deploy: db push + `manager-mail-sync` + `manager-mail-send` + `manager-mail-auto-ack` + frontend.

2026-07-19 | Guest bot time greetings + tighter RAG injection | `_shared/guestTimeGreeting.ts`: Israel time-of-day greetings (Tier-0 היי/שלום Meta+Whapi + LLM context line); `resolveGuestBrainKnowledgeInjection` — no full KB dump on chitchat RAG miss (factual miss still handoff). Deploy: `whatsapp-webhook` + `whapi-webhook`.

2026-07-19 | Automation room-assignment gate — no suite/day-pass room, no cron | `_shared/suiteNames.ts`: `isEffectiveSuiteGuest` = canonical suite room only; Premium Day always day-pass; `missing_room_assignment` blocks all pipeline stages in `checkEligibility` + `whatsapp-send` (manual/room_ready exempt). ACC: skip label + premium-day conflict badge. Deploy: `whatsapp-send` + `whatsapp-cron` + `automation-queue` + frontend.

2026-07-19 | Guest bot meals — breakfast universal from KB, lunch/dinner from profile | Breakfast Tier-0 + LLM: `08:00–10:30` (etc.) from knowledge_base for all guests; `breakfast_time` ignored for replies. Lunch/dinner answers use `lunch_time`/`dinner_time` from guest profile only. Deploy: `whatsapp-webhook` + `whapi-webhook`.

2026-07-19 | Guest bot dining fallback strips pipe breakfast segment | `formatRestaurantHoursLine` omits `בוקר … |` from bot_config when KB empty — Armonim fallback shows evening only. Deploy: `whatsapp-webhook` + `whapi-webhook`.

2026-07-19 | Guest bot meals — breakfast ≠ Armonim pipe hours | Tier-0: "ארוחת הבוקר שלנו" → pension time from DB (+ meal_location); KB verbatim for food stations; never `bot_config` pipe "בוקר …" as breakfast. Dinner still KB + guest `dinner_time`. Deploy: `whatsapp-webhook` + `whapi-webhook`.

2026-07-19 | Check-in guest profile 360 — merged edit + collapsible ETA board | GuestsPage: name click → `GuestContextDrawer` (not chat); לוח זמני הגעה collapsed by default (▶ toggle). `AddGuestModal` merged with smart profile (`GuestSmartProfileFields`) incl. `arrival_time`; drawer header 💬/✏️. Deploy: frontend only.

2026-07-18 | Guest bot reliability — truncation + Tier-0 + ice ops + HK ציק + survey UI + checked_out modal | Phases 1–3: truncation guard + dining/meal Tier-0 + ice instant amenity dispatch. Phase 4: `ציק`→`צק` in housekeepingWaParse. Phase 5: AddGuestModal `checked_out`; survey badge decoupled from category low. Audit fix: `resolveTruncatedReplyFallback` guest-intent-first (dining vs check-in token collision). Deploy: `whatsapp-webhook` + `whapi-webhook` + frontend.

2026-07-17 | Inbox blank-screen fix (TDZ crash) | `activeContact` useEffect moved below its `const` — dependency array evaluated before declaration caused `ReferenceError` on every mount. Deploy: frontend only.

2026-07-17 | Inbox Meta 24h false block when guest wrote on Whapi | `inferDefaultReplyChannel` follows last guest inbound (not bot outbound); auto-switch unified threads to Whapi when Meta window closed but Suites inbound is fresh; banner/send guard via `shouldWarnMetaWindowClosed` (Meta path only). Deploy: frontend only.

2026-07-17 | Stage 1 arrival copy — single source of truth in ACC (Option 2) | `Stage1ArrivalPanel` + `stage1ArrivalCopy.js`: ניהול חכם שלב 1 = מקור האמת ל-Whapi (`bot_scripts.pre_arrival_2d`); תצוגה «מה האורח באמת מקבל» (כולל CTA אוטומטי); מתג `bot_config.stage1_auto_append_cta`; זיהוי «כן מגיעים» גלוי; Override + BotScriptEditor preview. `whatsapp-send` מכבד מתג. migration 229. Deploy: frontend + `whatsapp-send` + `npx supabase db push`.

2026-07-17 | ACC Stage 5 — editable post-Co survey delay | `AutomationControlCenter.js`: Stage 5 (checkout_fb) shows housekeeping-Co trigger explainer + `post_checkout_survey_delay_minutes` editor (0–120 min, save on blur); legacy 20:30 timing greyed for suites. Deploy: frontend only.

2026-07-17 | Staff Agents UI — persona cards (נועה/ליאת/סיגל/Dream Bot) | `StaffAgentsPanel` + `staffAgentRoster.js` in Executive Playbook tab «סוכנים»; live last push, preview, links to templates. Nav renamed «סוכנים חכמים». `staff-notify-preview` daily = morning pulse. Deploy: frontend + `staff-notify-preview`, `resort-digest-cron`, `whapi-webhook`.

2026-07-17 | Eliad morning pulse (Wave 1) — short daily push, no task assignments | `composeExecutiveMorningPulse` replaces long daily digest: yesterday one-liner + today outlook + max 2 observational alerts (VIP/pending approval/anomaly). Removed `👉 מומלץ` from all push digests. `get_ops_digest_now` daily matches cron. Deploy: `resort-digest-cron`, `whapi-webhook`.

2026-07-17 | Whapi guest DM anti-spam — burst + outbound cooldown | `_shared/guestInboundBurst.ts`: Meta-parity burst coalescing (1.8s/5s) on Whapi; identical outbound to same phone within 120s suppressed. Meta `whatsapp-webhook` refactored to same module. Deploy: `whapi-webhook` + `whatsapp-webhook`.

2026-07-17 | Auth login deadlock fix | `onAuthStateChange` deferred `loadUserWithProfile` via `setTimeout(0)` (Supabase auth lock deadlock); Google login shows real errors (no mock fallback); `isGoogleAuthAllowed`; missing Google CLIENT_ID banner. Deploy: frontend.

2026-07-17 | Operational Dashboard — live Supabase home | `OperationalDashboard.js`: tasks + guest_alerts + urgent signals (HITL, complaints, Inbox, missing ETA, Meta blocked); Realtime; sidebar ops badge from DB not localStorage. Deploy: frontend.

2026-07-17 | Google login whitelist — mikeka13@gmail.com super_admin | `GOOGLE_AUTH_WHITELIST` in admin.js; migration 228 trigger; fixes blocked Google sign-in for existing co-owner account. Deploy: frontend + db push.

2026-07-17 | Hebrew brand in guest scripts + BotScriptEditor labels | migration 227: Dream Island→דרים איילנד in bot_scripts; stage_3_morning meta_template_name→suite_welcome_morning; webhook greeting/spa fallbacks; עורך סקריפטים UI בעברית. Deploy: db push + whatsapp-webhook + whapi-webhook + frontend.

2026-07-17 | KB conflict false-positives + spa RAG matching | `guestKnowledgeValidation`: compare hour-token sets (subset/superset) instead of raw string equality — fixes 4 spurious checkout/pool/spa/restaurant warnings when KB and bot_config agree. `guestRag`: Hebrew prefix stripping, booking synonym expansion (מזמינים↔הזמנת), per-line chunking for single-newline KB. Deploy: `whatsapp-cron` + consumers of `_shared` + frontend.

2026-07-17 | BotConfigPanel KB-source banner + spa hint in BotSettings | When `bot_settings.knowledge_base` is non-empty, «ידע המלון» shows banner + link to מוח הבוט; DEFAULT_KB_HINT includes spa booking example. Frontend-only (not deployed).

2026-07-17 | Brain health card in BotSettings + on-demand hallucination audit | 🩺 card: audit status (`bot_config` `guest_hallucination_audit_*`), KB↔bot_config conflict count, KB presence; «הרץ בדיקה עכשיו» → `whatsapp-cron` body `{audit:true}` (functions.invoke can't pass query) — audit-only early return BEFORE `CRON_ENABLED` kill switch (zero sends, never triggers dispatch off-schedule); `?audit=1` URL still works; Sunday auto-run unchanged. Deploy: `whatsapp-cron` + frontend.

2026-07-17 | Housekeeping check-in ack — silence already_checked_in | `already_checked_in` → no ℹ️ to WA group; quiet `room_status→תפוס` sync + log only. Deploy: `whapi-webhook`.

2026-07-17 | Guest bot brain unification (Meta + Whapi parity) | `assembleGuestBrainPrompt` + `buildGuestContextForAi` + unified `fetchGuestChatHistory` (6 msgs cross-channel); Meta FAQ → `generateGuestChatReplyWithTools`; Whapi balloon/admin Tier-0 ported; RAG keyword + low-confidence handoff; `guest_knowledge_chunks` (migration 226); weekly hallucination audit in `whatsapp-cron` (Sunday Israel); BotSettings ⚠ KB vs bot_config conflicts. Deploy: `whatsapp-webhook`, `whapi-webhook`, `whatsapp-cron`, `db push`, frontend.

2026-07-17 | Housekeeping WA turnover hardening — multi-room lookup + unified check-in | `housekeepingGuestLookup` matches `suite_rooms` (not only `guests.room`); lifecycle scoring (Co→✅→approve→צק אין); `_shared/suiteCheckinSync.ts` shared with WA check-in; `room-clean-notify` uses same lookup + passes `roomId` for multi-room WA. Deploy: `whapi-webhook`, `room-clean-notify`.

2026-07-17 | Multi-room ops context + missing departure_date guard | Inbox suite room picker (`guest_profile.inbox.selected_suite_room`); Ops Board mandatory room on pending_approval; import blocks suite without departure (NIGHTS/iNights); UI ⚠ + `guest_alerts` `missing_departure_date` (migration 225); room_ready Whapi timeout → no admin 🚨 false alarm.

2026-07-17 | Claim-before-send on ALL whatsapp-send dispatch blocks (Phase C completion) | `claimStageDispatch` wrapper wired into stage_2_arrival, night_before, 3× morning blocks, room_ready + generic BRANCH D (was BRANCH D only). `finalizeDispatchAttempt`: sent-collision with migration 088 unique index (force resend / per-room room_ready) → `duplicate_blocked` + `actual_status` payload instead of zombie processing row. Deploy: `whatsapp-send` (from clean worktree). Commit `7ba0422`.

2026-07-17 | Whapi failover E2E QA script + docs realignment | New `docs/qa_whapi_failover.md` (manual SOS / auto probe failover / env override + restore, SQL checks). active_sprint refreshed to 07-17; spa sticky-room entry corrected — deployed since 07-14 (bc7c36b/b090f52), live QA pending. Docs-only.

2026-07-16 | Webhook inbound auth — Meta HMAC + Whapi shared secret | `metaWebhookSignature.ts` verifies `X-Hub-Signature-256` on Meta POST (`META_APP_SECRET`); `whapiWebhookAuth.ts` gates Whapi POST via `X-Whapi-Secret` (`WHAPI_WEBHOOK_SECRET`). Fail closed if secret missing. `guestBotLlm` logs failover to `ai_failover_events` for Whapi DM path too. Deploy: `whatsapp-webhook`, `whapi-webhook` + set secrets + PATCH Whapi settings headers.

2026-07-16 | Eliad daily digest — compact team KPIs section | `composeDigestTeamOpsSection` appended to daily `resort-digest-cron` + `get_ops_digest_now`: Adir presence/operational %, team avg resolve time, checkout→ready avg/median. `teamOpsDigestFetch.ts` shared fetch. Deploy: `resort-digest-cron`, `whapi-webhook` (executiveAssistant), `staff-notify-preview`.

2026-07-16 | Orit CS mobile detail scroll fix | Removed fixed-height + nested scroll trap on email detail — full page scroll + sticky back + pulse hidden on detail. Frontend-only.

2026-07-16 | AddGuestModal multi-room picker + suite_rooms manual sync | `+ חדר נוסף` per SUITE_REGISTRY; save → `guests.room` combined + `syncGuestSuiteRoomsFromSelection` (manual-* rows only; never deletes EZGO import rows). GuestsPage room column shows all suite_rooms. Frontend-only.

2026-07-16 | Doc 2 root fix — restore iReservationsLineId (legacy EZGO column) for instant preset | Pre-Agent parser used `iReservationsLineId` not `iResLineId`; Resilient Agent required wrong column → AI fallback. Also `sGroupName`/`sOperationRemark`/`dtCheckIn`. Frontend-only.

2026-07-16 | Doc 2 Excel header-row scan — skip title rows before EZGO columns | `matrixRowsFromHeaderScan` in CSV/Excel parse; P0 handoff doc for Claude Code if still broken. Frontend-only.

2026-07-16 | Doc 2 preset priority fix — stale memory no longer blocks instant EZGO load | Preset runs before DB/AI; broken `import_mapping_memory` ignored; case-insensitive EZGO headers; no review screen for standard Doc 2. Frontend-only.

2026-07-16 | Doc 2 import regression — instant EZGO preset auto-load, no Claude gate | Preset/memory mapping skips MappingReviewPanel (direct grid); `normalizeImportRows` fixes Excel BOM/spaces; removed MappingReviewPanel useEffect that reset manual edits. Frontend-only.

2026-07-16 | Doc 2 mapping UX — approve bar no longer buried under paste textareas | `ArrivalImportPanel`: mapping gate moved directly under Doc 2 upload; auto-collapse EZGO email paste + scroll; `MappingReviewPanel` sticky approve bar; `DataSyncPage` Smart Paste below import hub. Frontend-only.

2026-07-16 | Housekeeping WA parser — multi-room list + action on next line (Adir `4,5` + `צ׳ק אין`) | `housekeepingWaParse.ts` pending-rooms across lines; comma/space lists; same for ready/checkout. Deploy: `whapi-webhook`.

2026-07-16 | Multi-suite room_ready — per-room WA send on GuestsPage + suite_rooms repair | migration 222; `suiteRoomReady.js` + GuestsPage per-room buttons; `suiteRoomReady.ts` canonical label match; profile shows per-room sent status. Deploy: `db push` + `whatsapp-send` + frontend.

2026-07-16 | Orit CS Agent — לידים/תלונות tabs + tier-0 classify + Graph outbound send + auto-ack | migration 221; `oritAgentClassify.ts`, `oritAgentSend.ts`, `OritCustomerServicePanel.js`; deploy `db push` + `manager-mail-sync/send/auto-ack/analyze` + frontend.

2026-07-16 | Survey scale 1-3 emoji (😞/🙂/🤩) + post-survey club/WA positive_feedback | guestSurveyUi + GuestSurveyForm + guest-portal-survey thresholds.

2026-07-16 | Suite post-checkout survey after housekeeping Co (~15min) + stay fixes | migration 220: `post_checkout_survey_queue`, `checked_out_at`, `checkout_fb` portal script; Co→enqueue→cron→whatsapp-send; cron `checkout_fb` disabled for suites; day-pass 19:00 auto-checkout + departure backfill; HK inline `N co`, already-Co still לניקיון; emoji/SLA courtesy shield (meta+whapi prior outbound). Deploy: `db push` + `whapi-webhook`, `whatsapp-cron`, `whatsapp-send`, `sla-escalation-cron` + frontend if pushed.

2026-07-16 | User Rules restored as 7 .mdc files (global ~/.cursor/rules + project .cursor/rules) | git-commit-safety, pull-requests, communication-style, etc.

2026-07-16 | Cursor recovery — chat index (239 transcripts), composer headers export (201), user rules backup, recovery guide | `docs/cursor_*`, `scripts/cursor-*.py|mjs`.

2026-07-16 | Orit CS Agent — mobile master-detail (list OR detail, back button, Android history) | `OritCustomerServicePanel.js` + `useIsMobile.js`; fixes cut-off detail pane on phone.

2026-07-16 | Fix Adir morning brief cron — `israelYmd()` default `now`; manual send onboarding+brief to Adir | automationSchedule, front-desk-morning-cron deployed.
2026-07-16 | Adir voice tasks — front_desk_voice source + [FRONT DESK] Whapi tag; room required; overlay lists create_executive_task | taskCard, executiveAssistant, migration 218.

2026-07-16 | Adir morning brief — today only (no tomorrow line in DM); tomorrow stays in voice tool | frontDeskMorningBrief.

2026-07-16 | Staff notify templates UI — edit Adir/Eliad messages & digests in Executive Playbook | migration 216 `staff_message_templates`, `StaffNotifyPanel`, `staff-notify-preview`.

2026-07-16 | Eliad digest — Hebrew SLA labels + deterministic action hint; Adir one-time onboarding before morning brief | resortDigestStats, frontDeskOnboarding, front-desk-morning-cron.

2026-07-16 | Adir staff notifications — unified Hebrew templates | `_shared/adirNotifyMessages.ts` — SLA/handoff/pre-checkin/portal/inventory; polished ETA + morning brief. Deploy: `sla-escalation-cron`, `whatsapp-webhook`, `guest-portal-order`, `inventory-portal-submit`.

2026-07-16 | SLA personal DM to Lidor disabled by default | `sla-escalation-cron` — `SLA_OPS_PERSONAL_ALERT_ENABLED` must be `true` for `SLA_OPS_ALERT_PHONE` pings; guest_request breaches fall back to ops group. Deploy: `sla-escalation-cron`.

2026-07-15 | Executive persona — «אני העוזרת האישית של {{name}}» + ExecutivePlaybook Adir card | DEFAULT_PERSONA + Eliad focus/overlay; migration 215 DB sync; UI read-only Adir profile + front_desk rules.

2026-07-15 | Front desk morning brief — Adir daily 07:00 Whapi DM | `frontDeskMorningBrief.ts` + `front-desk-morning-cron` (arrivals, open requests, «כוח בידיים» hints); migration 214 log + pg_cron. Deploy: function + `db push`.

2026-07-15 | ETA → Adir DM + Front Desk Assistant (אדיר) | `arrivalEtaAdirNotify.ts` — Whapi DM לאדיר כשאורח מדווח שעת הגעה (Meta/Whapi); `executiveIdentity` tier `front_desk`; כלים `get_arrival_desk_brief`/`resolve_guest_alert`/`learn_front_desk_rule`; whapi intercept `isStaffAssistantInbound`. 55 Deno tests. Deploy: `whapi-webhook` + `whatsapp-webhook`.

2026-07-15 | Executive Assistant — architect tools for Mike (system health, action log, rules audit) | 3 architect-only tools gated server-side to ARCHITECT_PHONE_DIGITS; Eliad keeps 15 operational tools. Deploy: `whapi-webhook`.

2026-07-15 | Executive Assistant — future arrivals + Eliad onboarding + per-executive persona overlays | New tool `list_guests_by_date` (arriving/departing/in_house by YYYY-MM-DD); system prompt shows today+tomorrow; `executiveIdentity.ts` `personaOverlay` — Eliad gets "how to work with me" guide on first message/help questions; Mike gets architect/XOS context. Migration 213 syncs live persona row. 38 Deno tests. Deploy: `whapi-webhook` + `db push`.

2026-07-15 | Inbox dismiss red alert — `dismissHumanRequest` on unified threads now clears meta+whapi (was filtering `inbox_channel=unified`, a UI-only key) | WhatsAppInbox.js + tests.

2026-07-15 | Whapi truncated-reply guard + facility typo (מסטאג) + Inbox dual-channel mute (🤖/🏨 per device, auto-claim on staff send) | whapi-webhook deploy + frontend push.

2026-07-15 | Facility reviews via guest bot — migration 212 `guest_feedback.facility_category`+`rating`; `_shared/guestFacilityReview.ts` tier-0 + `log_facility_review` LLM tool (Meta); Whapi tier-0; Feedback → משוב חופשי: facility filter chips (מסעדה מודגשת) + דירוג | whatsapp-webhook + whapi-webhook deploy + db push.

2026-07-15 | Orit CS bootstrap-only load — `orit-cs-bootstrap` returns mailbox+threads; UI stops RLS race that cleared threads after load | OritCustomerServicePanel + function deploy.

2026-07-15 | EZGO Operations email (Option A) — Doc 1 accepts Gmail `.eml` + HTML paste; panel «ממייל EZGO Operations» in ArrivalImportPanel (`ezgoEmailHtml.js`); no plain-text Gmail copy | DataSyncPage.

2026-07-15 | Inbox בריזורט/מגיעים היום — סוויטות בלבד | `isGuestInResortToday` = `checked_in` בלבד; סגמנט `arriving_today` לסוויטות בטרום-צ'ק-אין; צ'יפ חדש ב-WhatsAppInbox; ResortPulseBar מיושר; 29 tests.

2026-07-15 | Orit CS Agent — Graph M365 live path + UI connect | migration 208 `provider=microsoft`; `mailIngest.ts` Graph לפני IMAP; `OritCustomerServicePanel` כפתורי «חברי Outlook 365» + «סנכרן עכשיו» + תיקון טעינה; functions deployed; `docs/orit_outlook_connect.md` עודכן.

2026-07-15 | Smart Paste meal plan — HB/FB → `חצי פנסיון`/`פנסיון מלא` in portal (no meal times required); `inferMealPlanFromHints`; parse-raw-paste prompt hardened | stayMealsSchema + parse-raw-paste redeploy.

2026-07-15 | Smart Paste pipeline — `parse-raw-paste` Edge Function (Gemini 2.5 Flash JSON schema, suite_guest/day_guest); `SmartPastePanel` + fuzzy `match_guest_fuzzy` scenarios (enrich / missing alert / day guest) on DataSyncPage | migration 207 + functions deploy.

2026-07-15 | Smart Paste groundwork — `pg_trgm` + GIN index on `guests.name` + RPC `match_guest_fuzzy(p_name, p_arrival_date?)` (similarity > 0.3, max 15) | migration 207.

2026-07-14 | Morning Stage 3 send-window fix — `morning_suite`/`morning_welcome` capped 10:00; past window → `missed_window` (ACC manual only, no cron blast); `stage_3_morning` Whapi script synced to Meta 12:00/15:00 (was stale 09:00) | migration 206; automationSchedule.ts.

2026-07-14 | Whapi SOS service fallback — `dream_service_fallback` Meta template (כפתורים: יש לי בקשה | הכל בסדר תודה); `guest-emergency-broadcast` להגעות היום בלבד; Inbox מאוחד per-phone + בורר ערוץ שליחה | ACC: WhapiEmergencyBroadcastPanel; webhook ack לכפתורים.

2026-07-14 | Whapi failover + Dream Bot full guest outbound — migration 205: SOS ידני ב-ACC, auto-failover on /health probe, daypass=Meta only (never Whapi); spa sync opt-in suppress; missed_window for spa stages; 2.5s throttle in club broadcast | ACC Pulse: Whapi status + בדוק עכשיו + החזר ל-Whapi.

2026-07-14 | Housekeeping WA apostrophe normalize — `16 צ׳ק אין` / iOS U+2019 geresh variants → check-in | housekeepingWaParse normalize before match; whapi-webhook deployed.

2026-07-14 | Housekeeping WA check-in — `CI N` / `check in N` / `N ci` parsed like `Co N` for checkout; updates guests.checked_in + room תפוס | housekeepingWaParse.ts; deploy whapi-webhook.

2026-07-14 | spa_warmup_daypass anchor fix — DB `anchor_event=spa_time` (migration 204); cron ignores corrupted anchor; ACC «30 דקות לפני שעת הטיפול» UI + auto-repair on load | Per-guest spa_time from Spa Board, not arrival confirmation.

2026-07-14 | Day-pass spa journey polish — club only after positive survey; suites CTA after club join; consent_line for WA marketing; night_before_daypass spa-cohort gate | guest-portal-survey + GuestPortal + automationSchedule.

2026-07-14 | RoomBoard auto-sync — guest-aware display (resolveEffectiveRoomStatus) + silent DB reconcile on load/realtime; no manual button | roomBoardGuestResolve.js + roomBoardSync.js; WHAPI still source for cleaning/check-in/out events.

2026-07-14 | Feedback → סקרים: SurveyInviteTestPanel — preview WA invite + open portal #survey + single-guest Whapi send with confirm | Same `survey_invite_daypass` script; hermetic one-at-a-time.

2026-07-14 | Day-pass spa warmup — default 30 min before spa_time + ACC «X דקות לפני» editor; daypass channel → Whapi; migration 203 | Hermetic: spa cohort gate + msg_spa_warmup_sent + claim/dedup + cron 2.5s pulse. Stages morning/mid/checkout stay off intentionally.

2026-07-14 | Whapi suites restore after SOS — `WHAPI_GUEST_SOS_META` unset; `guest_suites_channel=whapi`; day-pass left `off`; redeployed automation-queue/whatsapp-cron/whatsapp-send/whapi-webhook | ACC Pulse: no SOS banner; Suites=Whapi.

2026-07-14 | Move Guest modal — room picker shows free/full by appointment time slot (home first); full rooms visible with Override | Reuses roomOccupancyAtSlot from spaStickyRoom.js.

2026-07-14 | Spa «יישור יום» safe align — greedy capacity check + cascade + parking-room mutual swaps; blocked list shows time/therapist + dismiss | Still therapist-sticky (move guests home only). No migration.

2026-07-14 | Guest Club broadcast — DEPLOYED | `guest-club-broadcast` + Feedback → סקרים panel: Whapi free-text or Meta template to `guest_club_members` status=active only (max 80/batch); dry-run + confirm.

2026-07-13 | Guest Club opt-in + preview/editor — DEPLOYED | Migrations 201–202; `guest-portal-club`; portal thank-you CTA; Feedback → סקרים preview/edit club copy + active members list. Broadcast offers = next.







2026-07-13 | Guest survey staff preview + editable labels — DEPLOYED | Shared `GuestSurveyForm` + `guestSurveyUi` (bot_config `guest_survey_ui` JSON, migration 197). Feedback → סקרים: תצוגה מקדימה (portal chrome) + עריכת תוויות (admin). Portal reads labels via `guest-portal-data.portalConfig.survey_ui`. Fixed stale `/5` display + negative threshold 4/10.

 Replaced the single global `GUEST_WHAPI_SUITES_ENABLED` gate with 2 independent bot_config-backed cohort selectors (ACC UI): `guest_suites_channel` (whapi\|meta, default meta/DreamBot) + `guest_daypass_channel` (off\|whapi\|meta, default off) — migration 196. `_shared/guestWhapiRouting.ts`: new `primeGuestChannelConfig()` (reuses `guestBotSettings.ts`'s cached KV fetch, module-level sync cache so all ~20 existing call sites keep their signatures unchanged) wired into the 7 real consumers (automation-queue, guest-portal-spa-request, whapi-webhook, whatsapp-cron, whatsapp-send, whatsapp-webhook, + guestInboundOrchestrator covered transitively). `shouldRouteGuestOutboundViaWhapiSuites` is now cohort-aware; `isStageEffectivelyActive` hard-blocks day-pass guests when channel="off" even if is_active=true (real OFF=OFF, not just "skip Whapi") — manual Override unaffected (Disable-Don't-Hide). **Live audit found migration 194/195 + guest-portal-survey/guest-portal-data/resort-digest-cron were ALREADY deployed to prod this morning** (08:03 UTC) despite `active_sprint.md` saying "awaiting כן" — `notification_log` showed 123 failed `spa_warmup_daypass` attempts (45 guests, 08:15-13:00 UTC) + 4 failed `survey_invite_daypass` (14:00-16:15) during today's Whapi-device SOS ban, all `whapi_text_401`/`meta_template_400` — is_active=false didn't block them (confirms the bug this session fixes), but zero guests actually received a message. **Survey scale 1-5→1-10** (Mike, Q1 — MVP stays day-pass+spa only): migration 196 widens `guest_surveys`' 6 category CHECK constraints (194 already live, can't edit in place); `guest-portal-survey` thresholds rescaled proportionally (GOOGLE_CTA_MIN_AVG_CATEGORY 4.0→8.0, NEGATIVE_CATEGORY_MAX 2→4, both still 80%/40% of scale); `GuestPortal.js` RatingRow category rows `max={5}`→`max={10}` + `small`. **checkout_fb_daypass vs. survey_invite_daypass dedupe**: `automationSchedule.ts` — spa-cohort day-pass guests now skip `checkout_fb_daypass` (`superseded_by_survey`, added to ACC's permanent-hide skip set) since they already get the structured survey as their one post-visit touch; non-spa day-pass guests unaffected. 29 rewritten + 3 new Deno tests (guestWhapiRouting.test.ts, automationSchedule.test.ts — 75 total pass), `deno check` delta-clean across all 7 touched Edge Functions (verified against a HEAD worktree baseline — fixed 2 real new `any`-vs-pinned-SupabaseClient-type errors by loosening `primeGuestChannelConfig`'s param type, same as the codebase's existing `ReturnType<typeof createClient>` convention), 331/331 jest, `npm run build` clean. `bot_config` write RLS requires admin/super_admin role — non-admin staff get a clear Hebrew FAIL VISIBLE toast on the new channel dropdowns, not a silent failure. **DEPLOYED 2026-07-13 evening:** migration 196 `db push` + functions `automation-queue`/`guest-portal-spa-request`/`guest-portal-survey`/`whapi-webhook`/`whatsapp-cron`/`whatsapp-send`/`whatsapp-webhook` + frontend push `main`.

2026-07-13 | P0: Whapi Suites device banned → SOS Meta/Dream Bot dual-path — DEPLOYED | WhatsApp restricted the Suites device (~17h, suspected automated-message/spam) + channel showed QR/disconnected — all Suites Whapi traffic (guest automation + housekeeping observer) unreliable/dead. New `WHAPI_GUEST_SOS_META` secret, checked inside `_shared/guestWhapiRouting.ts`'s `isGuestWhapiSuitesEnabled()` itself (not a new parallel gate) — every caller incl. `room_ready`'s direct read falls back to Meta automatically with zero per-caller edits; `isMetaGuestTemplateAllowed()` auto-true during SOS. `automation-queue.systemStatus.whapiGuestSosActive` + red ACC Pulse banner. 14 new Deno tests (23/23 pass), `deno check` delta-clean (39/66 pre-existing errors on whatsapp-send/whapi-webhook unchanged — systemic esm.sh version mismatch, not this change). Deleted temporary `debug-whapi-diag`. **Forensics:** queried prod `notification_log` — `morning_welcome` alone fired 493 failed attempts across 40 guests between 05:17–08:32 UTC today (retry-storm, not one guest); `whatsapp-cron` (carries the retry-storm fix below) deployed 08:38:20 UTC — storm ran the whole window on unprotected old code, stopped once the fix landed. Root cause already fixed; SOS is the ban-window lever, not the fix. Housekeeping-group inbound NOT covered by SOS (same banned device, no Meta substitute). Phase C `automationClaim` wiring for the 6 remaining fast paths (bigger than originally scoped — morning_welcome alone is 3 blocks) deferred to a tracked follow-up, not urgent since it didn't cause this ban.

2026-07-13 | Fix room_ready — always Whapi when Suites flag on (not Meta dream_room_ready1) — DEPLOYED | `whatsapp-send` room_ready fast-path: `useWhapiForRoomReady = isGuestWhapiSuitesEnabled()` instead of `shouldRouteGuestOutboundViaWhapiSuites(guest)`. Root cause: empty/stale `guests.room` (denorm lag vs suite_rooms / housekeeping `roomId`) dropped suite guests onto Meta `dream_room_ready1` after Whapi-first — Facebook template path failed while Suites device was fine. Housekeeping N✅ → ממתין לאישור → AICopilot approve unchanged. ACC: emptied stale `WHAPI_UNSUPPORTED_STAGES` (`room_ready`). Inbox «חדר מוכן» draft prefers `room_ready_reminder` on Whapi threads. Deployed: `whatsapp-send` functions deploy + frontend push (commit 0e2d026).

2026-07-13 | Smart Spa Board — therapist sticky-room hard gate + Move Guest + יישור יום (not yet deployed) | Follow-up on migration 193's advisory roster (no new migration). `src/utils/spaStickyRoom.js` (pure, 11 jest tests): `inferHomeRoomByTherapist` (earliest non-cancelled appt/therapist/day) + `resolveHomeRoomMap` (existing `spa_shift_roster` row always wins) + `planAlignDay` (rosterUpserts for un-rostered therapists + room moves for out-of-home appointments). `SpaBoard.js`: `AssignModal` now hard-blocks (Hebrew FAIL VISIBLE) assigning a therapist outside their home room unless an "חריג — שבץ בכל זאת" Override checkbox is ticked — Save button stays live per Disable-Don't-Hide. New `MoveGuestModal` — single `UPDATE spa_appointments SET room_id=…` (no RPC needed; unlike migration 177's two-row swap, one row never trips the exclusion constraint mid-statement), defaults target to the therapist's home room, therapist stays assigned unless staff explicitly opts into reassigning. New "🧭 יישור יום" toolbar button: seeds missing roster rows (additive INSERT only — never deletes/overwrites a row staff already set) then attempts a room move per out-of-home appointment, catching 23P01/exclusion_violation per-row into a FAIL VISIBLE blocked list with an inline «➡️ העבר אורח» retry — never touches EZGO line ids, never cancels. `SwapTherapistModal` demoted to "(חריג)" in title/tooltips, pointed at Move Guest as the default fix path. `npm run build` clean, 331/331 jest. Not deployed — awaiting Mike's `כן`/`תעלה`.

2026-07-13 | Automation retry-storm fix — cooldown/exhausted latch + claim-before-send (not yet deployed) | Stage 3 Shabbat morning re-sent every ~15m (same class as 2026-07-12 day-pass loop): a timeout/failed/blocked_by_meta attempt never stamps `guests.msg_*_sent`, so `whatsapp-cron` re-queued forever. **Phase B** (no migration): new `_shared/automationRetryGate.ts` — pure `evaluateRetryGate` wired into `checkEligibility` (the one choke point `whatsapp-cron` + `automation-queue` already share) → `cooldown` (30min) after 1 failure, `exhausted` after 4, scoped per (guest, stage_key) so it can never leak across triggers; batched `notification_log` reads in both callers mirror the existing suppression/7-day-read patterns (no N+1). Also gates the separate `stage_2_arrival` reconcile pass in `whatsapp-cron` (event_immediate stages skip the main due-loop by design, so needed its own check on the same map). ACC Live Queue: new `retryGate` field + `⏳ בהמתנה`/`🛑 מוצה`/`🔄 בתהליך שליחה` badges (Disable-Don't-Hide — Override still sends). `notifyAdminIfDispatchFailed` now also alerts on `timeout` (previously silently excluded — the exact gap that let this run unnoticed). **Phase C** (migration 195, partial unique index reusing the already-reserved `notification_log.status='processing'`): new `_shared/automationClaim.ts` — `claimDispatchAttempt`/`finalizeDispatchAttempt`, one row per attempt (INSERT then UPDATE, never a second insert), stale-claim reclaim after 5min, `force` bypass mirrors `checkPipelineDuplicate`'s convention. Wired into `whatsapp-send`'s generic BRANCH D pipeline path only this session (covers `pre_arrival_2d`, `mid_stay(+daypass)`, `checkout_fb(+daypass)`, `spa_warmup_daypass`, `survey_invite_daypass`, `night_before_daypass`) — the remaining special-cased fast paths (`night_before`, `morning_suite`/`morning_welcome`, `room_ready`, `stage_2_arrival`) are an explicit scoped follow-up, same helper, no new design. 61 new/updated Deno tests, `deno check` delta-clean (whatsapp-send: +1 error, same pre-existing loose-`guestId`-typing pattern already present twice in the file at the `checkPipelineDuplicate`/`logDuplicateBlocked` call sites — not a functional regression), `npm run build` clean.

2026-07-13 | Guest Experience Survey (Portal) + Spa warm-up automation (not yet deployed) | Migration 194: `guest_surveys` table (7-category + free-text, RLS+realtime, mirrors 117), `guests.msg_spa_warmup_sent`/`msg_survey_invite_sent`/`survey_completed_at`, 2 new `automation_stages` rows (`spa_warmup_daypass` — spa_time−75min via new `anchor_event='spa_time'` in `automationSchedule.ts`; `survey_invite_daypass` — 17:00 arrival day), `bot_scripts` seeds, widened `anchor_event`/`guest_feedback.source` CHECKs. New `guest-portal-survey` Edge Function (score validation, positive-gate→Google CTA, negative→`guest_feedback` mirror row). `GuestPortal.js`: reusable `SurveySection` (day-pass+spa cohort only, both views). `WhatsAppInbox.js`: day-pass+spa cohort split out of the default roster into its own "☀️ ספא יום" tab (mirrors the "departed" bucket pattern) so suite red-dots aren't diluted. `GuestFeedbackTabs.js`: new "📊 סקרים" sub-tab with deep-link `?tab=surveys`. `resort-digest-cron` + `resortDigestStats.ts`: optional survey stats section (additive, non-breaking). `npm run build` clean, 320/320 jest + 38/38 + 36/36 Deno tests pass, `deno check` delta-clean on all touched functions. Not deployed — awaiting Mike's `כן`/`תעלה`.

2026-07-13 | Smart Spa Board — therapist sticky-room + female preference (not yet deployed) | Migration 193: `spa_therapists.gender` (female/male/unknown, staff-set) + `spa_shift_roster` (advisory home-room per therapist/day, UNIQUE(date,therapist) + UNIQUE(date,room,therapist), RLS mirrors 176). `SpaBoard.js`: TherapistManagePanel gender select (immediate-save); new "🗓️ סידור משמרת" panel (full-day delete+insert replace, surfaces 23505 as Hebrew FAIL VISIBLE); AssignModal therapist picker sorts roster-home-match then female-match to top (all still selectable, Disable-Don't-Hide) + "רק מטפלת" toggle that read-merge-writes `guests.guest_profile.spa.therapist_pref` (reuses `spaActivitiesSyncEngine.js`'s merge pattern, never touches sibling spa.* keys); agenda + room-view cards show soft ⚠ for multi-room-today therapists and gender-preference mismatches — nothing blocks save. `npm run build` clean. Not deployed — awaiting Mike's `כן`/`תעלה`.

2026-07-13 | Whapi-first guest outbound — close remaining Meta holes (not yet deployed) | Gap-table audit found autonomous cron/pipeline paths already Whapi-clean; 3 real gaps + 1 fallthrough edge case fixed: `broadcast` and `payment_and_workshops` in `whatsapp-send` and `stage_2_pay` in `whatsapp-webhook` now default Whapi-eligible guests to the Suites device (reused `resolveMetaTemplateBodyText`/`guardPaymentLink`/`sendWhapiText`) instead of `sendViaTemplate`/`sendCtaUrlButton`; `morning_suite`/`morning_welcome` no longer skips its own Whapi FAIL VISIBLE guard when a stage has no session_message_script_key configured. New Phase 3 hard-fail: manual `force_channel=meta_template` on a Whapi-eligible guest is refused (Hebrew FAIL VISIBLE) unless `ALLOW_META_GUEST_TEMPLATES=true` — ACC's "🔵 Meta Template" button stays clickable (Disable-Don't-Hide) as the documented device-down fallback. ACC Live Queue/Override also fixed to read `effectiveWhapiGuest` (suite OR day-pass) instead of suite-only `effectiveSuite`. 15 new/updated Deno tests, 152/152 pass, `deno check` delta-clean on both functions, `npm run build` clean.

2026-07-12 | Day-pass automation → Whapi (stop Meta template cron loop) | Live: guest 3540 «ליאור ורותי חזיזה» (`day_guest` Premium Day) failed every ~15m on `night_before_daypass` → `dream_checkin_reminder_v2` (#132000 then #131008 URL button); flag never stamped. Root: Whapi gate was suite-only. Fix: `shouldRouteGuestOutboundViaWhapiSuites` includes day-pass; morning day-pass Meta fast-path skipped when Whapi on. 11 Deno tests. Deploy: whatsapp-send + consumers of guestWhapiRouting.

2026-07-12 | Agent session pipeline Research→Diagnostic→Execute→QA | Playbook §6.0 + §8 full copy-paste prompts; thin alwaysApply rule `.cursor/rules/XOS-Session-Pipeline.mdc`. Agent auto-routes by task type; Mike overrides with `רק research` / `רק diagnostic` / `תריץ QA`.

2026-07-12 | Fix dream_room_ready1 Meta #132000 param mismatch on room_ready | `room_ready` sent 2 body vars (name+room) but Meta-approved `dream_room_ready1` expects 1 — caused admin alert for ליאור ורותי חזיזה. New `_shared/metaTemplateVars.ts`: resolve live param count (Meta API → production fallback → snapshot), trim before `sendViaTemplate`. 6 Deno tests. Deployed `whatsapp-send` from desktop (cloud lacked token). Suite path remains Whapi when flag on.

2026-07-12 | Doc 2 — international WhatsApp phones sync + send | `normalizeWhatsAppPhone` in `ezgoParser.js`: keeps IL domestic path; accepts `+` / `00` / bare 11–15-digit country-coded numbers into `guests.phone` E.164 so Meta/Whapi can deliver. Grid edit (`normalizeGuestPhoneEdit`) + `extractGuestDetails` columns use it. Bare national digits without CC still rejected (FAIL VISIBLE). 33 ezgoParser + related import tests pass. Frontend-only.

2026-07-12 | Smart Spa Board — CSV בע"מ repair + Golden Profile name match + נקה הכל | Real `תפעול ספא 13.7.csv`: SheetJS dropped ~half rows on unescaped `בע"מ` — `repairEzgoCsvText` before parse (ZERO DATA LOSS); normalize Excel-serial/locale dates + numeric phones; `pickBestGuestMatch` uses Hebrew paren person (`limor (לימור סולומון)` → לימור סולומון) and skips org labels; day_guest auto-create uses Hebrew display name; write-through `spa_date`/`spa_time`/`guest_profile.spa` unchanged. SpaBoard: bulk «נקה הכל» for `spa_import_unmatched`. 77 spa unit tests (incl. live 13.7 file). Deployed frontend `cacc05d` (migration 191 already on remote).


2026-07-12 | Whapi «בקשות אורחים» cards → Hebrew + deep links | `_shared/guestAlertWhapiNotify.ts`: English headlines/CTA + HE→EN field-ops translation removed (wrong for Hebrew reception group). Card now Hebrew (aligned with RequestsBoard TYPE_META), keeps original message, adds tappable `?page=wa_inbox&phone=` + `?page=requests_board` (same shape as `staffDeepLink.js`). Link preview enabled. SLA guest-alert ping in `sla-escalation-cron` mirrored to Hebrew + same URLs. 5 Deno tests.

2026-07-12 | Smart Spa Board — English Ezgo CSV import + couple-room dual appointments | Real "פעילות ספא" machine CSV (`tmStart`/`sActivityDesc`/`iAddsLineId`…) was incompatible with the Hebrew-header parser — every row would have failed. Parser now accepts both formats; English rows canonicalize to Hebrew keys; `iLineStatus=0` counted as `skipped_cancelled` (not synced); `ezgo_line_id` = `iAddsLineId_sRowNum` (iAddsLineId alone is shared by couple halves). Migration 191: aliases for `סוויטת אבניו 2/3/4` + `טרקלין -חדר זוגי`; couple rooms allow 2 overlapping appointments (GiST single-only + capacity-2 trigger); `room_type` denormalized on `spa_appointments`. Natural-key re-import includes `therapist_id`. Import uses file `dtDate` when unanimous. Validated against Mike's CSV: 211 active / 53 cancelled / 0 unmapped rooms / 63 couple slots. 66 spa unit tests pass. Not deployed — needs `db push` 191 + frontend push.

2026-07-12 | Sprint C DOCS2 — guest profiles without a phone no longer silently dropped on import | Zero Data Loss gap: a named/ordered Doc 2 (suite CSV) row with no phone was previously skipped entirely at 3 layers — `ArrivalImportPanel._getSyncProfileIndices` (frontend gate), `sync_suite_arrivals` RPC (`IF v_phone IS NULL THEN SKIP`), and 3 grid columns hardcoded `guestPhone editable:false`. **Phase 1 correction found mid-Phase-2:** the RPC body I confirmed in Phase 1 (migration 147) was stale — `154_guest_automation_scope.sql` had already superseded it (filename doesn't contain "sync_suite_arrivals", missed by a filename-only grep); re-derived the plan against 154's actual body before writing SQL. Migration 190 (`CREATE OR REPLACE`): skip guard relaxed to phone-OR-name-OR-order; no-phone guest forced `automation_scope='muted'` server-side (defense in depth — courtesy_only would still fire a Stage 4 WhatsApp send with no phone to send to); Tier-1 + guest_index lookups made NULL-safe (`IS NOT DISTINCT FROM`) so a repeat import updates the same row instead of duplicating; multi-room-per-order tier gets a name-scoped no-phone branch (not blind NULL-match) to avoid merging two different unnamed occupants under one order; full-mode UPDATE changed `phone = v_phone` → `COALESCE(v_phone, phone)` so a blank-phone re-import can never null out a phone staff already entered. **Caught before it shipped:** `public.bookings.phone` is `NOT NULL` (`031_treatment_time.sql`) — without guarding that INSERT behind `v_phone IS NOT NULL`, importing one no-phone guest would have thrown and rolled back the entire sync batch (all guests in that file), not just the one row. Frontend: 3 grid columns (`DETAILED_GRID_COLS`/`SUITES_GRID_COLS`/`SUITE_ASSIGNMENT_GRID_COLS`) now `guestPhone editable:true`; new `normalizeGuestPhoneEdit()` (`ezgoParser.js`, reuses existing `normalizeILMobile`/`isDummyPhone`) validates/normalizes a hand-typed phone in `handleFilteredGridChange`, rejecting invalid input via toast without committing it; grid's existing "" (never null) no-value convention preserved throughout so the `??` fallback chains stay correct. Found and fixed 2 silent-drop gaps beyond the original ask while tracing the write path: `bookingGuestKey()` (`guestImportIntelligence.js`) required a phone to return a non-null key, so a no-phone guest's spa_time/meal_time/room/automation_scope patch (the separate JS-side `indicesByGuestKey` UPDATE loop, distinct from the RPC's own profile INSERT) was silently skipped — loosened to key by order+arrival with phone as an optional third segment; `_scopeGuestRowQuery` gained an order+date+`phone IS NULL` scoping branch so that patch can actually reach a no-phone row. `_getSyncProfileIndices` returns a new `createdWithoutPhone` array (distinct from `skippedNoPhone`, which now only means truly-blank rows) surfaced as a FAIL VISIBLE 📵 toast pre- and post-sync ("ייווצר בלי טלפון — מושתק"). 15 new/updated unit tests (`ezgoParser.test.js`, `ArrivalImportPanel.test.js`, `guestImportIntelligence.test.js`) + 5-scenario SQL test (`supabase/tests/sync_suite_arrivals_null_phone_test.sql`, BEGIN/ROLLBACK pattern) covering: no-phone create, re-sync dedup, later phone-enrichment updates the same row, Zero-Data-Loss phone preservation on blank re-import, and the truly-blank-row skip. 288/288 Jest tests pass, `npm run build` clean (+548B). Not yet deployed — awaiting Mike's `תעלה` (migration 190 needs `db push`; frontend needs `git push`).

2026-07-12 | Decouple Whapi suite dispatch from Meta template approval (is_active) | Mike's ask after the Stage 1 CTA fix: "make sure the whole suite journey is fully synced to the Suites device — Whapi doesn't need a Facebook template." Traced it: `automation_stages.is_active` has always meant "is this stage's Meta template approved" (per the documented night_before/morning_suite "🔴 Disabled — pending Meta" blocker), but `whatsapp-cron`'s scan, `automation-queue`'s Live Queue projection, and `whatsapp-send`'s own dispatch gate all did a blanket `.eq("is_active", true)` — pausing a stage for Meta-approval reasons silently starved Whapi-eligible suite guests too, who don't need that approval at all (same failure class as the Stage 1 `whapi_session_unavailable` incident, one stage over). Fix: new `isStageEffectivelyActive(stage, guest)` (`_shared/guestWhapiRouting.ts`) — a paused stage still fires for Whapi-eligible suite guests, Meta-bound guests completely unaffected (identical behavior to before). Single shared gate wired into all three call sites instead of three copies that could drift. `whatsapp-send`'s stageQuery also now selects `is_active` and applies the check post-guest-load (previously filtered in SQL pre-guest-load, which is why `morning_suite`/`morning_welcome`'s session block silently no-opped on a null stageRow — `night_before` happened to have hardcoded literal fallback keys that made it resilient regardless). 9 new Deno tests (`guestWhapiRouting.test.ts`), 43/43 total pass, `deno check` delta-clean on all 3 touched functions (confirmed via `git stash` baseline). No migration — pure eligibility logic over existing columns. Deployed: `whatsapp-send`, `whatsapp-cron`, `automation-queue`, `whapi-webhook`, `whatsapp-webhook`, `guest-portal-spa-request` (all consume the changed shared module), `main` (`700fbda`). **Flagged to Mike before deploy, approved:** if `night_before`/`morning_suite` are currently `is_active=false` in prod, Whapi-eligible suite guests will now start receiving them automatically — could not verify live value from this sandbox (RLS).

2026-07-12 | Stage 1 Whapi arrival-confirm CTA safety net + ACC timeout≠failed | Design Mode: presented 3 approaches (CTA text hotfix / Whapi interactive buttons / hybrid Meta-Stage1); researched Whapi's `POST /messages/interactive` docs first — vendor's own docs flag button-send as "not stable" + zero existing button-parse infra in `whapi-webhook` + unconfirmed compat with the session-paired (non-Business-API) Suites device, so Mike picked approach 1. `_shared/arrivalConfirmation.ts`: new `ensureArrivalConfirmationCta()` — Whapi Stage 1 has no interactive buttons (Meta's template does), so if `bot_scripts.pre_arrival_2d` is ever edited in ACC and the "כן, מגיעים!" phrase is dropped, the canonical CTA is appended defensively (no-op when already present — confirmed the live migration-100 seed text already carries it, matching `CONFIRMATION_RE`). Wired into `whatsapp-send`'s pipeline session-body branch, scoped to `trigger==="pre_arrival_2d" && (forceWhapiSession || useWhapiForPipeline)` only — Meta path (template+buttons) and every other stage untouched. Also fixed a real gap found during Phase 1 mapping: `AutomationControlCenter.js` `handleBulkDispatch`'s summary modal collapsed `status:"timeout"` into the same "נכשלו" bucket as real failures (the per-row Live Queue badge already distinguished them correctly) — added a separate `timeout` bucket/row ("⏳ לא ודאי אם הגיעו") + detail list. 7 new Deno tests (`arrivalConfirmation.test.ts`) — CTA no-op/append, appended-CTA round-trips through `isArrivalConfirmationMessage`, decline-never-confirms regression. `deno check` on `whatsapp-send/index.ts`: 37 pre-existing errors, confirmed identical via `git stash` baseline (esm.sh `SupabaseClient` generic-mismatch cascade, not new). `npm run build` clean (+102B). Migration 189 (root-cause DB fix, `whapi_session_unavailable`) already applied on remote per `supabase migration list` — this round is purely the CTA-durability safety net on top. Deployed: `whatsapp-send`, `whapi-webhook`, `whatsapp-webhook`, `whatsapp-cron` (all consume the changed shared module), frontend `main` (`d67ecd6`).

2026-07-12 | Inbox composer emoji picker (Sprint B, PRIMARY only) | Staff desktop has no native phone emoji keyboard — added a 😊 button next to the existing ⚡ quick-replies button in `WhatsAppInbox.js`, reusing the exact same popup pattern (bottom-sheet on mobile, absolute panel on desktop). Curated 40-emoji set (`src/utils/emojiPickerData.js`, no new dependency); `insertEmojiAtCursor` splices into the reply textarea at the current caret position via `replyRef`, keeps the panel open for multiple picks. SECONDARY (bubble long-press → WhatsApp reaction via Meta/Whapi API) was scoped out after Phase-1 research found zero existing reaction-send infra (`_shared/whapiSend.ts` has no PUT reaction, no Meta reaction POST) — Mike confirmed deferring it to its own sprint rather than bundling a full second feature in under the "small" bar. Frontend-only, no Edge Function/migration touched. `npm run build` clean (+378B). Not independently click-tested by the agent (no login creds in this sandbox, same known blocker as Voucher/Inventory) — Mike to verify in his own `npm start` session at 1280px + 390px.

2026-07-12 | Stage 1 Whapi bulk — wire null session_message_script_key + PIPELINE_SESSION_SCRIPT fallback | Live: 14/14 `whapi_session_unavailable` — prod `automation_stages.pre_arrival_2d.session_message_script_key` was NULL (bot_scripts row intact). Migration 189 re-wires the column; `whatsapp-send` adds hardcoded `PIPELINE_SESSION_SCRIPT` fallback so a cleared ACC field cannot block Suites-device session sends again.

2026-07-12 | Stage 1 catch-up — late import no longer deadlocks on «ממתין לאישור הגעה» | Root: `pre_arrival_2d` after T-2 returned `date_passed`, which `automation-queue` permanently hid from Live Queue, while Stage 2 stayed on `awaiting_confirmation` — guest never received the confirm ask. Fix: past window + `arrival_date >= today` → `missed_window` (`dueNow=false`, cron does not auto-send); queue surfaces it; ACC badge «⚠ פספס מועד», suite «שלח»/Override default to `whapi_session`, channel chip «מכשיר סוויטות». 5 new Deno tests (27/27). Deploy: `automation-queue` + frontend.

2026-07-12 | Sprint A — suite guests route via Whapi Suites device, from_me DMs mirrored into Inbox | `guest-portal-spa-request` was Meta-first with a raw, unlogged `sendWhapiText` fallback (Zero Data Loss gap — a successful fallback send left no Inbox trace); now routes through `whatsapp-send trigger:"inbox_reply"` with `inbox_channel: shouldRouteGuestOutboundViaWhapiSuites(guest) ? "whapi" : "meta"` (reused `_shared/guestWhapiRouting.ts`, already tested — no new routing logic), which also inherits BRANCH C's existing confirmed-fail→Meta / timeout→hard-stop contract for free. Audited all 13 call sites touching `sendWhapiText`/`inbox_reply`; only this one DMs a guest directly — every other portal/alert function (ops-request, upsell, order, task-action, inventory-portal-submit, notify-manual-task, sla-escalation-cron) is staff/group-only. `whapi-webhook`: new `mirrorWhapiOutboundDm()` — messages sent from the physical Suites phone (`from_me` on a 1:1 chat) now log into `whatsapp_conversations` (`inbox_channel='whapi'`, `[WHAPI]`-tagged) instead of being silently ignored; peer phone resolved from `chat_id` (not `from`, which reflects the device's own number on outbound events); dedupe reuses the existing global unique index on `wa_message_id` (same insert-then-catch-23505 pattern as `claimWhapiGuestInbound`) so an Inbox-originated send that also webhooks back as `from_me` doesn't double-log; empty-text voice/media gets a Hebrew placeholder (Zero Data Loss) instead of vanishing; no bot/LLM runs on `from_me`. `from_me` 1:1 reactions and non-`from_me` 1:1 reactions are still dropped (`not_a_group_reaction`) — deliberately out of scope this round, flagged for Mike as an open follow-up. `deno test` 4/4 pass (`guestWhapiRouting.test.ts` — no new test file needed, the only new logic is a trivial fallback-text ternary + reuse of existing helpers). `deno check` shows errors on both touched files but confirmed via `git show HEAD` that whapi-webhook already had 50 pre-existing errors (same esm.sh `SupabaseClient` generic-mismatch cascade as prior sessions) before this diff — not new logic bugs. Deployed: `guest-portal-spa-request`, `whapi-webhook`.

2026-07-12 | Guest bot — fix checkout/luggage hallucinating a "towels & robe" ack | Live incident: guest wrote "לצערי אנחנו צריכים לעשות צאק אאוט ומבקשים שמישהו יגיע לחדר 9 לקחת את המזוודה לקבלה" (checkout + please send someone for our luggage); bot replied with the exact `_buildToolOnlyReply` template but an invented summary — "מגבת וחלוק לבריכה" (towels & robe), bled in from an earlier, already-resolved topic in the same thread. Root causes fixed: (1) `INFORMATIONAL_GUEST_QUERY_PATTERN`'s bare `checkout|צ'ק-אאוט` alternative flagged this as pure FAQ regardless of an attached action ask — `isInformationalGuestQuery` now checks the new `isDepartureAssistRequest` classifier first; (2) `filterToolLoggedRequest` only ever gated whether a task got *created*, never whether the model's already-baked-in ack text was *true* — a suppressed/ungrounded tool call could still ship a false "העברתי את הבקשה (X)" to the guest. Added to `_shared/automationSchedule.ts`: `isDepartureAssistRequest`/`buildDepartureAssistSummary`/`buildDepartureAssistReply` (new Tier-0 intercept, checkout+luggage → pending_approval Ops Board task via the existing `createGuestOpsTask`, deterministic no-approval-implied reply, wired into both `whatsapp-webhook` pre/post-burst chains and `whapi-webhook`'s `handleGuestDirectMessage`, same on-property eligibility gate as the existing amenity intercept); `isRequestSummaryGrounded` (Hebrew-aware substring/stem overlap check — rejects a `log_guest_request` tool call whose summary shares no content word with the current message, independent of the allowlist check). `whatsapp-webhook` also now tracks the raw (pre-filter) tool call and rewrites `reply` to the canonical `GUEST_STAFF_HANDOFF_SENTENCE` whenever a suppressed/ungrounded call left the tool-only-ack template in place. 22/22 `deno test` pass in `automationSchedule.test.ts` (10 new, including the exact incident string, the FAQ/late-checkout exclusions, both grounding directions, and a burst-isolation case). `deno check` clean on all 3 touched files (pre-existing, unrelated `SupabaseClient` generic-mismatch noise confirmed via `git stash` baseline, +1 from the one new `createGuestOpsTask` call site added). Deployed 2026-07-12 (autonomous dual-bot health audit, see next entry).

2026-07-12 | Autonomous dual-bot health audit — deployed pending departure-assist fix + `.maybeSingle()` correction | Mike AFK, pre-authorized audit→fix→deploy run. Found the 2026-07-12 departure-assist/grounding fix above sitting uncommitted and fully tested (22/22 `deno test`) — verified `deno check` delta against a `git stash` baseline was only the expected new-callsite noise, then shipped it. Separately found `whapi-webhook`'s group-task insert using `.select().single()` — CLAUDE.md hard rule is `.maybeSingle()` only; a select-back RLS gap after a successful `INSERT` would have surfaced as `PGRST116` instead of FAIL VISIBLE. Fixed to `.maybeSingle()` + explicit null guard (`task_insert_no_row`), no behavior change on the happy path. Fast parity pass Meta vs Whapi (sanitize firewall, cancelled-guest outbound guard, stay-change shield, claim-mute) found no other gaps — sanitize runs inside `generateGuestChatReply` for Whapi vs an explicit `sanitizeReply()` call for Meta, both wired, confirmed by call-site trace not just grep count. Deployed: `whatsapp-webhook`, `whapi-webhook`; pushed `main` (`efbdef6`). **Ask Mike:** Whapi guest DM has no burst/message-coalescing (Meta's `coalesceBurstIfLeader`) — looks like a deliberate scope difference (Whapi DM's shield set is a documented curated subset), left alone rather than building it unrequested under a time-boxed run; flag if that's actually a gap.

2026-07-11 | Executive Assistant — GM tool ceiling raised (task close/approve, room-ready, Requests Board, room status, on-demand digest) + per-executive learned rules | Audit found Eliad's assistant could report but not close the loop: no task close/approve, no guest-status change, no Requests Board / room-status reads, no on-demand digest, and `xos_ai_rules` (module=executive) was one flat pool shared between Eliad and Mike. Added 5 tools to `_shared/executiveAssistant.ts`, all lean reuse of existing tables/helpers, none inventing new write paths: `update_task_status` (resolves by `task_id` or `room`+`keyword`; ambiguous match → candidate list, never a guess; `open` on a `pending_approval` task calls `notify-manual-task` — the same guarded Approve & Dispatch path `OperationsBoard.js` uses, so a retry can't double-send the group card; `done`/`in_progress`/`rejected` mirror `OperationsBoard`'s own transitions and guards) — `query_open_tasks` now also returns each task's `task_id` so a chained "find then close" doesn't need a second lookup; `list_guest_alerts` (Requests Board / `guest_alerts` read, unresolved-only by default); `get_room_status` (`room_status` table — housekeeping state, deliberately separate from `guests.status` per CLAUDE.md §3); `get_ops_digest_now` (reuses `resortDigestStats.ts` + the exact query shape `resort-digest-cron` uses — **read-only, never writes `resort_digest_log`**, so an on-demand pull can't collide with the scheduled push's idempotency); `set_guest_status` (scoped to `room_ready` only, suite guests only, only from `pending`/`expected` — calls the same `whatsapp-send` `trigger:"room_ready"` pipeline `GuestsPage.js`'s button uses, so the arrival-day gate and duplicate-blocked idempotency aren't duplicated; status flip commits even if the guest notification send fails, reported back as `guest_notified:false` — FAIL VISIBLE, never a silent "done"). Deliberately **not** built: check-in/cancel/date-change via voice — no tool exists for these, and the persona now explicitly refuses and points to the admin screen instead of improvising through another tool. Tool-call loop: rounds 1-2 both carry tools (previously only round 1 did), so a genuine chain (e.g. "close the task for room 5") can actually finish instead of round 2 being forced to compose text around an unfinished action; round 3 never carries tools, forcing a final reply and capping worst-case latency — single-tool turns (the majority) still finish in 1-2 rounds exactly as before. Migration 188 adds `xos_ai_rules.owner_phone` (nullable — every pre-188 rule keeps applying to both executives, Graceful Fallback); `fetchExecutiveRules` now filters `owner_phone IS NULL OR = caller`, `learn_executive_rule` writes the caller's own phone, so Mike's QA-directed rules stop bleeding into Eliad's conversational persona. Digest-content rules stay intentionally unscoped (`get_ops_digest_now` + `resort-digest-cron` both read `module='executive'` without the filter — one digest, one recipient, regardless of who taught the rule). Persona rewritten with an explicit capability map (can do directly / must ask one clarifying question / always refuses) instead of only prose rules. 21 new Deno tests (33/33 total in `executiveAssistant.test.ts`) covering every new gate: ambiguous-task candidates, approve-dispatch via mocked `notify-manual-task` fetch, day-pass/invalid-transition blocks on `set_guest_status`, FAIL VISIBLE on unconfigured/failed guest notification, and owner_phone dedupe/scoping (shared rule dedupes for both, private rule dedupes only for its owner, another executive's private rule does *not* dedupe). `deno check` clean on both files. **Not deployed** — migration 188 not pushed, `whapi-webhook` not redeployed — awaiting Mike's `כן`/`תעלה`.

2026-07-11 | ETA → Requests Board `arrival_eta` + Eliad digest as personal assistant | ETA capture still writes `guests.arrival_time` + guest reply; now also inserts `guest_alerts` (`alert_type=arrival_eta`, label «🕐 שעת הגעה», filter on RequestsBoard; board-only — no red-dot/ops). Profile chip «🕐 שעת הגעה». Resort digest reframed as Eliad's personal assistant Hebrew voice + learn footer; digest-relevant `xos_ai_rules` echoed on next report; persona + migration 187. Deploy: db 187, `whapi-webhook`, `whatsapp-webhook`, `resort-digest-cron`, frontend.

2026-07-11 | ETA Record-Only — `מתכננות להגיע ב-12:00` miss | Live Suites chat: guest «מתכננות להגיע ב-12:00» got exact «רשמתי לפניי» copy but Tier-0 never fired — regex had `מתכננ(?:ת|ים)?` only (missed feminine plural + `להגיע ב-HH:MM`), so LLM echoed the phrase without writing `guests.arrival_time`. Fix: `ARRIVAL_TIME_UPDATE_RE` → `מתכנ(?:ן|נת|נים|נות)` + `להגיע ב[-–]?\d`. Morning board already exists: GuestsPage «לוח זמני הגעה — היום». Deploy: `whapi-webhook` + `whatsapp-webhook`.

2026-07-11 | Guest bot — prompt-leak firewall + ETA «מתכננת להגיע לקראת» | Live Whapi leak: model quoted system rules (`"…רב-מגדרית". - Yes. * "לעולם אל תציג"`) instead of answering. Root: Whapi `guestBotLlm` lacked Meta's sanitize firewall; Gemini priming continued as a rules quiz; ETA Tier-0 missed `מתכננת להגיע`/`לקראת` so the message hit the LLM. Fix: shared `guestBotSanitize.ts` (COT + prompt-regurgitation → empty → handoff), stronger priming/anti-quote, ETA regex+hourWord. 8 Deno tests. Deploy: `whapi-webhook` + `whatsapp-webhook`.

2026-07-11 | Executive Assistant — fix Inbox-only replies on voice (delivery architecture) | Voice notes to Mike/Eliad's personal assistant showed a reply in XOS Inbox but nothing on WhatsApp. Root causes: (1) `handleExecutiveVoiceMessage` always logged outbound even when `sendWhapiText` failed (silent catch → false `executive_reply_sent`); (2) slow voice+LLM paths invite Whapi webhook retries — second attempt hit `claimed:false` and skipped the executive handler entirely. Fix: new `deliverExecutiveDmReply` (prefer inbound `chat_id`, retry, phone fallback, FAIL VISIBLE `⚠ שליחה נכשלה` row, success only with real send); `executiveAlreadyRepliedSuccessfully` idempotency; unclaimed-retry re-enters executive path when no successful outbound yet; Gemini timeout 8s→15s. 13/13 Deno tests. Deploy: `whapi-webhook`.

 Inbox `syncInboxContactWithGuestMap` + `reconcileMessageWithGuestMap` always copied `guests.claimed_by` (Dream Bot) onto Whapi contacts/rows, so the ✓ badge lied and/or a click released the wrong state; claim could also land on a new stub while `whapi-webhook` muted-checked the real guest → bot kept replying. Fix: per-channel claim resolve via `guest_channel_claims` map (+ ready flag), phone lookup before Whapi stub create, stamp `guest_id` on local rows, LLM `sendGuestDmReply` passes `staffMuted`. Tests in `guestTiming.test.js`.

2026-07-11 | Whapi Inbox timeout — 45s + Hebrew uncertain-delivery copy | Staff saw red `whapi_timeout: timeout_no_response…25s` on Suites-device replies even when the guest may already have received the message (no Meta fallback on timeout — by design, avoids duplicates). `_shared/whapiSend.ts`: `WHAPI_FETCH_TIMEOUT_MS` 25s→45s for text+image. Inbox: `inboxSendErrors.js` maps `status:timeout` / `whapi_timeout` to Hebrew «לא ודאי אם ההודעה הגיעה — בדקו בוואטסאפ…למנוע כפילות» (free-text, bulk, room-ready). 4 unit tests. **Deployed** — `whatsapp-send` + 11 Whapi consumers + frontend push (`39730ef`).

2026-07-11 | Smart Spa Board — Activities auto-create day_guest + hour agenda + dual entry | `spaActivitiesSyncEngine.js` now auto-creates a `day_guest` profile (room="Premium Day 1", status='expected') when an Activities row has no phone match but a plausible Israeli phone (`972\d{8,9}`, guards against a misaligned/garbage טלפון cell reaching an INSERT into the Golden Profile — caught by external Plan-agent review) + a real name; couple/group cells on one phone get one profile + a `couple_shared_phone` flag (staged to `spa_import_unmatched` as `suspicious_shared_phone`, never a guessed second guest). Added `not_in_file` (informational count of existing appointments this date absent from the current import — never cancels) and `meal_time_set` (explicit meal mention in note/extras only, never board-basis guessing, never overwrites an existing `meal_time`) to the summary. 9 new orchestrator tests (mocked Supabase) + 6 `extractSpaMealTime` unit tests, 54/54 pass. UI: `ActivitiesImportZone` extracted to `src/components/spa/ActivitiesImportZone.js` so `SpaBoard.js` and `DataSyncPage.js` mount the identical component (one engine, two entry points); SpaBoard default view is now an hourly agenda (chronological list) with room-columns as a secondary "לפי חדרים" tab; import toast surfaces all new summary counts in both mount points. `npm run build` clean, full suite 269/269 pass. Not deployed — awaiting Mike's approval.

2026-07-11 | Ops Whapi cards — drop Assigned + tag bot vs staff | Removed misleading `👤 Assigned` line (department→first-profile lookup labeled Eliad/CEO as assignee). `taskCard.ts`: `guest_request` → `[BOT]`, `inbox_routed` → `[GUEST WA]`, `manual` → `[MANUAL TASK]`. OperationsBoard: `guest_request` label «🤖 בוט · בקשת אורח» + `inbox_routed` «📥 ניתוב מתיבה». Touch: `notify-manual-task`, `whapi-webhook`, `OperationsBoard.js`.

2026-07-11 | Sync Claude gemini-1.5-flash fix onto main + Hybrid handoff deploy | Cherry-picked Claude Code commit (`process-knowledge` fallback loop + drop dead `gemini-1.5-flash` from `guestBotModelRoute` / `suggest-import-mapping` / BotSettings) onto `main` and pushed — functions redeployed from `main` so repo and edge match. Hybrid unanswered-guest escalation also shipped: migration 186 + `sla-escalation-cron` (pending_approval auto-approve 7m + soft handoff 20m). `SLA_ESCALATION_ENABLED` already set in secrets.

 Staff reported the housekeeping group ("צ'ק אין צ'ק אאוט") replying "כבר מסומן כצ'ק-אין" when they typed e.g. "26 צ'ק אין" — root cause: a 15:00 Israel sweep (three independent writers: `whatsapp-cron`, `whatsapp-webhook`, `GuestsPage.js`) auto-promoted every arriving-today guest to `checked_in` before staff could report it manually, so `housekeepingCheckInSignal.ts`'s `guest.status === "checked_in"` check always won the race and returned the false-positive `already_checked_in` ack. On Shabbat this was flatly wrong too — guests get rooms after 15:00 (18:00 policy), so the sweep marked them in before they'd even arrived. Removed all three write paths (the `whatsapp-cron` `isPastAutoCheckinGateway` update block, `whatsapp-webhook`'s `applyAutoCheckinPromotion()` + call site, `GuestsPage.js`'s `applyReceptionMatrixSync` auto-promote branch) and neutralized the shared `shouldAutoPromoteToCheckedIn` (now hardcoded `false`) in both `automationSchedule.ts` and its frontend mirror `guestCheckinMatrix.js`, so `resolveEffectiveGuestStatus` no longer virtually flips a guest to `checked_in` in the UI/routing layer either — auto checkout at 11:00 on departure day is untouched. `AUTO_CHECKIN_LOCAL_HOUR`/`isPastAutoCheckinGateway`/`isCheckinBeforeTodayAutoGateway` were kept: `checkEligibility`'s night_before Friday-bundle rule still needs the 15:00 instant to distinguish a genuine early/manual check-in from a stray one, which is untouched date math, not a write. The housekeeping WA group writers (`housekeepingCheckInSignal.ts`, `housekeepingReadySignal.ts`, `housekeepingWaParse.ts`) and manual GuestsPage/RoomBoard check-in (`performSuiteCheckIn`) were verified unchanged. Added 4 new tests (`automationSchedule.test.ts` ×2 Deno, `guestCheckinMatrix.test.js` ×2 Jest) asserting the promotion function now always returns false and that effective status no longer auto-flips past the old gateway; all 7 pre-existing Friday-bundle tests plus the 25 housekeeping-parse tests still pass unchanged. `npm run build` has one pre-existing unrelated ESLint failure in `GuestJourneyTimeline.js` (confirmed via `git stash` to predate this change, not touched here). **Not deployed** — local-only per instructions, awaiting explicit approval before `functions deploy` (`whatsapp-cron`, `whatsapp-webhook`, `whapi-webhook` for the shared `_shared/automationSchedule.ts` change) / frontend build+push.

2026-07-11 | Resort Ops Digest — smart-analytics pass (headline, SLA compliance, capped lists) | Follow-up to the same-day Resort Ops Digest ship (previous entry). Mike asked to make the digest genuinely impressive — "statistics and smart management" — not just re-verify what was built. Added, all from data already in the schema (no new capture): (1) `composeExecutiveHeadline` — a one-glance ✅/⚠️ line pulling from room-ready gaps, SLA breaches, and anomalies together, so a CEO knows if the period needs attention before reading anything else; (2) `computeSlaCompliance` — the digest never used `tasks.sla_deadline` before despite it already existing; now tracks within-SLA/breached/breached-and-still-open + a compliance %, FAIL VISIBLE (a "done" task with a missing/late `resolved_at` counts as breached, never silently compliant; rejected tasks excluded from the denominator entirely — no valid resolution to measure); (3) percentages alongside raw counts (room-ready on-time rate, avg late minutes) since a CEO reads "87%" faster than a list. **Live testing at real production volume then caught a much bigger problem than any of that:** a weekly digest with 152 arrivals produced a 150+ line wall of text (every single unmarked room, every single suite) — the opposite of scannable, would have looked broken rather than smart. Added `formatCappedList` — worst-first, capped to 5 items per section with a "+N more" trailer (late rooms sorted by delay-minutes descending so the worst offender leads, requests-by-suite/anomalies were already sorted by severity). Verified live against real data at increasing scale: weekly (152 arrivals, 36 suites) confirmed the wall-of-text problem before the fix; monthly (35 suites, 0 arrivals — `checkin_time` isn't populated that far back, correctly reported as 0 rather than guessed) confirmed the fix produces a compact, genuinely scannable message after it. 35/35 `deno test` pass (10 new: SLA compliance edge cases, headline composition, capped-list behavior including a synthetic 10-item worst-first stress test). **Caveat for Mike:** Eliad already received 3 real test messages today during verification — the first two (daily 2026-07-10, weekly 2026-07-04–07-10) predate this pass, so the weekly one was still the uncapped wall of text; only the monthly one (2026-06) reflects the final polished version. Deployed (`functions deploy resort-digest-cron` ×2, once per fix).

2026-07-11 | Resort Ops Digest — daily/weekly/monthly Hebrew summary to Eliad via Whapi | New feature, mirrors the `orit_agent_digest_log`/`manager-morning-digest` pattern end-to-end. `_shared/resortDigestStats.ts` — pure, zero-I/O aggregation functions (`computeArrivals` bucketed by Israel-local checkin hour with a continuous 22:00–04:59 "late night" band spanning midnight; `computeRoomReadyTiming` comparing `room_ready_at` vs `checkin_time` into on_time/late/**unknown** — FAIL VISIBLE, never silently folded into on_time; `computeRequestsBySuite` grouped by `tasks.room_number` with resolved/open/rejected + per-`sla_category` tallies; `computeAnomalies` at a confirmed `ANOMALY_SAME_TYPE_THRESHOLD = 3`; `resolveDigestRange` always resolving a period that has fully ended — daily=yesterday, weekly=7 days ending yesterday, monthly=previous calendar month, reusing `automationSchedule.ts`'s existing `israelYmd`/`ISRAEL_UTC_OFFSET_HOURS`) — 21 `deno test` cases, one of which caught a real bucketing bug (00:30 arrivals were misclassified as "before 15:00" instead of the late-night band) before it shipped. Migration 184 adds `guests.room_ready_at` (TIMESTAMPTZ) + `resort_digest_log` (period/period_date/body_sent/wa_message_id, `UNIQUE(period, period_date)` idempotency). New `resort-digest-cron` function (`?period=daily|weekly|monthly`) fetches guests/tasks in range, composes the Hebrew body, sends via `sendWhapiText` to a new `executiveIdentity.ts` export `CEO_PHONE_DIGITS` (Eliad specifically, not Mike's QA number), logs to `resort_digest_log` + `whatsapp_conversations` (`[WHAPI]` tag, `guest_id: null`). **Live manual invoke caught a real gap the migration comment had only promised, not delivered:** `room_ready_at` was never actually written anywhere — fixed 4 call sites (`suiteRoomReady.ts`'s `syncGuestRoomReadyAggregate`, `whatsapp-send`'s no-`suite_rooms`-row fallback path, and `whatsapp-cron`'s two auto-checkout resets, which now also null it back out so a new stay on a reused guest row never inherits a stale prior-stay timestamp). Verified live: first invoke sent a real WhatsApp message to Eliad and logged it; a second same-period invoke returned `skipped: true`, proving both the idempotency guard and the log write. Mike also asked for a morning-useful cadence — daily digest summarizes the *full previous day*, sent 07:00 Israel next-morning (not 22:00 same-day), so it never reports "today so far." Phase 4: migration 185 registers 3 `pg_cron` schedules (`resort-digest-daily` 0 4 * * *, `-weekly` 0 4 * * 0, `-monthly` 0 4 1 * *, all UTC) — confirmed registered/active via `supabase db query --linked`. Known limitations flagged transparently, not fixed: cron times are static UTC (same as every other cron in this system — drifts ~1h in Israel winter, no DST auto-adjust); `tasks.room_number` stores bare numbers ("8") while `guests.room` stores full suite names ("אמטיסט 8") — readable by a human but not string-matched across the digest's two sections. Mike also asked to "handle anything relevant to system health" — fixed 2 unrelated pre-existing `deno check` failures found while testing: `automationSchedule.ts:1068` had a required parameter after an optional one (TS1016; all 4 call sites already always passed it, so the `?` was just wrong, not load-bearing); `executiveAssistant.ts` (×2) + `fieldOpsTranslation.ts` had an unsafe `TextBlock[]` → `Record<string,unknown>[]` cast (TS2352) fixed via `as unknown as` per TS's own suggested fix, zero behavior change. All 42 tests across the 4 affected `_shared` test files pass. Deployed: migrations 184+185, plus 13 functions (`resort-digest-cron` new + 12 redeployed for the shared-module changes: `automation-history`, `automation-queue`, `guest-portal-ops-request`, `notify-manual-task`, `whapi-webhook`, `whatsapp-cron`, `whatsapp-send`, `whatsapp-webhook`, `sla-escalation-cron`, `task-action`, plus a second `whatsapp-send`+`whatsapp-cron` redeploy after the `room_ready_at` write fix).

2026-07-11 | Executive Assistant — Eliad sync audit + role-aware `{{focus}}` persona | Mike asked to verify the assistant behaves correctly for Eliad, not just his own QA number. Audit against live production data (read-only `supabase db query`, no log access in this dev env) found: identity resolution solid (Eliad's `profiles.phone`/`role` correct, double-covered by `KNOWN_EXECUTIVES` + the DB fallback); `executive_bot_settings` persona row correctly shared/templated for both; but `xos_ai_rules` (module='executive') is one flat pool with no per-person scoping, and had 2 rows from Mike's own testing phrased "כאשר מייק אומר..." — reworded to a generic rule (removed the name, merged the duplicate) since the underlying fact applies to both. Also found Eliad's only 3 logged inbound messages were Mike's own synthetic diagnostic payloads (`wa_message_id` pattern `diag-eliad-text-test-*`), not real usage, and zero rows in `executive_action_log` for his number — so tool-calling was verified live for Mike only, never for Eliad. Sent Eliad two real onboarding messages via a one-off deployed-then-deleted Edge Function (reusing `sendWhapiText`/`formatWhapiSuitesConversationLog` so they land in his real conversation history exactly like a normal bot reply): a personal-assistant introduction, and a follow-up noting voice notes are supported and that the assistant remembers what he teaches it. Mike then asked why the persona uses `{{name}}`/`{{title}}` variables at all if the assistant is "for Eliad" — clarified the variables protect against the model believing it's mid-conversation with Eliad while actually replying to Mike's own test messages (78 logged messages vs Eliad's 3) — confirmed keeping them, and extended the pattern: added a third `{{focus}}` token (`ExecutiveProfile.focus`, `executiveIdentity.ts`) so the one shared persona/tools/rules pool still reads naturally per person — Eliad's focus emphasizes real operational resort management, Mike's explicitly names testing/correcting the assistant as a legitimate part of his role. `buildExecutivePersona` (`executiveAssistant.ts`) now does a third `.replaceAll`; `DEFAULT_PERSONA_TEMPLATE` and the live `executive_bot_settings` row both updated to match (verified byte-identical via query); `ExecutivePlaybook.js` admin caption updated to document the new token. Verified via `deno test` (10/10 pass, same 4 pre-existing unrelated type errors) plus a local script directly exercising `buildExecutiveSystemPrompt` with both profiles to confirm no cross-contamination between Eliad's and Mike's rendered prompts. Deployed (`functions deploy whapi-webhook`) and pushed to main.

2026-07-11 | Executive Assistant — deployed + live voice/data debugging session | Followed the previous entry's deploy (migration 182, `whapi-webhook`, frontend). Mike then live-tested voice notes on the Suites device and found real, reproducible bugs — each isolated via direct production diagnostics (synthetic webhook payloads + `whatsapp_conversations`/`xos_ai_rules` queries, no log access available in this dev environment) and fixed in sequence: (1) a failed voice transcription used to `continue` with zero reply and zero Inbox trace — added a graceful Hebrew fallback reply + Inbox placeholder row, mirroring the staff-group voice path's existing pattern; (2) the real transcription error, now visible, was `gemini_transcribe_404` — `whapi-webhook`'s `transcribeVoice()` had `GEMINI_MODEL="gemini-1.5-flash"` hardcoded with no fallback, and Google had retired that model from `v1beta`; switched to a `[gemini-2.5-flash, gemini-2.0-flash]` fallback list (same models already proven live in the executive assistant's own text path) — fixes voice for both the Suites DM pipeline and the staff-group task pipeline (shared function); (3) defensively strip Whapi's `"audio/ogg; codecs=opus"` mime string down to the bare type before Gemini's `inline_data` (never actually exercised before — the 404 fired at the routing level, before Google parsed the body); (4) after transcription started working, the assistant got stuck echoing "I can't understand the recording" regardless of content — root cause: `fetchExecutiveHistory` didn't strip the 🎤 Inbox-display prefix before feeding history back as LLM context, so after a few failed attempts the model anchored on its own recent "🎤 → can't understand" pattern and kept repeating it; strip added, verified live (a real question through the same polluted history got a correct answer); (5) directly asking "do you understand my recording?" still got a false "I can't understand" — the model was reasoning from general AI knowledge (text LLMs can't process raw audio) instead of this pipeline's actual behavior (voice is always transcribed to plain text before it reaches the model); added an explicit persona rule; pushed to both the code default and the live `executive_bot_settings` row (already seeded, migration 183, so the code fallback alone wouldn't reach it) — verified live with Mike's exact phrase. Mike then asked to verify "connected to guest profile and everything needed" — `find_guest_by_room` was silently stripping `guest_profile`/`guest_notes`/dates/spa/meal/attention fields it already had in hand, and had no name-search or "who's arriving today" tool; widened the tool + added `list_guests_by_scope` (arriving/departing/in-resort). Live testing that also surfaced a real pre-existing data bug: room/name guest lookups picked the furthest-future active booking (`arrival_date DESC`), so a room with two sequential bookings (today's guest checking out, tomorrow's checking in — normal churn) silently returned the wrong one; fixed to prefer whichever candidate is actually current today. `learn_executive_rule` verified live end-to-end too (rule landed in `xos_ai_rules`, then removed — it was a test). Also fixed one real regression introduced mid-session: widening the guest-lookup select for the `guest_id` path (not just room/name) briefly dropped the cancelled/checked_out safety filter — caught by the existing `send_guest_message — blocks a cancelled guest` test failing, reverted to reusing `loadActiveGuestById` for that path. `deno test --allow-env --no-check` 10/10 pass throughout (`--no-check` still only routing around the same 4 pre-existing unrelated errors). Every fix in this entry was deployed (`functions deploy whapi-webhook`) and verified against production data before moving to the next, per Mike's live-in-the-loop testing — not a batch of speculative changes.

2026-07-11 | Executive Assistant — Mike QA number connected (0506842439) | `_shared/executiveIdentity.ts` generalized from one hardcoded CEO number to a `KNOWN_EXECUTIVES` map (Eliad + Mike) behind a new `resolveExecutiveInbound()` → `ExecutiveProfile` (displayName/title); env fallback now also accepts `EXECUTIVE_PHONES` (comma-separated, old singular `EXECUTIVE_PHONE` still works); profiles-fallback keyed by email now covers both. `_shared/executiveAssistant.ts` persona/system-prompt/rules-header text is now built from the resolved profile instead of a hardcoded "אליעד" string; `learn_executive_rule` tool description genericized to "המנהל"; `handleExecutiveVoiceMessage` now resolves the profile itself and replies `executive_not_authorized` instead of crashing if resolution somehow fails after the caller's own `isExecutiveInbound` gate. Migration 182 links Mike's profile phone (`promote7il@gmail.com` → `+972506842439`) — note: the brief asked for migration number 177, but that slot was already taken by `177_spa_therapist_swap.sql`, so this landed as 182 (next free number after 181). `ExecutivePlaybook.js` copy updated to name both authorized executives instead of only Eliad. `executiveAssistant.test.ts`: added Mike-resolves / Eliad-resolves-without-env / unknown-number-fails-closed cases, fixed one pre-existing test that expected the CEO number to fail without `EXECUTIVE_PHONE` set (no longer true now that it's in `KNOWN_EXECUTIVES`) — `deno test --allow-env --no-check` 10/10 pass (`--no-check` needed only to route around 4 pre-existing TS2352/TS1016 errors in `automationSchedule.ts`/`fieldOpsTranslation.ts`, confirmed via `git stash` to predate this change). `npm run build` clean. **Not deployed** — local-only per instructions, awaiting explicit approval before `db push` / `functions deploy` / frontend push.

2026-07-11 | DREAM BOT Inbox mobile UX (Phases 1-3) | `App.js`: Inbox topbar on mobile slims to hamburger-only (bell/account/logout already reachable via the drawer, no duplication needed); ResortPulseBar hides on Inbox mobile; thread screen (`body.wa-inbox-mobile-thread`) now hides the topbar entirely too, not just `.mobile-bar` — fixes the "triple chrome" (App topbar + PulseBar + WA green header) stacked above every thread on a phone. `WhatsAppInbox.js`: mobile roster header compacted (✓ סמן-הכל icon-only, matching the existing refresh button) so the sticky filter block reliably stays one line; channel/sort chips now collapse behind a "סינון ▾" toggle, leaving only search + the primary status chips always visible — more roster rows fit above the fold. Swipe-to-reveal's vertical-scroll bailout raised 24→32px to cut accidental archive/resolve during fast flicks. Android/browser back button now closes an open thread via `history.pushState`/`popstate` instead of exiting the app (mobile only; desktop untouched); roster scroll position is preserved across a thread visit (the list/thread panes remount on toggle, so this previously reset to top every time). `AICopilot.js` + `RequestsAlertWidget.js`: both floating widgets now hide entirely while a thread is open on mobile (via a `MutationObserver` on `document.body`'s class) instead of floating a raised-but-orphaned offset over the composer once `.mobile-bar` is gone. **Deployed** — `npm run build` clean, pushed to main. Live click-through not possible in this dev environment (no login credentials available to the agent, same limitation as prior sessions) — Mike verified in his own `npm start` session before approving. One item deferred: Phase H (webhook per-message `catch` at `whatsapp-webhook/index.ts:4555` never replies to the guest nor sets `human_requested` on an uncaught exception) — flagged in the Phase 0 diagnostic, not yet approved/touched.

2026-07-11 | Inbox «נקרא» + fix unread surviving refresh | Roster button ✓ נקרא marks thread read without opening (same `markPhoneInboundRead` as open/reply). Root cause of «לא נקרא» coming back after F5: load stored cursors under `phone` while unread counted `phone::channel`. Migration 181 adds `inbox_channel` to `inbox_read_cursors` PK; save/load use `buildReadCursorsMap` threadKeys; Whapi cursors now persist too. Cursor-load merges optimistic marks (no wipe on slow SELECT). `inboxReadState` helpers + tests.


2026-07-11 | Fix CRA `npm start` OOM on Windows/Node 24 | Root cause: `NODE_OPTIONS=--max-old-space-size=…` set in the shell often never reaches the `react-scripts` Node process on Windows (`npm.cmd` / env propagation), so webpack still died at ~900MB (`FATAL ERROR: invalid table size`). Fix: `package.json` `start`/`build` now invoke `node --max-old-space-size=… node_modules/react-scripts/bin/react-scripts.js …` directly (heap flag on the real process). Added `.env.development` with `GENERATE_SOURCEMAP=false` to cut peak heap in dev. Verified: `npm start` → "webpack compiled with 1 warning", `http://localhost:3000` returns 200.

2026-07-11 | Smart Spa Board — Ezgo Activities import UI (Phase 3) | `SpaBoard.js`: "📊 ייבוא דוח פעילויות" toggles `<ActivitiesImportZone>` (drag/drop or click, .xlsx/.xls/.csv, same `xlsx` dynamic-import pattern as `SpaStagingPanel.js`) — parses via Phase 1's `parseEzgoActivitiesReport`, syncs via Phase 2's `syncEzgoSpaActivities` for `selectedDate`, then shows a toast with created/updated/room_unmapped/conflicts/unmatched counts and refreshes the board. New `<UnmatchedPanel>` — FAIL VISIBLE, always rendered (not behind a toggle) whenever `spa_import_unmatched` has unresolved rows for the selected date; shows guest/phone/time/room_raw + the Hebrew reason label. `room_unmapped` rows get an inline "שייך לחדר" select that inserts a `spa_room_aliases` row and marks it resolved (fixes future imports, does not retroactively create the missing appointment — noted in the button's title); every other reason gets a "✕ טופל" manual-dismiss. Realtime subscription extended to `spa_import_unmatched`. **Not verified by live click-through** — this dev environment's `npm start` (webpack dev-server compile) hits a Node heap OOM crash (`FATAL ERROR: invalid table size Allocation failed`) even with `NODE_OPTIONS=--max-old-space-size=4096`, reproducible and unrelated to this diff (same failure on a clean checkout); `npm run build` (production) is clean both before and after this change. Verified by careful manual line-by-line re-read of the full component instead — matches the same limitation already logged for this component in the 2026-07-10 SpaBoard entries.

2026-07-11 | Smart Spa Board — Ezgo Activities sync engine (Phase 1 + 2) | New `src/utils/ezgoSpaActivitiesParser.js` — pure row parser for the daily Activities report (time range, phone, room-name, guest-name/group-label, note), every non-empty row always returns (never dropped; unparseable fields surface as `warnings`, ZERO DATA LOSS). New `src/utils/spaActivitiesSyncEngine.js` — resolves each row's room (via `spa_room_aliases`), therapist (find-or-create by name), and guest (phone lookup + disambiguation), then upserts `spa_appointments` and write-throughs `guests.spa_date`/`spa_time`/`guest_profile.spa` (queried fresh per guest so an untouched earlier appointment that day still wins correctly), staging anything unresolved into `spa_import_unmatched`. An external Plan-agent review (playbook §6 step 5b — Golden Profile write-through is high-stakes) caught 4 real bugs before this touched anything live: (1) two guests sharing one phone with overlapping stays could both resolve to the same guest_id — fixed by disambiguating on the row's own לקוח name first (word-order-insensitive); (2) re-importing a row that matched a staff-cancelled appointment would silently "revive" it without ever setting status back — fixed by excluding cancelled appointments from the re-import index entirely; (3) `guests.phone` was compared to `row.phone` without matching normalization, so a guest stored in an unexpected phone shape would silently miss every match — fixed by reusing the parser's own `normalizeActivitiesPhone`; (4) an unmatched row was labeled by `!guest ? 'no_guest_match' : 'room_unmapped'`, so a row missing only a valid time range was mislabeled as a room problem — fixed with an explicit priority classifier, plus migration 179 (pushed) widening `spa_import_unmatched.reason`'s CHECK to add `invalid_time_range`/`write_failed`. 41 unit tests on the pure helpers (guest disambiguation, idempotency matching, profile-patch building), `npm run build` clean. Not yet wired to any UI — Phase 3 (SpaBoard import button) is next.

2026-07-11 | Smart Spa Board — full Ezgo Activities sync schema (Phase 0) | Migration 178 (pushed): `spa_appointments` gains `ezgo_line_id`/`phone_snapshot`/`treatment_type` (partial unique index on `(appointment_date, ezgo_line_id) WHERE ezgo_line_id IS NOT NULL` — re-import idempotency without breaking rows that lack a line id); new `spa_room_aliases` (Ezgo raw room-name string → `spa_rooms.id`, a table not a hardcoded map so Mike can fix an unmapped room from the UI later without a deploy) and `spa_import_unmatched` (FAIL VISIBLE staging with full `raw_row` JSONB — ZERO DATA LOSS for rows the sync engine can't resolve a guest and/or room for). Seeded 21 high-confidence aliases (couple rooms direct + `(זוגי)`/`סוויטת ` variants, single rooms `חדר 1..15` excluding 9/10 mapped sequentially to `ספא יחיד 1..13` per Mike's confirmation). Deliberately left unmapped: "ג'קוזי 1" — Mike didn't know which physical room it corresponds to and explicitly said not to guess; will surface as `room_unmapped` on first real import. This is schema only — Phase 1 (parser), Phase 2 (upsert engine + guest write-through), Phase 3 (SpaBoard import UI), Phase 4 (`spa-schedule-webhook` upgrade), Phase 5 (bot context enrichment) not yet built.

2026-07-11 | Smart Spa Board — swap therapists between appointments | Staff asked for an easy way to exchange which therapist is assigned to which room (e.g. a preference/schedule conflict mid-day). A naive two-step UPDATE would trip migration 176's `spa_appointments_therapist_no_overlap` exclusion constraint mid-swap (each therapist briefly looks double-booked against the row not yet updated), so migration 177 makes that one constraint `DEFERRABLE INITIALLY IMMEDIATE` (room constraint untouched — room_id never changes in a swap) and adds `swap_spa_therapists(p_appt_id_a, p_appt_id_b)`, a `SECURITY DEFINER` RPC (same auth.uid()-check pattern as `delete_guest_profile`, migration 141) that locks both rows, defers just that constraint for its own transaction, and exchanges `therapist_id` atomically. `SpaBoard.js`: each occupied appointment card with an assigned therapist gets a small 🔄 button opening `SwapTherapistModal`, listing other same-day appointments that also have a therapist assigned; picking one calls the RPC and refreshes. A genuine remaining conflict (e.g. swap would double-book against a third appointment) surfaces at commit as SQLSTATE 23P01 — mapped to the same FAIL VISIBLE "⚠ התנגשות בלוח הזמנים" wording already used in `AssignModal`. `npm run build` clean. **Deployed** — migration 177 pushed, frontend pushed. Live click-through still blocked in this dev environment (no login credentials available to the agent) — Mike verified/approved via description, not an in-browser click-through.

2026-07-10 | Smart Spa Board (WS-A Phase 1 + Phase 2 queue/fix) | New `src/components/SpaBoard.js` — nav route `spa_board` ("💆 לוח ספא", App.js). Incoming-requests queue reads `guest_alerts` where `alert_type='spa_request'`, each row gets a chat-launch button (reuses `openDreamBotChat`) and a "שבץ תור" action. Room-columns board (couple/single sections, mirrors RoomBoard.js's layout conventions) shows `spa_appointments` for a selected date per room, with a visible ⚠ when `therapist_id` is null (FAIL VISIBLE/Disable-Don't-Hide, CLAUDE.md §0). Assign modal: guest search (same `.ilike` pattern as ReceptionistView.js), room/therapist/date/time fields; on save, a Postgres exclusion-constraint violation (23P01 — the GiST constraints from migration 176) is caught and shown as a plain-language "⚠ התנגשות בלוח הזמנים" instead of a raw SQL error; on success, writes through `guests.spa_date`/`spa_time` and resolves the originating alert. Therapist name editor (`✏️ עריכת שמות מטפלים`) — inline list against the 70 placeholder rows seeded in migration 176, unique-name violations (23505) surfaced as a clear "⚠ השם כבר קיים" instead of a raw error. Self-caught bug during review: `AssignModal` never actually unmounts (it renders `null` when there's no draft), so its `useState` initializers only ran once — reopening for a different room/alert would keep the previous open's stale form values; fixed with React's `key`-remount pattern. Also fixed `guest-portal-spa-request`: was writing `alert_type='request'` (generic) instead of `'spa_request'` — both the `guest_alerts` insert and the Whapi notify card headline now use the correct type. Verified via `npm run build` (clean) and a manual line-by-line re-read — live browser click-through was not possible this session (Claude in Chrome / preview tools disconnected in this environment).

2026-07-10 | WS-B ops-task unification (Phase 4) + WS-A spa schema (Phase 0) | Migration 176 (not pushed): `spa_rooms`/`spa_therapists`/`spa_appointments` — real seed (7 couple + 13 single rooms, exact names; 70 zero-padded therapist placeholders), GiST exclusion constraints on both `room_id` and `therapist_id` against a generated `appointment_range` (naive date+time, matching `spaSchedule.ts`'s existing convention, no timezone math) so a double-booking on either axis is rejected at the DB layer, not just app logic; cleaner-role RLS lockdown added to match migration 087's pattern. New `_shared/createGuestOpsTask.ts` extracted from whatsapp-webhook's former private `createPendingOpsApprovalTask` — single writer for guest-initiated physical (amenity/maintenance/cleaning) ops tasks, now called by Meta (both the Tier-0 regex intercept and the `log_guest_request` LLM-tool path — zero behavior change there), a brand-new Whapi guest-DM Tier-0 intercept (previously whapi-webhook created NOTHING for a guest DM physical request — it silently fell through to the LLM chat reply), and `guest-portal-ops-request` (in-house-eligible + allowlist-matched taps now route to the Operations Board with SLA instead of the Requests Board; today's single generic "הזמנת שירות לחדר" CTA doesn't match the allowlist so it's unaffected — new itemized amenity CTAs with quantity are a separate follow-up). Added a guest+department duplicate guard (an external Plan-agent review caught a real bug here first: keying dedup on the 3-bucket `sla_category` instead of `department` could have silently swallowed a room-cleaning request as a "duplicate" of an unrelated open AC-repair ticket — fixed before this was ever deployed). guest_alerts fallback path preserved unchanged for ineligible/non-allowlisted requests (Zero Data Loss — nothing that reads `alert_type='portal_room_service'` breaks). **Deployed** — migration 176 pushed; whatsapp-webhook, whapi-webhook, guest-portal-ops-request deployed.

2026-07-10 | Whapi guest-DM context parity — Suites device now gets full guest_profile like Meta | Investigation found the gap was backwards from initial assumption: Meta's `buildGuestStageContext` (whatsapp-webhook/index.ts) already injected the full `guest_profile` JSONB (VIP status, dietary/allergy tags, occasion, arrival_context, staff_note) plus arrival_time/room_type/arrival_confirmed/spa schedule/computed guest stage into its prompt; Whapi's `buildWhapiGuestContextLine` (whapi-webhook/index.ts) only sent name/room/status/arrival_date/departure_date and never read `guest_profile` at all, despite its own routing-suffix prompt telling the LLM a guest profile is "always attached." Fixed by extending `buildWhapiGuestContextLine` to also inject `guest_profile` (via the existing shared `formatGuestProfileForAi` from `_shared/guestProfile.ts`, previously Meta-only), `arrival_time`, `room_type` suite label, `arrival_confirmed`, spa schedule (via `_shared/spaSchedule.ts`'s `formatSpaScheduleDisplay`, also previously Meta-only), and the same computed guest-stage (pre-arrival/arrival-day/in-house) + pre-checkin room-name masking Meta already has. Meta unchanged. Deliberately deferred (tracked as a separate follow-up): Whapi still lacks `log_guest_request` tool-calling that Meta has — this pass is guest-profile context only. **Deployed** whapi-webhook.

2026-07-10 | P0 — Meta guest bot: every free-text after welcome died with `DATE_CHANGE_RE is not defined` | Root cause: a prior refactor moved the Stay-Change Shield regex into `_shared/guestInboundOrchestrator.ts` (line 151) but left it un-exported, while `whatsapp-webhook/index.ts` (line ~3705) still referenced it as a bare identifier — threw `ReferenceError` on every guest free-text message that reached that point (fruit/price asks, "6 קולבים", bare "?", etc.), swallowed by the wrapping try/catch, writing a fake `[SYSTEM] ⚠️ שגיאה פנימית` row to `whatsapp_conversations` with zero reply sent to the guest. Fix: exported `DATE_CHANGE_RE` from `guestInboundOrchestrator.ts` and added it to the existing import in `whatsapp-webhook/index.ts` — no regex/semantic change, no duplication. Audited the same refactor for sibling landmines: two other stale comments (lines ~987, ~2442) falsely claimed `ARRIVAL_TIME_QUESTION_RE`/`ARRIVAL_TIME_UPDATE_RE`/`PORTAL_BASE_URL` were "moved/imported" when they were never exported — corrected the comment text; left those three symbols private since nothing else references them. `whapi-webhook` unaffected (never imported `DATE_CHANGE_RE`), no changes there. **Deployed** whatsapp-webhook.

2026-07-10 | Stage 2.5 Shabbat bundle extended to Friday arrivals + Whapi routing unconditional for all suite automation | `automationSchedule.ts`: `isFridayArrivalDate`/`isShabbatBundleArrival` — night_before now fires same-day (day_offset 0) at `local_time_shabbat` for Friday arrivals too (previously Saturday-only, Friday guests got the weekday Thursday-19:00 reminder); new `already_checked_in` skip (Friday + status=checked_in + `checkin_time` before today's 15:00 gateway — timestamp-based, not raw status, so it survives the same-tick auto-checkin race). `whatsapp-cron/index.ts`: 15:00 auto-checkin sweep moved to run AFTER `due[]` is computed (was before) so the night_before scan sees pre-promotion status this tick instead of racing its own promotion at the identical AUTO_CHECKIN_LOCAL_HOUR=15 instant. `whatsapp-send/index.ts`: `shouldUseWhapiForGuestAutomation` collapsed to a thin wrapper over `shouldRouteGuestOutboundViaWhapiSuites` — dropped the `dispatch_channel`/Shabbat-only gates, so ALL autonomous suite automation (pre_arrival_2d, stage_2_arrival, night_before, morning_suite/welcome, mid_stay, checkout_fb, room_ready) routes to Whapi uniformly once `GUEST_WHAPI_SUITES_ENABLED` is on, any day; `resolveShabbatAwareScriptKey`/`resolveShabbatAwareSessionImageUrl` now take a caller-supplied `isShabbat` boolean instead of recomputing internally, so night_before can pass Friday+Saturday while morning_suite/pipeline stay Saturday-only. `room_ready` and `guestInboundOrchestrator.ts`'s live Stage-2 reply had their own duplicate `dispatch_channel` gates removed to match. `automation-queue`: `already_checked_in` added to the temporal-guard visible-skip set (shows "pending" + reason in the Live Queue, never hidden — it's a per-day guard, not a permanent one like wrong_room_type). New tests: `automationSchedule.test.ts` (7 cases), `guestWhapiRouting.test.ts` (4 cases) — first Deno tests in the repo; `deno test --no-check` (repo has a pre-existing unrelated `TS1016` in `resolveTruncatedReplyFallback` blocking plain `deno check`). ACC routing-info panel text updated (Friday+Shabbat bundle, channel independent of arrival day). **Not deployed** — `npm run build` clean, awaiting approval.
2026-07-10 | Inbox mobile — fix "can't switch conversation" after opening a thread | Root cause: `ContactItem`'s swipe-to-archive gesture (`WhatsAppInbox.js`) tracked only horizontal pointer delta with no vertical-dominance check and no `pointercancel` handling — any roster scroll with >48px horizontal jitter (routine on a diagonal thumb-flick, esp. in RTL) flipped a row into `swiped=true`, silently eating that row's next tap instead of opening the contact. Fixed: gesture now bails out once vertical motion dominates, and listens for `pointercancel` alongside `pointerup`. Also gave `.wa-mobile-back` `position:relative;z-index:50` so the thread header's back button always wins over the `⋮`-menu's full-screen close-catcher (previously required 2 taps to leave a thread while that menu was open). Verified via isolated gesture-logic simulation (old logic reproduced the bug on a realistic RTL diagonal scroll, new logic doesn't); full in-app click-through blocked by Supabase login wall (no test creds in this environment — same known limitation as Voucher/Inventory hubs). `npm run build` clean. **Not deployed** — awaiting approval.
2026-07-10 | Meta prompt-assembly dedup + GuestsPage ETA board | `whatsapp-webhook/index.ts` now consumes shared `fetchGuestBotSettings`/`fetchGuestLearnedRulesSuffixes`/`FALLBACK_SYSTEM_PROMPT`/`appendGuestBrainInvariantSuffixes` from `_shared/guestBotSettings.ts`+`guestBotPrompt.ts` instead of ~95 lines of duplicated fetchers/suffix constants — final assembled prompt text/order unchanged; `fetchBotConfig`/`buildSystemPrompt`/`fetchBotScripts` stay local (config-cache side-channel into `sendReply()`, 3rd-tier fallback text divergence, multi-key script reuse). Tool-calling (`log_guest_request`), `buildGuestStageContext`, and in-room override remain Meta-only by design — not unified with Whapi's `generateGuestChatReply` (no tool-calling support there yet). `AILearningButton.jsx` chat-module label now says "Dream Bot + Whapi" to match `BotSettings.js`. `GuestsPage.js` gained a new "לוח זמני הגעה — היום" panel (ETA-sorted, FAIL VISIBLE ⚠ for missing/invalid `arrival_time`); `isPreArrivalTodayGuest` exported from `guestCheckinMatrix.js` for reuse. **Not deployed** — `npm run build` clean, awaiting approval.
2026-07-10 | Migration 173 didn't stick — real root cause was the fix's own transport (session P0 follow-up) | 173 was applied ad hoc via `Get-Content -Encoding UTF8 file.sql \| npx supabase db query --linked` instead of `db push`; PowerShell 5.1 re-encodes piped text to a native process's stdin via the console codepage (not UTF-8), silently turning every Hebrew char into literal `?` before it reached Postgres — confirmed by production audit (only `night_before_reminder_shabbat` + `stage_3_morning_shabbat` still 100% `?`, byte-for-byte, every other Hebrew row seeded via `db push` intact). migration 174 re-seeds both rows with a DO-block self-test; `isGarbledDbLabel` renamed/broadened to `isGarbledDbText` (also catches `�` and `Ã..` mojibake) and reused for a FAIL VISIBLE banner in `AutomationControlCenter.js`'s weekday+Shabbat session textareas, plus a `⚠ script_key` subtitle in `BotScriptEditor.js` when the title itself is garbled. **Not deployed** — migration 174 not pushed, frontend not pushed; awaiting approval.
2026-07-10 | Fix Shabbat bot_scripts display_name encoding (???? in UI) | Root cause: migration 172 applied via PowerShell pipe without `-Encoding UTF8` corrupted Hebrew `display_name` in DB. migration 173 re-seeds shabbat scripts; new `src/utils/botScriptLabels.js` single source for friendly labels + `isGarbledDbLabel` fallback in BotScriptEditor/ACC. **Deployed** migration 173 + frontend.
2026-07-10 | Shabbat arrival automation via Whapi (Suites device) | migration 172: `session_message_script_key_shabbat` + `session_message_image_url_shabbat` + `local_time_shabbat` on `automation_stages`; seeds `night_before_reminder_shabbat` (Friday 15:00 text + suiteshabat.jpeg) + `stage_3_morning_shabbat` (18:00 check-in). `whatsapp-send`: `shouldUseWhapiForGuestAutomation` auto-routes Shabbat suite arrivals via Whapi; Shabbat-aware script/image resolution; `sendWhapiImage` in `whapiSend.ts`. `AutomationControlCenter`: Shabbat variant editor panel. `ArrivalImportPanel`: auto-preselect Saturday suite guests. **Deployed** migration 172 + whatsapp-send + whatsapp-cron + automation-queue + whapi-webhook + frontend.
2026-07-10 | Whapi guest DM — strip [WHAPI] leak to guests | Root cause: inbox-only `[WHAPI]` dispatch tag on `whatsapp_conversations` rows was fed to LLM history → model echoed prefix in replies; `_shared/outboundDispatchTag.ts` strips tags from history + outbound send; `whapi-webhook` + `guestBotLlm.ts` fixed. **Deployed** whapi-webhook + whatsapp-webhook.
2026-07-09 | Unified guest brain — Meta + Whapi share BotSettings | `_shared/guestBotModelRoute.ts`, `guestBotPrompt.ts`, `guestBotSettings.ts`, `guestBotLlm.ts` — Whapi DM uses same prompt priority, suffixes, learned rules, preferred_model + Claude/Gemini failover; `BotSettings.js` dual-channel toggles. **Deployed** whatsapp-webhook + whapi-webhook + frontend.
2026-07-09 | Unified staff-handoff + routing prompts (both bots) | `_shared/guestBotHandoff.ts` — handoff sentence + Inbox red dot; **Deployed** whatsapp-webhook + whapi-webhook.
2026-07-09 | Whapi task card unify + bot stale-topic fix | `_shared/taskCard.ts` — `notify-manual-task` cards now match whapi-webhook layout (`New Task Opened` / `Task` / `Status`) with `📍 Source` on its own line; `automationSchedule` allowlist expanded for שער/ידית-לדלת → Tier-0; `whatsapp-webhook` `FOCUS_CURRENT_MESSAGE_SUFFIX` stops LLM re-mentioning already-answered topics; `whatsapp-send` inbox_reply clears `requires_attention`/`needs_callback` on staff manual reply. **Deployed** whatsapp-webhook + notify-manual-task + whatsapp-send + whapi-webhook.
2026-07-09 | Whapi/Meta guest-journey parity rollout (§1-§4) | §1 unified guest-inbound phone lookup (`resolveGuestByInboundPhone`, arrivalConfirmation.ts) — whapi-webhook no longer picks the wrong duplicate-phone row. §2 new `_shared/guestInboundOrchestrator.ts` (`runGuestArrivalConfirmation`, record-only ETA, `resolvePlaceholders`) shared by both webhooks — Whapi now supports "כן מגיעים"→Stage 2 and record-only ETA capture, both firing ahead of staff-claim/bot-off gates same as Meta; `bot_active_whapi` stub (migration 170). §3 `whatsapp-send`: autonomous cron path now honors `guests.dispatch_channel="whapi"` for night_before/morning_suite/morning_welcome/room_ready/stage_2_arrival-reconcile/generic pipeline triggers — previously Meta-only regardless of dispatch_channel; `WHAPI_UNSUPPORTED_STAGES` emptied (was morning_suite/morning_welcome/room_ready). §4 migration 171 `guest_channel_claims` (per-channel staff claim) + `bot_active_meta` alias; `isGuestStaffClaimActive(guest, channel)` gained a channel param (default "meta", zero behavior change for every existing caller); `WhatsAppInbox.js` gained a second toggle (מכשיר הסוויטות/Whapi) and per-channel claim (`setClaim`, `mergeIncomingRows`, `normalise` no longer leak a Meta claim onto a Whapi thread or vice versa). **Known gaps (not fixed this round):** `whatsapp-send`'s autonomous-dispatch claim gate still only checks the Meta claim (a Whapi claim doesn't yet pause Whapi automation); `guest_channel_claims` isn't in the Realtime publication (no live cross-session claim sync yet); `whatsapp-webhook`'s local `isStaffClaimMutingGuest` was intentionally left as its own Meta-only duplicate of `isGuestStaffClaimActive` (zero risk, zero functional gain to unify); `inbox_read_cursors` still Meta-only; balloon/admin/operational intercepts + LLM-reply unification on Whapi deferred to next round. **Not deployed** — migrations 170/171 not pushed, functions not redeployed, frontend not pushed. `npm run build` clean.
2026-07-09 | Pipeline segment fix — suite vs day-pass separation | Root cause: bifurcated stages still had `applies_to=all` in DB → Live Queue showed BOTH pipelines per guest; migration 169 re-asserts applies_to + `guest_pipeline_stage_suppressions` per-guest cancel; automation-queue hard-filters; ACC segmented UI + ✕/↩ per stage; guestJourneyStages rewrite. Includes 168 send-window fix. Hidden `local_time_end=12:00` (migration 129) + `local_time=17:00` in ACC → never-send; `effectiveCeilLocalHour` in resolver; ACC shows «עד שעה» for mid_stay; migration 168 clears invalid ceilings. `_shared/automationDuplicateGuard.ts` `checkPipelineDuplicate()` now checks `AUTOMATION_DUPLICATE_GUARD_ENABLED` first (default "true", unchanged behavior) — set to `false` in Supabase secrets to disable the `notification_log`-based duplicate block globally; `force:true` bypass and `already_sent` guest-flag checks in `automationSchedule.ts` are untouched. **Deployed** whatsapp-send + whatsapp-webhook; secret set to `false`.
2026-07-09 | Orit CS Agent read-only IMAP mode | migration 165 `read_only_mode`+IMAP fields; `_shared/imapMail.ts`+`mailIngest.ts`; sync ללא auto-ack/שליחה; UI העתקה ל-Outlook; דייג'סט «פניות חדשות אתמול»; `docs/orit_outlook_connect.md` עודכן.
2026-07-09 | automationDuplicateGuard delivery-first fail mode | `_shared/automationDuplicateGuard.ts` now supports `AUTOMATION_DUPLICATE_LOOKUP_FAIL_MODE` (`fail_open` default, `fail_closed` optional): transient `notification_log` read failures no longer block guest sends by default, while confirmed duplicates (`already_sent`) remain blocked.
2026-07-08 | Greeting Tier-0 + courtesy shield fix (session 147) | "היי"/"שלום" were in COURTESY_ONLY_PATTERN → silent exit with zero reply; split GREETING_ONLY_PATTERN + handleGuestGreeting (bot_scripts greeting_reply); courtesy silence only when thread already has outbound. migration 161. **Deployed** webhook + db.
2026-07-08 | whatsapp-webhook low-confidence FAQ handoff (session 146) | Defensive Shield was suppressing the canonical handoff sentence instead of sending it — guests got silence when the LLM correctly declared "I don't know"; now sends verbatim + flags staff. **Deployed** whatsapp-webhook.
2026-07-08 | Inbox ghost messages — Stage 2 logging + fetchAll race (session 144b) | Root cause: (1) `whatsapp-send` `stage_2_arrival` fast-path sent to Meta + `notification_log` but never inserted `whatsapp_conversations` (cron/pipeline fallback invisible in Inbox); (2) pre-157 `intent='arrival_confirmed'` CHECK failures logged error only; (3) `fetchAll` replaced `allMsgsRef` wholesale, wiping Realtime rows mid-flight. Fixes: conv log in whatsapp-send stage_2; `insertGuestOutboundIfNotMuted` retries with `intent=null` on 23514; `fetchAll` uses `mergeThreadRows`; Realtime INSERT always triggers `fetchSince`; migration 160 backfills missing Stage 2 rows from `notification_log`. **Not deployed.**
2026-07-08 | Inbox red dot — fallback handoff + complex FAQ paths (session 145) | `whatsapp-webhook`: generic fallback ("אעביר לצוות הקבלה") and low-confidence FAQ miss (🤔) now set `human_requested` on the inbound row via existing `patchClaimedInbound` — same mechanism as guest_alerts Tier-0 shields; also ops-room LLM dispatch branch. `GuestAttentionBadge.js`: new `fallback_no_match` (🔁). **Deployed** `whatsapp-webhook`; frontend pushed to main (Vercel auto-deploy).
2026-07-08 | DREAM BOT Inbox — reaction "תגובה" placeholder + stuck network-error banner (session 144) | Two unrelated bugs, verified by code inspection (not guessed). **Bug 1 (frontend-only, no webhook change needed):** `inboxReactions.js`'s reaction-emoji regex captured a single `\p{Extended_Pictographic}` code point — real WhatsApp reactions are usually multi-codepoint (❤️ = heart + VARIATION SELECTOR-16, 👍🏼 = thumb + skin-tone Emoji_Modifier, both default quick-reactions), so the whole anchored pattern failed to match and fell through to the "guest_reaction, unparsed" branch → content-free 💬 "תגובה" bubble, even though the webhook (`whatsapp-webhook/index.ts` line ~3495) had logged the real emoji + quoted snippet correctly. Fixed with `EMOJI_SEQUENCE`, a proper emoji-grapheme-cluster pattern (modifier/VS16/ZWJ/flag-aware) — verified against 9 real-world emoji cases (including ZWJ family 👨‍👩‍👧 and flag 🇮🇱) plus 2 non-reaction text cases. The unparseable-fallback branch now returns `null` instead of a fake stub, so any future edge case falls back to the raw stored message (FAIL VISIBLE §0.3) instead of a hidden/generic bubble. 8 new regression tests. **Bug 2:** `fetchAll`/`fetchSince`/`fetchOlder` in `WhatsAppInbox.js` set the error banner via bare `err.message` on failure but never cleared it on the next successful poll. Confirmed against `@supabase/postgrest-js` source (in `node_modules`) that a network-level fetch failure surfaces as literally `"TypeError: Failed to fetch"` in `err.message` — so one transient blip (tab wake-from-sleep, brief WiFi drop) left a permanent, unrelated-looking red banner stuck over the thread. Added `setNetworkLoadError`/`clearStaleNetworkError` — operation-prefixed Hebrew messages that self-clear only the exact stale text they set (never clobbers a different, still-relevant error like a send failure). Send-action catches (room-ready, manual reply) now always prefix with the operation name too instead of showing a raw library string. `npm run build` clean, 23/23 suites, 190/190 tests (was 182). **Not deployed** — frontend-only, no migration/function touched; awaiting `git push` approval. Not verified live in browser (demo login rejected in this dev environment, same limitation as sessions 47/51/52/127) — Mike should confirm a real ❤️/👍🏼 reaction renders correctly and a network-error banner self-clears on the next successful sync.
2026-07-08 | Guest-journey audit fixes — Shabbat template bypass + day-pass checkout editor lock (session 143) | `whatsapp-send` `resolvePipelineTemplateName()`: manual `force_channel=meta_template` dispatch of morning_suite/morning_welcome honored `automation_stages.meta_template_name` (weekday name, migration 102) before the Shabbat check → Saturday-arrival guest got the 15:00 template; now always routes to `suite_welcome_morning[_shabbat]` (same rule as night_before; autonomous path was already correct). **Deployed.** Migration 159: `bot_scripts.checkout_fb_daypass` `trigger_event` 'ongoing'→'checkout_fb' — the 'ongoing' label (seeded in error by 099) locked the text field in BotScriptEditor even though the text IS sent (lookup by script_key). **Applied selectively** (158 sidelined+restored, only 159 pushed, self-test passed; 157 turned out already applied, contra session-141b docs — 158 remains the only pending migration). active_sprint.md template names corrected to live code. Verified NOT bugs: morning_daypass script is reachable (manual force-session, hardcoded). commit ebffe1b. 179 tests, build clean.
2026-07-08 | Guest requests routing fix — Operations → Requests Board (session 142) | `guest-portal-ops-request` now writes `guest_alerts` (`portal_room_service`) + `onGuestAlertInserted` → Whapi «בקשות אורחים» (was `tasks`+Adir DM). `whatsapp-webhook`: `logAdministrativeRequestTask` replaced with `logAdministrativeRequestAlert` (spa/reception); removed duplicate `tasks` inserts from financial/stay-change handlers. migration 158 backfills open `portal_room_service`+`קבלה/בקשות` tasks → `guest_alerts` and marks tasks `done`. `RequestsBoard` labels for `portal_room_service`/`financial_issue`. 179 tests, build נקי. **Not yet deployed.**
2026-07-08 | Stage 2 arrival confirmation invisible in Inbox — CHECK constraint bug (session 141b) | Root cause found via live DB audit (notification_log vs whatsapp_conversations, last 10 days): `whatsapp_conversations_intent_check` never allowed `intent='arrival_confirmed'` (only its text-path sibling `'confirmation'`) — every BUTTON-tap Stage 2 reply since commit 48e2314 (2026-07-04) was delivered to the guest via Meta successfully but silently failed to log into the Inbox (125 sent, only 9 logged, zero button-sourced). Migration 157 widens the constraint. Also added a Defensive Shield check (`isAutoAwayMessage`/`handleAutoAwayMessage`) — some guests' own numbers are WhatsApp Business accounts whose automated away-message was being misread as a real reply and answered by the LLM with confusing off-topic text; now logged silently with `intent='auto_away_message'`, no LLM call. 179 tests, build נקי. **Not yet deployed** — awaiting Mike's approval.
2026-07-08 | Orit CS Agent plan complete (session 140c) | `manager-mail-auto-ack` function+`runOritAutoAck` shared; UI: snooze/toggle auto-ack/FAIL VISIBLE mailbox; `docs/orit_outlook_connect.md`; tests `oritAgentMail`; build נקי.
2026-07-08 | Orit CS Agent access flag (session 140b) | migration 156 `profiles.orit_cs_agent_access`+RLS; מתג ב-`UserManagement`; super_admin תמיד רואה; `orit@dream.io` seeded.
2026-07-08 | Orit CS Agent — סוכן שירות לקוחות לאורית (session 140) | migration 155 `orit_agent_*` (mailbox/threads/messages/auto-ack/drafts/digest)+RLS+seed demo+cron; `_shared/oritAgentMail.ts`+`oritAgentAi.ts`+`microsoftGraph.ts`; functions: `manager-mail-oauth`/`manager-mail-sync` (auto-ack 72ש')/`manager-mail-analyze`/`manager-mail-send`/`manager-morning-digest` (Whapi DM); `OritCustomerServicePanel.js`+`auth.js`+`App.js` route `orit_cs_agent` (אורית+super_admin); build נקי. **מקומי — לא deployed.**
2026-07-07 | guest_alerts → Whapi «בקשות אורחים» (session 137b) | `_shared/guestAlertWhapiNotify.ts` — `onGuestAlertInserted` אחרי כל insert ל-`guest_alerts` (webhook/portal/inbox); אותו `resolveRequestsWhapiGroupId` כמו Inbox; functions: webhook+inbox-route-request+guest-portal-upsell+guest-portal-spa-request.
2026-07-07 | Group courtesy automation scope (session 139) | migration 154 `guests.automation_scope` (full/courtesy_only/muted)+`merge_automation_scope`; remark occupant (שם+טלפון בהערות, coordNameDuplicated) → courtesy_only = Stage 4 בלבד; רכז עירייה בלי דייר → muted; `ArrivalImportPanel` toggle+עמודת אוטומציה; `_shared/automationSchedule.ts` gates; functions: send/cron/webhook/queue; 179 tests, build נקי.
2026-07-07 | Inbox guest image preview (session 138) | migration 153 `message_type`/`media_url`/`media_caption`+bucket `wa_inbox_media`; `_shared/metaMedia.ts` הורדה מ-Meta+שמירה ב-Storage; `whatsapp-webhook` ענף `image`→`intent=media_received`; `WhatsAppInbox` בועת תמונה+lightbox; 177 tests, build נקי.
2026-07-07 | inbox-route-request Whapi group fix | `resolveRequestsWhapiGroupId` — secret `WHAPI_REQUESTS_GROUP_ID` / routing_config / auto-discover «בקשות אורחים» via Whapi API; Inbox toast מבדיל DM מול קבוצה.
2026-07-07 | whatsapp-webhook/index.ts (session 136) | Bugfix: repeat typed "כן, מגיעים!" after Stage 2 already sent fell through to free-text LLM (off-script "hallucinated" reply) instead of staying silent like a repeat button-tap. Removed `canGuestConfirmArrival()` pre-check from pre-burst + post-burst text-confirmation branches — `handleStage2ArrivalConfirmation()` now always runs first and handles the repeat case internally, same as the button path. Deployed (commit c0c7d83).
2026-07-07 | whatsapp-webhook/index.ts (session 136 cont.) | `EdgeRuntime.waitUntil()` keeps the isolate alive until the async `processAsync()` task (LLM round-trip + burst coalescing, 2-5s+) finishes, instead of freezing right after the response to Meta and silently dropping the reply. Falls back to the old un-awaited call when unavailable (local dev). Deployed (commit 0fa852c).
2026-07-07 | whatsapp-send/index.ts (room_ready automation_muted exemption) | `automation_muted` (group guest) now exempts `room_ready` + manual triggers from the mute — those are one-off staff-approved dispatches, not the automated timeline sequence the flag exists to silence. Without this a muted guest's room never notified at all. Deployed (commit c0c7d83).
2026-07-07 | housekeepingWaParse (backend `_shared/*.ts` + frontend `src/utils/*.js`) | ✅ checkmark now always wins over check-in phrasing on the same line (room turned over arrives first; "N צ'ק אין" is typed later, on its own) — was previously ambiguous. New test coverage for the same-line-both-signals case. Deployed via whapi-webhook (commit 51ee425).
2026-07-07 | ⚠️ Concurrent-editor collision incident | Found a stale `.git/index.lock` (~37 min old, no live process) plus a pre-existing staged index containing a *partially-written* `whatsapp-webhook/index.ts` (truncated mid-comment, `Deno.serve()` never closed) and a truncated `docs/changelog.md` line — evidence Cursor and this session both touched the same files around the same time (see CLAUDE.md's existing "ריבוי כלים על אותו repo" warning). Cleared the stale lock after confirming no process held it, then caught the truncation via a routine post-commit `git show --stat` file-count check and fixed it in an immediate follow-up commit before pushing. No broken code reached origin/main. Lesson: always verify commit file-count matches intent, especially right after any lock/staging anomaly.
2026-07-07 | Doc 2 suite sync upgrade (session 134d) | `ArrivalImportPanel`+`guestImportIntelligence` — עמודת «פעולת סנכרון»; enrich ממלא חדר רק אם ריק; שיבוץ-בלבד דרך RPC `suite_rooms`+`findExistingGuestRow` (לא דורס); הזמנה מרובת-חדרים → תווית משולבת `אמטיסט 8 · וילה 3` + שורת suite_rooms לכל חדר (הודעת חדר מוכן לכל סוויטה); 170 tests, build נקי.
2026-07-07 | HK WA → manager approval gate + live guest match (session 134c) | `_shared/housekeepingGuestLookup.ts`+`roomPendingApprovalPush.ts` — חיפוש אורח הגעה-היום לפי מספר/שם סוויטה; WA `14✅`→אותו `ממתין לאישור` כמו טאבלט; push+ack עם שם אורח; AICopilot `arrival_date=היום`; functions: whapi-webhook+room-pending-approval-notify.
2026-07-07 | AICopilot bell fix — housekeeping ready signal (session 134b) | `AICopilot.js` — realtime INSERT+UPDATE; חיפוש אורח בחלון שהייה + `guestRoomMatchesSuiteId` (מספר בלבד↔שם סוויטה); פעמון גם בלי התאמת אורח; `suiteRegistry.guestRoomMatchesSuiteId`.
2026-07-07 | Housekeeping WA observer Phase 1+check-in (session 134) | `WHAPI_HOUSEKEEPING_GROUP_ID` — ready (`N✅`)→ממתין לאישור+ack; **צ'ק אין** (`17 צ׳ק אין`/`check in`)→`guests.checked_in`+`room_status` תפוס+ack; migration 150; whapi-webhook; 7 tests.
2026-07-07 | Guest handoff copy — removed staff names from `CANONICAL_STAY_CHANGE_HANDOFF_MSG` (`automationSchedule.ts`); deployed 47e82a7 — webhook/cron/automation-queue/notify-manual-task.
2026-07-07 | Smart Guest Profile ETA sync + ops approval gate (sessions 132–133, deployed 2436c0e) | `_shared/guestEta.ts` — חילוץ HH:MM (עברית+אנגלית AM/PM) + `persistGuestEta` (gate `arrival_date>=היום` ישראל, בלי alerts); webhook Tier-0 refactored; `pickGuestFromRows`→`israelYmd`; GuestsPage מציג 🕐 ETA + מיון ETA בסקופ «היום»; migration 149 `pending_approval` + OperationsBoard אישור לפני שיגור Whapi; functions: webhook+notify-manual-task; 158 tests.
2026-07-07 | ACC schedule date+stage picker (session 131b) | `QueueBulkScheduleModal` — מצב «לפי שלב» עם בחירת תאריך+שעה (ייבוא מאוחר / שלב 1); גם «לפי יום הגעה» עם תאריך שליחה נפרד; `defaultStaffScheduleDateYmd` ב-`israelTime.js`; 140 tests.
2026-07-07 | ACC Live Queue bulk schedule (session 131) | migration 148 `staff_scheduled`+`staff_schedule_tasks_batch`; `QueueBulkScheduleModal` (לפי יום/שלב/הודעה); cron+automation-queue מכבדים תזמון צוות; 138 tests. migration 147: `sync_suite_arrivals` Tier-2 לא דורס אורח-קבוצה שני (שם+טלפון שונים, אותה הזמנה); `automation_muted` one-way בייבוא; `_getSyncProfileIndices` משתמש ב-`mergedCandidates.guestPhone`.
2026-07-07 | Import enrich mode (session 129) | Doc 2 מצב ברירת-מחדל «השלמת פרופיל» — `enrichOnly` ב-`sync_suite_arrivals` (migration 146) + `buildEnrichGuestPatch`/`getDbMatchDiffLabels`; עמודת «הבדל מול DB»; 🆕 נוצר / קיים מקבל רק שדות ריקים; «ייבוא מלא» נשמר; 131 tests.
2026-07-06 | Inbox guest reaction UX (session 128) | תגובות אמוג'י מ-Meta מוצגות כ-chip קומפקטי (לא בועה לבנה); `intent=guest_reaction`+migration 145; lookup snippet עם fallback ל-outbound אחרון; `metaWamid.ts` שומר wamid ב-inbox_reply/broadcast/pipeline; לא נספרות ב-unread; 128 tests.
2026-07-06 | P1+P2 Inbox live + CRM workflow (session 125) | מיון roster «פעילות» טהור (הוסרה הצמדת לא-נקרא); `mergeThreadRows` — רענון היסטוריה לא מאבד הודעות + DESC window; גלילה לא קופצת על עדכוני roster; שם אורח ב-GuestsPage/GuestDashboard פותח שיחת DREAM BOT (👤 לפרופיל); טוסטים ל-duplicate_blocked ב-AICopilot/SuitesDashboard; כפתורי פורטל/שלב 2 טוענים טיוטה לעריכה (לא שליחה ישירה); סנכרון פילטר היום/מחר צ'ק-אין↔Inbox; 123 tests. אומת חי: בוט קולט ועונה (13:53), Inbox מציג.
2026-07-06 | P0 suite/day-pass routing guard + Inbox reactions (session 125) | `_shared/suiteNames.ts` — חדר-סוויטה מנצח `room_type` שגוי ב-`checkEligibility`+whatsapp-send (שער `suite_daypass_stage_gate` חדש); ACC/queue מפיצים `effectiveSuite`+`roomTypeConflict`; באדג' «⚠ סתירת סיווג» ב-GuestsPage/GuestDashboard/ACC; webhook — reaction נרשם כ«❤️ תגובה על ההודעה: «snippet»» (log-only); 117 tests.
2026-07-06 | Webhook pipeline hardening | `sendReply`/`sendTemplate` → `sanitizeMetaRecipientPhone`; ingest non-text ל-Inbox; GET `?diag=1`; Inbox מציג «אחרון ב-DB»+אזהרת Meta.
2026-07-06 | Inbox mount revalidate + intent widen | mount תמיד `fetchAll` (cache=paint בלבד); `fetchSince` gte; emergency `fetchAll` בלי Realtime; migration 143 intent; webhook insert error logs.
2026-07-06 | Inbox live sync fix | `fetchSince` gt+DESC; `id` מנורמל; כפתור «🔄 רענן רשימה»; webhook לא ממשיך כש-INSERT ל-whatsapp_conversations נכשל.
2026-07-05 | Inbox guest profile sync hardening | מפת `guests` חיה = מקור אמת יחיד; ניקוי שדות רפאים ב-cache+הודעות; תג «לא רשום» במקום «מחר» לאורח שנמחק; 111 tests.
2026-07-05 | Inbox departed badge fix | `getGuestArrivalRosterLabel`+`getGuestTimingBadge`: עזיבה מנצחת `checked_in` ישן — תג «⚪ אחרי עזיבה» במקום «🟢 בריזורט»; 110 tests.
2026-07-05 | Resort Pulse attention sync fix | «דורש טיפול» = `human_requested` פעיל ב-Inbox (לא דגלי guests ישנים); realtime על whatsapp_conversations.
2026-07-05 | Unified Ops UI sprint (session 124) | ResortPulseBar+GlobalCommandPalette(Ctrl+K)+GuestJourneyTimeline ב-GuestContextDrawer; receptionist nav מצומצם; OperationsBoard כפתור 💬 שיחה+SLA; ACC תצוגת יום (`previewAt` ב-automation-queue); 107 tests.
2026-07-05 | checkout_fb lifecycle gate (hotfix) | `_shared/pipelineLifecycle.ts`+`checkEligibility`+whatsapp-send/cron: שלב 5 חסום לעתידיים/no-show/תאריכים שבורים; checked_out מותר ל-post-stay; 7 tests.
2026-07-05 | Guest delete full system sync | RPC `delete_guest_profile` (141)+`guests` REPLICA IDENTITY FULL (142); `_shared/guestOutboundGuard.ts` חוסם שליחה ללא אורח פעיל; Inbox תג «לא רשום»+באנר; 96 tests.
2026-07-05 | Inbox guest-delete sync | WhatsAppInbox: realtime DELETE/INSERT על guests; `syncInboxContactWithGuestMap` מנקה פרופיל שנמחק; groupByPhone last-message-wins; 9 tests.
2026-07-05 | Inbox supervision + roster segments | WhatsAppInbox: כפתור «רענן היסטוריה»+מונה X/Y הודעות ב-DB; סינון/מיון (בריזורט/מחר/יומיים/עתיד/בטיפול/לא נקרא)+קיבוץ לפי סקשן; `classifyInboxRosterSegment` ב-guestTiming; 7 tests.
2026-07-05 | Arrival confirm reconcile (נתי גולן fix) | `_shared/arrivalConfirmation.ts`: matcher מחוזק (bold/emoji), lookup suffix; webhook catch-up בדדופ-skip; cron backfill מ-inbox לפני Stage 2 reconcile; 4 tests.
2026-07-05 | Shared guest date filter (Task 4) | `CheckinTimelineFilterBar`+`useCheckinTimelineFilter`+sessionStorage sync — אותו מסנן (היום/מחר/7 ימים/ארכיון+תאריך מדויק) ב«ניהול אורחים» ו«צ'ק-אין»; `applyCheckinRosterFilter` ב-guestCheckinMatrix.
2026-07-05 | Inbox room-ready quick action (Task 3) | WhatsAppInbox כפתור «🛎️ חדר מוכן» — ממלא textarea מ-room_ready_reminder (חלון פתוח) או dream_room_ready1 לפני שליחה ידנית.
2026-07-05 | Automation duplicate shield (Task 2) | migration 139 `duplicate_blocked` ב-notification_log; `_shared/automationDuplicateGuard.ts`; whatsapp-send/webhook חוסמים+מתעדים כפילויות; ACC פאנל+היסטוריה+שגר המוני.
2026-07-05 | Queue→Inbox deep link + Stage 2 hotfix | ACC תור חי: שם אורח לחיץ→wa_inbox; badges אימות שליחה (notification_log); webhook: intercept «כן מגיעים» לפני auto-checkin, lookup טלפון multi-row, fallback whatsapp-send pipeline; deployed c05a001.
2026-07-05 | Stage 2 false-flag backfill | migration 138 מאפס msg_stage_2_arrival_sent כשאין notification_log מוצלח; cron reconcile בודק לוג ולא רק דגל.
2026-07-05 | morning_suite timing + Stage 2 reconcile | תזמון ישראל ב-automationSchedule; migration 137 מחזיר morning_suite ל-06:00 אם הוגדר 15:00; cron מסנכרן arrival_confirmed→stage_2; webhook מעדכן wa_window לפני שליחה.
2026-07-05 | whatsapp-webhook Stage 2 staff-claim fix | אישור «כן מגיעים» שולח Stage 2 גם כש-claimed_by פעיל; `msg_stage_2_arrival_sent` רק אחרי Meta send אמיתי; post-burst confirm לפני staff exit.
2026-07-05 | GuestProfileModal + stay meals + portal | פרופיל אורח: סוג הזמנה (פרטי/קבוצה), ריכוז חדרים מ-suite_rooms, פנסיון (חצי/מלא/ערב)+שעות ארוחה; migration 136; פורטל אורח מציג meals_itinerary; 82 tests.
2026-07-05 | import multi-room display+sync dedupe | עמודת «קבוצה»: `חדר 1 מ־2`/`חדר 2 מ־2` (לא «2 חדרים»×2); סנכרון: profile אחד לכל הזמנה+טלפון+תאריך; `room_count` לפי מספר שורות; 76 tests.
2026-07-05 | import multi-room same guest | `isSameBookingGuest` — אותה הזמנה+טלפון+תאריך: חדר שני לא ⚠ conflict; תגית «🔄 קיים · 2 חדרים»+עמודת קבוצה.
2026-07-05 | import order-aware sync (migration 134) | `sync_suite_arrivals` מתאים אורח לפי order+date+phone → order יחיד → phone+date; `buildExistingGuestsLookup`/`findExistingGuestRow`; 73 tests.
2026-07-05 | merge 544fdef deployed | push main + db push 132/133 + functions whatsapp-webhook/send/guest-portal-data; Stage 2 מיידי+spa_date מאוחדים.
2026-07-05 | spa_date sync (portal+automation+WA) | סנכרון מלא: `_shared/spaSchedule.ts` ב-webhook/send/portal-data; placeholders `{{SPA_*}}` עם תאריך+שעה; פורטל+Inbox+ייבוא EZGO ממלאים `spa_date` מ-arrival_date; migration 131_guests_spa_date.
2026-07-05 | AddGuestModal + israeliTime.js | פרופיל אורח: תאריך טיפול ספא (`spa_date`) + בוחר שעות 24ש' ישראלי (dropdown 07:00–22:00); תצוגה משולבת ב-GuestsPage/GuestDashboard/GuestContextDrawer.
2026-07-04 | ArrivalImportPanel + AutomationControlCenter + migration 132 | ייבוא opt-in: ברירת מחדל «ללא וואטסאפ»; עמודת אוטומציה בגריד; migration 132 שומר automation_muted ב-reimport; ACC תור חי — פאנל הפעלה מרוכזת לאורחים מושתקים.
2026-07-04 | whatsapp-webhook + migration 133 | Stage 2 מיידי אחרי «כן מגיעים»: webhook מתעלם מ-offset_hours בלחיצה חיה; stage_2_arrival לפני stage_2_pay; migration 133 מאפס offset_hours.
2026-07-04 | whatsapp-send + _shared/metaTemplateLog + migration 130 | Inbox WYSIWYG שלב 1 deployed: גופי תבניות מלאים (night_before_suites/_shabbat וכו') + fallback Meta API; migration 130 תיקון wa_template_name column.
2026-07-04 | whatsapp-send + whatsapp-webhook + migration 129 | אוטומציה: cron תמיד Meta template (לא bot_scripts בגלל חלון 24ש'); mid_stay ללא «בוקר טוב»+חלון 10–12; Stage 2 רק מ-bot_scripts (בלי fallback מומצא).
2026-07-04 | automationSchedule.ts + whatsapp-webhook | חיזוק מגן תשובה קטועה בשעות כניסה: `looksLikeCheckInHoursReply`+`resolveTruncatedReplyFallback` ב-sendReply chokepoint; Tier-0 מורחב; 4 בדיקות `checkInPolicyFaq.test.js`.
2026-07-04 | WhatsAppInbox.js + guestTiming.js | Inbox: unread רק כשאין מענה אחרי inbound; פילטר «אחרי עזיבה» מפריד אורחים שעזבו מהרשימה הראשית.
2026-07-04 | whatsapp-webhook + automationSchedule.ts | Tier-0 שאלות שעות כניסה/חדר (`isCheckInPolicyQuestion`+`buildCheckInPolicyReply`); מגן תשובה קטועה (`isReplyObviouslyTruncated`); Gemini מחבר כל חלקי טקסט; `buildSystemPrompt` כולל כניסה 12:00+שבת 18:00.
2026-07-04 | GuestsPage.js + App.js + BotConfigPanel.js + whatsapp-send | מובייל צ'ק-אין: כרטיסי אורח במקום טבלה שבורה; overflow גלובלי; פאנל שעות כניסה חול/שבת/חג בהגדרות בוט; resolveDayTimings מ-bot_config.
2026-07-04 | WhatsAppInbox.js | הוסר צבע סגול לשורות «בריזורט» ברשימת DREAM BOT — נשארה תגית 🟢 בריזורט בלבד.
2026-07-04 | automation-queue + AutomationControlCenter + automationSchedule | תור חי: שלב 2 (stage_2_arrival) גלוי בין שלב 1 ל-2.5 — «ממתין לאישור הגעה» לפני כן, מיידי אחרי; תיקון סינון event_immediate + awaiting_confirmation.
2026-07-04 | whatsapp-webhook + automationSchedule + guestRoomResolve | בקשות אורח (guest_request): פתרון חדר מ-guests/suite_rooms לכרטיס Whapi; SLA (15ד amenities / 30ד maintenance); פיצול משק/תפעול; Tier-0+LLM גם ביום הגעה (לא רק checked_in).
2026-07-04 | WhatsAppInbox.js + App.js | מובייל DREAM BOT: ניווט stack (רשימה או שיחה מלאה) במקום slide שבור ב-RTL; כפתור חזרה; הסתרת toolbar+תפריט תחתון בשיחה.
2026-07-04 | WhatsAppInbox.js | תיקון מובייל עברית: overflow+RTL הציג חצי ריק — clip wrapper ב-LTR, slide תמיד translateX(-50%).
2026-07-04 | WhatsAppInbox + guestTiming.js | רשימת שיחות: במקום «תואם מהמערכת» — תגית הגעה יחסית (היום/מחר/עוד יומיים/בריזורט); גם בכותרת השיחה.
2026-07-04 | WhatsAppInbox.js + whatsapp-send | כפתור 🔁 «שלח שוב הודעת הגעה (שלב 2)» בפעולות מהירות; manual_script+stage_2_arrival עם resolveStage2ArrivalPlaceholders מלא (שם/ספא/פורטל).
2026-07-04 | whatsapp-webhook | Stage 2 fix: «כן מגיעים» תמיד שולח stage_2_arrival (לא LLM/ספא) — הוסר gate על arrival_confirmed; handleStage2ArrivalConfirmation משותף; post-burst intercept.
2026-07-04 | WhatsAppInbox.js + staffDeepLink.js + App.js + index.js | Inbox: שעה מדויקת בכל בועה (תאריך+שעה אחרי היום); כפתור 📱 QR לפתיחה מהירה בטלפון; deep link ?page=wa_inbox אחרי login.
2026-07-04 | whatsapp-send + migration 128 | שעות הגעה: כניסה למתחם תמיד 12:00 (גם שבת); צ׳ק-אין 15:00 חול / 18:00 שבת; תיקון applySaturdayCheckInTimeOverride שלא הפך 12:00→15:00.
2026-07-04 | whatsapp-send + migration 126 | Stage 2.5 שבת: cron תמיד שולח night_before_suites/_shabbat (לא session בגלל חלון 24ש'); isShabbatArrivalDate מרכזי; שעות שבת 15:00/18:00 ב-bot_config.
2026-07-04 | ezgoParser.js | ייבוא Doc 2: זהות מ-sRemark רק כש-sClientFullName כפול בקובץ; שורה יחידה=עמודה בלבד (תיקון «יום הולדת» כשם).
2026-07-04 | ezgoParser + ArrivalImportPanel + detailedReservationParser | ייבוא Doc 2: שם בהערה+טלפון בעמודה; 📵 שקיפות לפני סנכרון; תיקון CSV ש"ח (24 שורות במקום 7).
2026-07-04 | ezgoParser.js + ArrivalImportPanel.js | חילוץ חכם מהערות: שם+טלפון מ-sRemark (חיתוך בחדר/שח/₪, אורח ליד טלפון ב-/+, דמה 111); ⚠ שם חשוד.
2026-07-04 | ezgoParser.js + guestImportIntelligence.js | קבוצות מוניציפליות: טלפון מזמין דמה (111) לא נכנס לפרופיל — זהות מההערות; שורות לא נחסמות כ-⛔ umbrella; automationMuted נשאר.
2026-07-03 | ArrivalImportPanel + suiteRegistry + guestImportIntelligence + migration 125 | סנכרון Doc 2 מדויק: שיוך סוויטה מ-roomName+suiteType, 🔄/⚠ בלי false conflict, room scoped ל-arrival_date.
2026-07-03 | whatsapp-send | Session scripts: `applySaturdayCheckInTimeOverride` — הגעה בשבת מחליף 15:00→18:00 בטקסט bot_scripts בזמן שליחה (ללא שינוי DB).
2026-07-03 | whatsapp-send + WhatsAppInbox.js | Dispatch transparency: לוג `[META]`/`[SESSION]`+כפתורים ב-inbox; 🔵/🟢 ב-UI; התראת Whapi לאדמין על כשל שליחה.
2026-07-02 | automationSchedule.ts + whatsapp-webhook | בקשת בלונים לחדר → לוח בקשות (guest_alerts) בלבד, לא תפעול; תשובה קבועה לקבלה+נציגת בלונים; secret אופציונלי BALLOON_VENDOR_PHONE.
2026-07-02 | automationSchedule.ts + whatsapp-webhook | Allowlist + dispatch matrix: תפעול→Whapi EN+tasks; קבלה/בקשות→tasks; שאר→לוח בקשות/KB.
2026-07-02 | whatsapp-send + migration 119 + whatsapp-cron + GuestsPage | Idempotency guard `room_ready_notified` — חסימת כפל הודעת «חדר מוכן»; איפוס ב-checked_out.
2026-07-02 | HousekeepingTabletView.js + RoomBoard.js + migration 118 | Jacuzzi ping-pong: סטטוסים ממתין לג'קוזי/מוכן לפיניש, כפתורי קרא לג'קוזי+סיימתי פיניש, מסגרת cyan+pulse.
2026-07-01 | ArrivalImportPanel.js | תיקון תצוגת גריד שיבוץ: ביטול סינון ספא, טקסט כהה, מזהה שורה ייחודי, חדר גולמי מהקובץ.
2026-07-01 | ArrivalImportPanel.js | תיקון גריד ריק במצב שיבוץ סוויטות — ביטול סינון ספא + עמודות שם/הזמנה/חדר.
2026-07-01 | ArrivalImportPanel.js | Doc 2: מצב «עדכון שיבוץ סוויטות בלבד» — UI toggle + UPDATE ממוקד ל-room בלבד (התאמה order_number/שם).
2026-07-01 | WhatsAppInbox.js + whatsapp-webhook | «קח שיחה» משתיק בוט לאורח ספציפי (guests.claimed_by); תיקון UI שאיפס claim בהודעה חדשה.
2026-07-01 | ReceptionChecklist.jsx + receptionChecklistTemplate.js | צ'קליסט: עריכה/מחיקת משימות, ניקוי שורות ישנות, שדה «מי ביצע» (שם חופשי), מעקב לפי שמות בפועל.
2026-07-01 | WhatsAppInbox.js + guestTiming.js | DREAM BOT סגול רק ל-status=checked_in; סטטוס מטבלת guests מנצח join ישן.
2026-07-01 | ReceptionChecklist + receptionChecklistTemplate | צ'קליסט קבלה: 21 משימות verbatim מהטופס הפיזי, עמודת הערות דיגיטלית, מטריצת אלונה/שיראל/אורן, איפוס 04:00.
2026-07-01 | GuestsPage.js + WhatsAppInbox.js | צ'ק-אין: מחיקה כללית לנבחרים (במקום איפוס ספא); DREAM BOT — סגול ל-checked_in מחוזק דרך מפת טלפון+סטטוס.
2026-07-01 | GuestPortal + guest-portal-data/spa-request + migration 115 + ReceptionChecklist | פורטל: הסתרת כפתור ספא כשיש spa_time / toggle גלובלי; תשובת Concierge Meta; צ'קליסט קבלה — מטריצת חתימות סיוון/שיראל/אלונה.
2026-07-01 | guest-portal-spa-request + GuestPortal + whatsapp-webhook | בקשת ספא מהפורטל: requires_attention+guest_notes, Whapi DM קנוני לאורח, LLM guard מפני קישורי ספא.
2026-07-01 | ArrivalImportPanel.js + detailedReservationParser.js | דוח מפורט: סינון עם/בלי חדרים, כפתור ייבוא דינמי (סוויטות/בילוי יומי), `profile_type`+`profile_batch_type` ב-payload.
2026-07-01 | GuestsPage + guestCheckinMatrix + migration 114 | מטריצת צ'ק-אין: רשימה פעילה (הגעה היום / checked_in במהלך שהייה), ארכיון «אורחים לאחר שהות», auto 15:00+checkout, סטטוס checked_out.
2026-07-01 | ReceptionChecklist.jsx + migration 114 | צ'קליסט קבלה יומי DB: 3 אקורדיונים, progress+date audit, operator+timestamp, reset 04:00 Israel.
2026-07-01 | notify-manual-task + whatsapp-cron | תרגום EN ל-Whapi רק למחלקות תפעול/משק; auto_checkout ב-cron לפי departure_date.
2026-07-01 | notify-manual-task + fieldOpsTranslation.ts | משימה ידנית/inbox_routed: תיאור עברית → Gemini EN לכרטיס Whapi בלבד; DB נשאר עברית. מודול משותף גם ל-guest_request ב-webhook.
2026-07-01 | App.js Dashboard (Phase 2a UI) | Luxury stat-cards: gold top accent, Playfair values, semantic stat-sub tokens; 2-col grid @768px + 390px readable sizes; urgent banner tokens.
2026-07-01 | src/App.js (Phase 0 UI) | Design tokens (:root status/spacing/shadow/hit-target) + utility classes (.u-touch-*, .u-badge-nowrap); mobile-bar safe-area; stat-card hover polish.
2026-07-01 | whatsapp-webhook + whatsapp-cron + automationSchedule.ts | Unified routing matrix: auto check-in 15:00 (cron+webhook); תפעול→Whapi+תרגום EN; קבלה/בקשות→tasks בלבד (ספא/הארכה); מגן stay-change מעודכן (אדיר ואפק).
2026-07-01 | whatsapp-webhook + automationSchedule.ts | מגן הארכת שהייה/late checkout: intercept לפני LLM/upsell, handoff ניטרלי לצוות הסוויטות, needs_callback+attention_reason=date_change.
2026-07-01 | whatsapp-webhook + automationSchedule.ts | Tier-0 operational intercept: אורח checked_in + מילות מפתח חדר → tasks (guest_request) + requires_attention בלי LLM; תשובת קונסיירז' דטרמיניסטית.
2026-07-01 | HousekeepingTabletView.js + migration 113 + room-pending-approval-notify | לוח ניקיון: כשחדר+ג'קוזי נקיים → push להנהלה + realtime על room_status לפעמון AICopilot (אישור הודעה+צ'ק-אין).
2026-06-30 | docs/xos_agent_playbook.md | §11 one-message handoff — Mike approval loop (see browser → כן → commit/push ask → תעלה).
2026-06-30 | docs/xos_agent_playbook.md | §2.1 Mike Approval Loop + §12 Quick Card — agent waits after each change; Mike uses כן/תקן/עצור/תעלה only. desktop Wow + mobile comfort per phase; Phase 4 mobile shell+GuestsPage; Phase 5 real-phone QA; §11 full desktop kickoff prompt.
2026-06-30 | docs/xos_agent_playbook.md + .cursorrules | מדריך סוכן חכם: תקשורת חסכונית בטוקנים, UI phases 0–3, desktop-first, פרוטוקול שדרוג אוטומטי של חוקים.
2026-06-30 | BotSettings.js + migration 112 | עריכה ומחיקה לכללים ב-`xos_ai_rules` (RLS UPDATE/DELETE + כפתורי ✏️/🗑️ במוח הבוט).
2026-06-30 | BotSettings.js + whatsapp-webhook | כללים מ-«למד את המערכת» מוצגים במוח הבוט; chat/routing מופרדים; `ANTI_REASONING_LEAK_SUFFIX` + מגן עברית ב-sanitizeReply.
2026-06-30 | OperationsBoard.js + migration 111 | תור חי Realtime על `tasks` — 👍🏼 בקבוצת Whapi מעביר משימה ל«בוצע» בלי רענון; `applyTaskRowUpdate` כמו Inbox.
2026-06-30 | whatsapp-webhook + automationSchedule.ts | In-room keyword override (pending/expected→checked_in + in-house AI tone); insert-first wa_message_id dedup + burst coalescing (~1.8s) למניעת כפל תשובות.
2026-06-30 | WhatsAppInbox.js + inboxAlertSounds.js | צלילי התראה: אורח סוויטה (צליל גבוה) / לא נוכח בריזורט (צליל נמוך) על הודעה נכנסת חדשה.
2026-06-30 | WhatsAppInbox.js + guestTiming.js | רשימת DREAM BOT: אורחים נוכחים בריזורט היום — רקע/שם/אווטאר סגול (`isGuestInResortToday`).
2026-06-30 | WhatsAppInbox.js + notify-manual-task | ניתוב תחזוקה/משק בית מתיבת אורח → כרטיס Whapi לקבוצת התפעול (`[GUEST WA]`); אותו מנגנון כמו לוח תפעול.
2026-06-30 | AutomationControlCenter.js | תור חי: בחירה מהירה לפי שלב בכל יום הגעה (סמן הכל שלב 4 וכו׳); שגר המוני בפעימות 2.5ש׳ (כמו cron).
2026-06-30 | migration 110 + automationSchedule + AutomationControlCenter | Stage 4 `require_checked_in` toggle (default OFF for mid_stay); cron sends without check-in when disabled.
2026-06-30 | AutomationControlCenter.js + automation-queue | תור חי לפי יום הגעה; תיקון Stage 4 (not_checked_in→pending); arrivalDate ב-API.
2026-06-30 | ArrivalImportPanel.js | Doc 1 «ספא סוויטות בלבד»: פרסור שורות «לאורחי הסוויטות» בלבד; סנכרון לפי order_number+arrival_date לפרופיל קיים; מצב מלא נשאר זמין.
2026-06-30 | ArrivalImportPanel.js + ezgoParser.js | Import grid defaults to spa-only rows with «הצג את כל האורחים» toggle; sync still uses full dataset via _profileIdx; enrichProfilesFromExcel phone fallback when order join misses.
2026-06-30 | AutomationControlCenter.js | Queue tab: exclude sent/simulated/skipped from live queue; 🗑️ per-row + clear-all dismiss for blocked_by_meta (UI-only, no backend change).
2026-06-30 | CustomerProfilePane.js | Guest drawer: guest_alerts requests + collapsible guest_notes system log under "הערות ובקשות".
2026-06-30 | CustomerProfilePane.js | Smart profile chips + red alert badge + edit modal in guest name drawer (GuestsPage + GuestDashboard).
2026-06-30 | guestProfileSchema + guestProfile.ts | Removed profile options: VVIP, shuttle_needed (kosher_strict/halal/seafood allergy already absent).
2026-06-30 | WhatsAppInbox.js | Guest notes banner collapsed by default with ✕ dismiss; minHeight:0 on message scroll fixes overlap; slightly larger bubble text for readability.
2026-06-29 | CLAUDE.md §4/§6/§7 | Doc sync: receptionist RBAC (full Sidebar + ops/inbox/vouchers/data_sync); Record-Only ETA pipeline + arrival_time; future suite routing to 120363429859248777@g.us + LLM English translation.
2026-06-29 | whatsapp-webhook/index.ts + migration 108 | Record-only arrival time: regex extract HH:MM → guests.arrival_time + guest_notes audit; fixed Hebrew reply; no needs_callback/alerts/ops routing; log_guest_request prompt excludes ETA updates.
2026-06-29 | auth.js + App.js + OperationsBoard.js | Receptionist RBAC: full sidebar (staff + wa_inbox/data_sync/vouchers/ops); admin routes still blocked; create_ops_task for receptionist.

2026-06-29 | whatsapp-send/index.ts | Stage 2.5 force override: session_message always on manual Send Now — bypass window/Shabbat/Meta routing; fail visible on missing script.

2026-06-29 | detailedReservationParser.js + ArrivalImportPanel.js | CSV quote-safe parser (RFC4180 + בע\"מ fix); .csv read as text not SheetJS — fixes raw CSV dumped into guest name.

2026-06-29 | GuestPortal.js + guest-portal-data | Secure payment CTA below itinerary (payment_url/balance/pending only); muted gold styling. Build clean.

2026-06-29 | src/components/GuestPortal.js | ItineraryPanel + DayUseView: בסיס אירוח row (🍴 meal_location) directly under spa; conditional render; meal_time row separate.

2026-06-29 | detailedReservationParser.js + PriceDiscrepancyModal.js + ArrivalImportPanel.js | Dedicated «ייבוא דוח הזמנות מפורט» import: Excel serial dates, board-basis→meal_location, dual-price conflict modal, טלפון נוסף→guest_notes, חדרים→roomsQuantity/bookings.room_count; bypasses AI mapper. Build clean.

2026-06-29 | migration 106 + db history fix + AddGuestModal + whatsapp-send + AutomationControlCenter | Resolved duplicate migration 101 (renamed guest mute to 106, db push clean); lead_source/automation_muted on guest form; Stage 2.5 Send Now forces session+image when 24h window open.

2026-06-29 | migration 101 + ezgoParser + ArrivalImportPanel + automationSchedule + whatsapp-cron/send/webhook | Advanced PMS CSV (מקור הגעה): lead_source + automation_muted on guests; מחלקת מכירות rows imported but pipeline/cron/stage_2_pay muzzled; preset mapping for 01.7.26-style exports.

2026-06-29 | whatsapp-send + interactiveSend | Stage 2.5 session images: resolveStageSessionImageUrl + sendStageSessionMessage helpers; cron/Send Now routes to session when 24h window open; image+caption via sendImageMessage with wamid assert + session_image_failed on Meta error.

2026-06-29 | whatsapp-send + whatsapp-webhook | Re-added night_before_suites/_shabbat to TEMPLATE_IMAGE_HEADER_DEFAULTS (Meta requires IMAGE header); removed from TEMPLATE_NO_HEADER block list.

2026-06-29 | whatsapp-send + interactiveSend + whatsapp-webhook | Meta payload audit: IMAGE header only for dream_suite_reminder (removed night_before_suites/_shabbat — body-only templates); session_message_image_url no longer injected into template components; sendViaMeta uses sendImageMessage with strict image.link shape + wamid assert.

2026-06-29 | whatsapp-send/index.ts | Stage 2.5 rich-media fix: sendViaMeta switches to type:image+caption when image_url present; sendViaTemplate buildTemplateComponents() validates header/body/button params; night_before session+template paths pass automation_stages.session_message_image_url.

2026-06-29 | scheduled_tasks (104) + AutomationControlCenter + whatsapp-send template_test | Smart Dispatch Override: pending schedule warning modal (Israel time), שלח עכשיו + ⚡ force send with duplication guard (cancel scheduled_tasks + Manual Override log); isolated Meta template test tab (is_test:true).

2026-06-29 | UndoSnackbar + HoldToConfirmButton + QuietHoursGate + AdminChangelogDashboard + App.js/index.js | Fail-safes: RequestsBoard 6s undo snackbar; WhatsAppInbox hold-1.5s bot toggle; quiet-hours gate (22:00–08:00 IL) on all manual WA sends; admin changelog timeline at admin_updates + /admin/updates deep link.


2026-06-29 | whatsapp-webhook/index.ts | Phase 3: fetch xos_ai_rules (chat+routing) with 5-min cache + try/catch; append learnedRulesText to finalSystemPrompt (flows to Gemini systemTurn + Claude system).

2026-06-29 | WhatsAppInbox.js + RequestsBoard.js | Phase 2: AILearningButton integrated ? module=chat (DREAM BOT header) + module=routing (Requests Board action bar).

2026-06-29 | migration 103 + AILearningButton.jsx | Unified AI Learning Phase 1: xos_ai_rules table (RLS + cleaner lockdown) + reusable capture button component (not yet integrated).

2026-06-28 | CLAUDE.md + .cursorrules | ?12??13 Deploy checklist mandatory at end of every code session; needs_callback docs synced (session 59).

2026-06-28 | src/components/AddGuestModal.js | needs_callback checkbox label: "????? ?????? ???? ??" (removed misleading "??? ????" copy).

2026-06-28 | automationSchedule.ts + whatsapp-webhook/index.ts | Decouple needs_callback from bot logic: cron eligibility no longer skips guests with open callback; webhook no longer silences LLM/button routing when needs_callback=true (UI badges unchanged).

2026-06-28 | supabase/functions/whatsapp-cron/index.ts | Queue dispatch throttling: sequential batches (size 10) with 2.5s sleep between each whatsapp-send call to avoid Meta burst rate limits.

2026-06-28 | src/components/GuestPortal.js | Removed duplicate concierge WA button from ItineraryPanel empty state ? SuiteQuickActions remains the single CTA.

2026-06-28 | supabase/migrations/099_daypass_stage4_stage5_split.sql + automationSchedule.ts + whatsapp-cron/index.ts + AutomationControlCenter.js | Stage 4 restore: migration 099 pushed to prod (was local-only); mid_stay_daypass/checkout_fb_daypass rows + is_active=true; resolveStageSchedule returns scheduledFor even when skipReason set (queue monitor visibility); CORE_PIPELINE_STAGE_KEYS registry + Pulse UI missing-stage warning; cron mid_stay eval logging.

2026-06-28 | supabase/functions/whatsapp-send/index.ts + AutomationControlCenter.js + src/utils/whatsapp.js | Stage 2.5 root fix: removed 24h free-text hijack ? cron/default always sends night_before_suites/_shabbat Meta template; session text only on manual force_channel=session_message; entry_time/check_in_time substitution in script path; frontend THREE_PARAM drift corrected.

2026-06-28 | whatsapp-cron/index.ts + whatsapp-send/index.ts | Stage 2.5 debug: cron logs active_stages + night_before eligibility; whatsapp-send logs trigger/BRANCH_D/night_before routing; fix force_meta bypass + resolvePipelineTemplateName stale DB template.

2026-06-28 | supabase/functions/_shared/metaPhone.ts + whatsapp-send/index.ts + _shared/interactiveSend.ts | Meta silent-delivery fix: sanitizeMetaRecipientPhone strips non-digits, 05?9725, applied to all Graph API `to` fields before fetch.

2026-06-28 | src/components/GuestPortal.js | SuiteQuickActions: persistent concierge WhatsApp CTA in SuiteView (CONCIERGE_WA) ? always visible even when itinerary has spa/meal data; meal_location logic untouched.

2026-06-28 | src/components/ArrivalImportPanel.js | Auto-date detection (filename DD.MM.YY + first-cell fallback) pre-fills arrival picker with FAIL VISIBLE confirmation banner; expanded _extractMealTime with HB/Half Board, Dinner, ?????, evening-range (18:00?21:30) patterns + spa-line guard.

2026-06-28 | src/components/ArrivalImportPanel.js | HTML parsing bugfix: _cellText DOM-walk converts <BR>?\n (was textContent, phone lost on suite rows); suiteSpaOnly?false (group filter by regex is enough); content-sniff HTML detection; meal_time column in spa preview table. Build clean.
2026-06-28 | src/components/ArrivalImportPanel.js | Doc 1 HTML support: parseHtmlDailyReport for EZGO .htm exports (nested table ? parseComprehensiveReport); suiteSpaOnly filter (?????? ????????/????? ??????, skip group slots); multiline order-cell phone fix; meal window regex (?- HH:MM); DropZone accepts .htm/.html. npm run build clean.

2026-06-28 | automation-queue/index.ts + src/components/AutomationControlCenter.js | Step 3 ? Blocked-by-Meta UI (Phase 2 dashboard): (1) automation-queue now includes blocked_by_meta rows in attentionRequired. (2) New orange "?? ????? ?????? Meta" accordion card in Live Queue tab ? shows guest/stage/template-name with badge-orange; explanatory banner "?????? ?????? ?????? ? ? Meta ??? ?????". (3) Queue table status badge handles blocked_by_meta ? badge-orange "?? ????? ??????". (4) History table status badge handles blocked_by_meta ? badge-orange "?? ????? ?????? Meta". (5) History tab filter chips: ??? / ? ???? / ?? ????? Meta / ? ????? ? active chip has bold border. (6) SCRIPT_KEY_FRIENDLY +3 entries for pre_arrival_2d/mid_stay/checkout_fb (migration 100 scripts). npm run build clean; automation-queue deployed.

2026-06-27 | docs/ created, .cursorrules added | System knowledge reorganized: architecture.md + active_sprint.md generated from 11 source docs; 6 stale docs archived to docs/archive/; .cursorrules workflow directives installed.
2026-06-27 | src/components/AutomationControlCenter.js | Phase 1 Live Monitor UI: "???? ?????" wrapped in collapsible accordion (auto-opens on errors, closed when clean); top-5 most-recent-first with 260px max-height + gold custom scrollbar; "? ????? ?????? ???" button dismisses all active alerts from operational view.
2026-06-27 | src/components/AutomationControlCenter.js | Auto-fill variables fix: added resolvePreviewTimings() (UTC-safe, mirrors whatsapp-send resolveDayTimings), ARRIVAL_TIME_STAGE_KEYS set, per-card previewTimings state + "? ??? ????????" button in StageCard; MetaTemplatePreviewBox now resolves {{1}}=guest name, {{2}}=entry time, {{3}}=check-in time (12:00/15:00 weekday, 15:00/18:00 Shabbat). Room/date wrong-slot mapping eliminated.
2026-06-27 | supabase/functions/whatsapp-send/index.ts + src/components/AutomationControlCenter.js | Fully deterministic template routing (session 57): (1) morning_suite/morning_welcome now have a dedicated fast-path (morningDispatch) that reads arrival_date UTC day-of-week ? Saturday?dream_welcome_morning_shabbat, else?dream_welcome_morning. Only {{1}}=name passed; entry/check-in times are baked into each template's body. Safety: Shabbat template failure retries with weekday template + console.warn. PORTAL_BUTTON_TRIGGERS emptied (both morning triggers handled in fast-path). PIPELINE_VARS simplified to [name] for safety. (2) AutomationControlCenter: ARRIVAL_TIME_STAGE_KEYS + resolvePreviewTimings + per-card auto-fill state removed; replaced with DETERMINISTIC_ROUTE_STAGE_KEYS and a read-only routing info panel showing weekday/Shabbat template names per stage. npm run build clean; whatsapp-send deployed.
2026-06-27 | src/components/AutomationControlCenter.js | Meta Fallback dropdown: replaced read-only template display in StageCard with live <select> bound to patchStage({meta_template_name}); lists all Meta templates (? APPROVED first, ? PENDING kept so current selection never silently disappears); added night_before_suites + night_before_suites_shabbat + dream_suite_reminder to META_TEMPLATE_FRIENDLY map; auto-fill/preview panels conditioned on selection.
2026-06-27 | supabase/functions/whatsapp-send/index.ts | Dream Bot Dispatch consolidation: (1) TEMPLATE_IMAGE_HEADERS extended with night_before_suites_shabbat. (2) getLastInboundTimestamp() added ? queries whatsapp_conversations for raw last-inbound ts, explicit 24h math, independent of wa_window_expires_at. (3) night_before now has a dedicated 24h compliance engine + early-return fast-path: within 24h ? free text via night_before_reminder bot_script; outside window / null ? static approved template (Saturday?night_before_suites_shabbat, else?night_before_suites) with {{1}}=name ONLY ({{2}}/{{3}} removed). Safety gate: query failure defaults to template. (4) Removed stale resolveNightBeforeTimes/nightBeforeTimes/nightBeforeError code from generic BRANCH D. (5) PORTAL_BUTTON_TRIGGERS trimmed to morning_suite+morning_welcome (night_before owns its own portalButtonParam in fast-path).

2026-07-09 | migration 164 + WhatsAppInbox.js + whatsapp-webhook/whatsapp-send/whapi-webhook | Inbox channel isolation: `whatsapp_conversations.inbox_channel` (`meta`|`whapi`), separate roster threads per phone+channel, filter chips, outbound bind via `inbox_reply.inbox_channel`, Whapi 1:1 guest_dm ingest in whapi-webhook. **Deployed** (main 64b3738, migration 164, 3 functions).
2026-06-28 | src/components/GuestDashboard.js + src/components/GuestsPage.js | session-58 Operational Dashboard Sync: GuestDashboard edit parity (editingGuest + pencil button); GuestsPage suite-only bifurcation filter + badge; Room Ready WA Fail Visible fix. commit 5c602b2.
2026-06-28 | src/components/GuestPortal.js + src/components/ArrivalImportPanel.js | session-62 Portal bug-hardening: (1) Timezone fix ? `today` now uses `toLocaleDateString('en-CA', {timeZone:'Asia/Jerusalem'})` instead of UTC `.toISOString().slice(0,10)` so phase/countdown never shows wrong day after midnight local. (2) ItineraryPanel (SuiteView) + DayUseView inline block ? both now always render; show concierge WhatsApp CTA fallback instead of returning null when spa_time+meal_time both absent (FAIL VISIBLE ?0.3). (3) ArrivalImportPanel PATH A post-RPC spa_time/meal_time UPDATE scoped with `.eq("arrival_date", g.arrivalDate)` to prevent cross-date pollution for repeat guests. Build: Compiled successfully.
2026-06-28 | supabase/migrations/101_room_ready_bot_script.sql + supabase/functions/whatsapp-send/index.ts + src/components/SuitesDashboard.js | session-60 Room Ready Latch & Date Guardrail: (1) Migration 101: seeds bot_scripts row 'room_ready_reminder' (free-text body for 24h-open session path; {{GUEST_NAME}}/{{ROOM_NAME}} placeholders; self-test). (2) whatsapp-send: PIPELINE_TEMPLATE["room_ready"] renamed dream_room_ready ? dream_room_ready1; new room_ready fast-path inserted before BRANCH D generic fallback ? mirrors night_before pattern: last-inbound < 24h ? free-text from room_ready_reminder script; ?24h / no history ? dream_room_ready1 template with {{1}}=name, {{2}}=room; script-missing fallback to template; full log + flag stamp + conv thread. (3) SuitesDashboard: today guardrail (r.arrival_date === today); sendingWa Set state; sendRoomReady() handler (invokes whatsapp-send trigger:room_ready + optimistic msg_room_ready_sent stamp); dispatch button per room card (Disable Don't Hide ?0.2 ? greyed+tooltip for non-today arrivals / no guest match / already sent). npm run build: Compiled with warnings (pre-existing GuestPortal.js CONCIERGE_WA, not introduced here).
2026-06-28 | supabase/migrations/100_safe_dispatch_blocked_by_meta.sql + supabase/functions/whatsapp-send/index.ts | session-59 Safe Dispatch + Automation Diagnostic Mode (Phase 1 + Phase 2 DB+Edge): (1) Migration 100: notification_log.status CHECK widened to include 'blocked_by_meta'; 3 bot_scripts free-text seeds (pre_arrival_2d/mid_stay/checkout_fb); automation_stages.session_message_script_key wired for 5 stages; inline DO$$ self-test. (2) isMetaTemplateError() helper ? detects Meta error #132001 (template pending/not found) in error string. (3) All 5 pipeline catch blocks (night_before/day_pass_morning/morning_suite+welcome session-text/morning_suite+welcome template/BRANCH D generic) updated to 3-way ternary: timeout?timeout, Meta template error?blocked_by_meta, else?failed. Guest flag NOT stamped on blocked_by_meta (cron retries until template approved). (4) New morning 24h session-text block for suite morning_suite/morning_welcome: if wa_window open AND session_message_script_key set, sends stage_3_morning free-text (stage_3_morning bot_script), logs+returns early; if script missing/empty silently falls through to Shabbat-aware template fast-path. whatsapp-send deployed.
2026-06-28 ? parseHtmlDailyReport: added TD[2] board column (HB/BB) to c2Parts + 19:00 dinner default for HB/?????-??? guests with no explicit meal time; ?????-????? lunch groups excluded (ArrivalImportPanel.js)
2026-06-28 | src/components/SuitesDashboard.js + src/components/GuestPortal.js | UI refinement: (1) SuitesDashboard ? guests SELECT now includes meal_time; ??? meal_time badge added to each room card alongside existing ?? spa_time badge. (2) GuestPortal ItineraryPanel + DayUseView ? meal ItineraryRow now displays only meal_location (or fallback "????? ?????") instead of the operational meal_time timestamp; hides row entirely when meal_time is absent. npm run build: Compiled successfully.
2026-06-28 | src/components/ArrivalImportPanel.js + src/utils/ezgoParser.js | HTML parsing gaps A-E: (A) DOM-targeted <TH> date extraction (falls back to raw-text regex); (B) handleDoc1 auto-populates arrivalDate picker + FAIL VISIBLE banner from HTML date; (C) dinnerDefaultPhones Set replaced by boardDefaults Map ? full HB/FB/BB/RO board-basis classification (HB?19:00/??? ?????, FB?19:00/???? ??????, BB?????/????? ????, RO?null); (D) meal_location:null added to parseComprehensiveReport record struct; (E) sync PATH A/B + ezgoParser.enrichProfilesFromExcel use rec.meal_location with "????? ???????" only as fallback ? board-basis location now propagates to guests table.
2026-06-28 ? session 58b: EZGO HTML smart router (trimStart BOM fix), parseHtmlDailyReport tds<2 guard (was <4, dropped all 3-col rows), strict meal labels HB???? ??????/FB??????? ???/BB??? ????? ???? (null time, no guessing), extras-only c2Parts (board/meals removed from _extractMealTime path), PATH B enrichment-only (removed INSERT branch), enrichProfilesFromExcel meal_location propagates independently of meal_time.
2026-06-28 | supabase/functions/whatsapp-send/index.ts | Zero-failure template dispatch: removed pre_arrival_2d URL-button injection (dream_arrival_confirmation is QUICK_REPLY-only); template-name-based dynamic URL whitelist; force+meta_template early path bypasses window/idempotency/day-pass gates; resolveTemplateVars/resolvePipelineTemplateName helpers; night_before script-missing falls back to template; generic BRANCH D ok now reflects send status.
2026-06-28 | supabase/functions/whatsapp-webhook/index.ts | dateChangeReply + typed date-change handoffMsg updated to Mike's new copy (availability ask before staff callback).
2026-06-28 | supabase/functions/whatsapp-webhook/index.ts | Gemini askGemini: exponential backoff (4 attempts, Retry-After aware) on 429/5xx/timeouts before next model or Claude failover ? whatsapp-send has no Gemini path.
2026-06-28 | AICopilot.js + RequestsAlertWidget.js | Draggable FAB widgets: viewport clamp on drag, resize/out-of-bounds snap to default corner + clear stale localStorage, z-index 10400.
2026-06-29 | migration 105 + _shared/paymentLinkGuard.ts + whatsapp-webhook/whatsapp-send + AutomationControlCenter | Stage 2 Pay guardrails: direct_payment_url validation, ezgo_portal_url async recovery flag, failed_missing_link/processing notification_log statuses, idempotency + 3s inline recovery, dream_payment_and_workshops button token fix.
2026-06-29 | whatsapp-send/index.ts + AutomationControlCenter.js | Stage 2.5 zero-guard: force+open window always session image+text (ignores force_channel=meta_template); queue Send Now ungated + omits force_channel for night_before.
2026-06-29 | migration 107 + WhatsAppInbox.js + whatsapp-send/index.ts | Inbox realtime (whatsapp_conversations publication) + store/display actual sent text; legacy [סקריפט]/[תבנית] rows resolved in UI with guest placeholders; fetchSince gte watermark + thread auto-scroll.
2026-06-29 | futureSuiteRoomServiceRouting.ts + guest-portal-ops-request + sla-escalation-cron | Future suite room-service alerts → Whapi group 120363429859248777@g.us (replaces 972504025317 DM).
2026-06-29 | WhatsAppInbox.js | Bulk "ניקוי כל ההתראות": confirm guard, clears human_requested + needs_callback for visible alert contacts, optimistic roster update.
2026-07-07 | docs/xos_agent_playbook.md | session-131 Prompt-engineering hygiene: added §6 step 5b (spawn Plan agent for external verify on high-stakes changes instead of self-check only) + §6.1 Reset-on-Drift Protocol (stop and start fresh session instead of self-correcting mid-hallucination). Rest of the "role/task/constraints/examples/verify" formula was already covered by existing rules.
2026-07-17 | WhatsAppInbox.js + inboxSendErrors.js | Inbox: באנר + שגיאה כשחלון 24ש׳ Dream Bot סגור — «בחר מכשיר הסוויטות»; בדיקת Meta inbound בלבד.
2026-07-07 | docs/xos_agent_playbook.md | session-131b Anti-laziness hygiene: §6 step 1b Design Mode (3 architecture options, no code, before new/unclear features); step 3 reuse-first discipline; step 5 exact-match self-verify (flag shortcuts, no silent easier-version); new §6.2 QA (Mike names edge cases, self-review = first pass only on automation/payment/RLS); §7 No Auto-Piloting made an explicit hard rule (git/db/functions need approval, local build/test do not, one phase at a time).
2026-07-19 | executiveIdentity.ts + executiveAssistant.ts + staffNotifyTemplates.ts + staffAgentRoster.js + auth.js + App.js + migration 230 | Named agent personas: `assistantName` (נועה/מאיה/ליאת) on ExecutiveProfile, substituted via `{{assistant_name}}` in the shared persona template + Eliad's morning pulse opening line; `staffAgentRoster.js` `noa_mike`→`maya_mike` (fixed duplicate name); `executive_playbook` route + sidebar gated to `super_admin` only (was admin+super_admin); migration syncs existing DB rows.
2026-07-19 | _shared/*.test.ts | Test fixtures aligned with canonical suite rooms (`isEffectiveSuiteGuest`) and dining/checkout behavior changes; 348/348 _shared tests pass.
